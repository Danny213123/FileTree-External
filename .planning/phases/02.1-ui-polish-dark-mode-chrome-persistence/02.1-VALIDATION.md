---
phase: 02.1
slug: ui-polish-dark-mode-chrome-persistence
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-23
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `02.1-RESEARCH.md` § "Validation Architecture" (lines 737–782).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` via `cargo test` (no external test crate; existing Phase 02 suite = 55 unit + 2 integration = 57 tests passing) |
| **Config file** | `Cargo.toml` (no separate config) |
| **Quick run command** | `cargo test --lib desktop:: settings::` |
| **Full suite command** | `cargo test` |
| **Estimated runtime** | quick ~3–5 s · full ~10–15 s |

---

## Sampling Rate

- **After every task commit:** Run `cargo test --lib desktop:: settings::`
- **After every plan wave:** Run `cargo test`
- **Before `/gsd-verify-work`:** CI quad green (`cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo build --release && cargo test`) + light/dark screenshot pair committed to `images/`
- **Max feedback latency:** ~5 seconds (quick); ~15 seconds (full)

---

## Per-Task Verification Map

> Task IDs are placeholders until the planner emits PLAN.md frontmatter. Rows are grouped by decision/requirement; the planner will map each row to a concrete task ID during plan generation.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | settings | 1 | SET-01 / D-13 | — | `Settings.active_tab` JSON round-trip (default + custom + unknown variant preserved) | unit | `cargo test --lib settings::tests::active_tab` | ❌ W0 | ⬜ pending |
| TBD | settings | 1 | SET-01 / D-13 | — | Old settings.json (no `active_tab`) yields default `"details"` | unit | `cargo test --lib settings::tests::backward_compat_no_active_tab` | ❌ W0 | ⬜ pending |
| TBD | desktop-misc | 1 | D-11 | V5 input validation | `drive_letter_from_path` handles `C:\…`, `\\?\D:\…`, `\\server\share\…`, empty, non-Windows → falls back to `'C'` | unit | `cargo test --lib desktop::tests::drive_letter_from_path` | ❌ W0 | ⬜ pending |
| TBD | theme | 1 | D-05 | — | WCAG relative luminance (pure white = 1.0, pure black = 0.0, threshold-edge accent) | unit | `cargo test --lib desktop::theme::tests::accent_luminance` | ❌ W0 | ⬜ pending |
| TBD | theme | 1 | D-05 | — | `selected_text_for_accent` → `#000000` when luminance > 0.6, else `#ffffff` | unit | `cargo test --lib desktop::theme::tests::selected_text_threshold` | ❌ W0 | ⬜ pending |
| TBD | theme | 1 | D-05 | — | Palette constants match UI-SPEC exact RGB (regression guard against the `rgb(12,13,15)` → `#1e1e1e` rewrite — Pitfall 9) | unit | `cargo test --lib desktop::theme::tests::palette_matches_spec` | ❌ W0 | ⬜ pending |
| TBD | menu-ownerdraw | 2 | D-12 / D-06 | — | `draw_menu_item` pure-fn variant returns correct fg/bg per state flags (normal/hot/selected/disabled) | unit | `cargo test --lib desktop::paint::tests::menu_item_colors` | ❌ W0 | ⬜ pending |
| TBD | tabs | 2 | D-08 | — | `tab_cell_width(label, dpi)` returns `max(min_width, padded_label_width)` | unit | `cargo test --lib desktop::tabs::tests::tab_cell_width` | ❌ W0 | ⬜ pending |
| TBD | tabs | 2 | D-08 | — | Hit-test: click at x=120 with tabs at [0..100, 100..200, 200..300] returns `Some(1)` | unit | `cargo test --lib desktop::tabs::tests::hit_test_tab_index` | ❌ W0 | ⬜ pending |
| TBD | state | 1 | D-13 | — | `ActiveTab::Details.as_str() == "details"`; `ActiveTab::from_str("top") == Some(Top)`; `from_str("unknown") == None` | unit | `cargo test --lib desktop::state::tests::active_tab_enum` | ❌ W0 | ⬜ pending |
| TBD | status-footer | 2 | POL-03 | — | Existing `format_status_*` helpers (13 tests) continue green after `msctls_statusbar32` replacement | unit | `cargo test --lib desktop::paint::tests::format_status` | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/settings.rs::tests` — add `active_tab_roundtrip`, `active_tab_default`, `backward_compat_no_active_tab`
- [ ] `src/desktop/theme.rs` — new `mod tests` with `accent_luminance`, `selected_text_threshold`, `palette_matches_spec`
- [ ] `src/desktop/tabs.rs` — new `mod tests` with `tab_cell_width`, `hit_test_tab_index`
- [ ] `src/desktop/state.rs::tests` — `active_tab_enum` round-trip (extend existing mod or add)
- [ ] `src/desktop/paint.rs::tests` — extend with `menu_item_colors` (requires extracting color-decision logic to a pure fn)
- [ ] `src/desktop/mod.rs::tests` — `drive_letter_from_path` pure-fn extracted + tested

*No framework install needed — `cargo test` is project-native and runs in CI.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Dark-mode chrome coverage (menu / combobox / scrollbars / status footer / list-view header all dark) | D-03, D-05 | Visual; no automated GDI surface capture | Launch in dark mode → screenshot → diff vs `images/Screenshot-<date>-dark-after.png` reference captured during verification |
| First-paint flash absence | D-04, D-10 | Visual; timing-sensitive | Launch app at ¼-speed screen recording; verify no light frame between window creation and first paint |
| View-menu state sync | D-12 | Visual; interactive | Toggle `View → Dark Mode` via menu → close menu → reopen → confirm checkmark reflects new state. Repeat for Show Hidden / Show Files |
| Active-tab restore across launches | D-13 | Cross-process; requires settings.json persistence | Switch to Errors tab → close app → reopen → verify Errors tab is active |
| Drive-picker restore | D-11 | Interactive; depends on saved settings.last_path | Set last_path to `D:\Foo` → restart → confirm drive picker shows `D:` selected |
| Light/dark visual parity | D-07 | Visual; whole-window comparison | Capture `images/Screenshot-<date>-light-after.png` + `-dark-after.png`; diff against `images/Screenshot 2026-05-23 190727.png` (light reference) |
| Tab keyboard accelerators | POL-02 | Interactive | Press Ctrl+1..Ctrl+5 → confirm correct tab activates |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
