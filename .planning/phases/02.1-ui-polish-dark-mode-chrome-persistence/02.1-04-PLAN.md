---
phase: 02.1
plan: 04
type: execute
wave: 2
depends_on:
  - 02.1-01
  - 02.1-03
files_modified:
  - src/desktop/tabs.rs
  - src/desktop/state.rs
  - src/desktop/ffi.rs
  - src/desktop/paint.rs
autonomous: true
requirements:
  - SET-01
must_haves:
  truths:
    - "tabs.rs registers a 'FileTreeTabStrip' window class with custom WM_PAINT, WM_LBUTTONDOWN, WM_SIZE, WM_SETCURSOR"
    - "ActiveTab enum has 5 variants with as_str/from_str and a TAB_NAMES const array"
    - "draw_tab_strip renders 5 tabs with icon + label and a 2px accent underline under the active tab"
    - "draw_tab_strip reads the accent color from DesktopState.accent_color (Plan 02.1-03) — single source of truth, no hardcoded accent"
    - "Hit-test on click returns the correct tab index"
    - "Hovering a tab cell sets the cursor to IDC_HAND (no visual repaint)"
  artifacts:
    - path: "src/desktop/tabs.rs"
      provides: "Tab-strip window class, tab_window_proc, tab_cell_width, hit_test_tab_index, recompute_rects"
      min_lines: 150
    - path: "src/desktop/state.rs"
      provides: "ActiveTab enum + TAB_NAMES const + DesktopState.active_tab/tab_strip/tab_panels/tab_rects/icon_font fields"
      contains: "enum ActiveTab"
    - path: "src/desktop/paint.rs"
      provides: "draw_tab_strip helper called from tabs.rs WM_PAINT"
      contains: "fn draw_tab_strip"
  key_links:
    - from: "src/desktop/tabs.rs WM_LBUTTONDOWN"
      to: "parent window WM_COMMAND"
      via: "PostMessageW(GetParent(hwnd), WM_COMMAND, MAKEWPARAM(ID_TAB_DETAILS + index, 0), hwnd as Lparam)"
      pattern: "PostMessageW.*WM_COMMAND.*ID_TAB"
    - from: "src/desktop/paint.rs draw_tab_strip"
      to: "src/desktop/icons.rs draw_icon"
      via: "icons::draw_icon(hdc, x, y, codepoint, 14, fg) per tab cell"
      pattern: "icons::draw_icon"
    - from: "src/desktop/paint.rs draw_tab_strip"
      to: "src/desktop/state.rs DesktopState.accent_color"
      via: "2px accent underline at tab cell bottom for the active tab; accent passed in from caller or read from &DesktopState parameter — value originates from refresh_accent in Plan 02.1-03 and is updated live on WM_DWMCOLORIZATIONCOLORCHANGED in Plan 02.1-05"
      pattern: "accent_color"

threat_model:
  applicable: []
  not_applicable: [V2, V3, V4, V5, V6, V10, V14]
  threats:
    - id: T-02.1-04-01
      category: D
      component: tab WM_PAINT under nested with_state_mut
      disposition: mitigate
      mitigation: "Per state.rs reentrancy discipline, paint reads state via with_state_mut which uses try_lock — if locked, paint falls back to a safe default render (e.g., blank panel bg). No deadlock surface."
---

<objective>
Implement the new `src/desktop/tabs.rs` tab-strip window class — custom-painted, owner-drawn — and the `ActiveTab` enum + state fields it depends on. This is the strip itself; wiring it into the main window layout + command routing + persistence happens in Plan 02.1-05.

Purpose: D-08 owner-painted tab strip with 5 tabs (Details / Top / Extensions / Duplicates / Errors), each with a Bootstrap Icons glyph + label, accent underline under the active tab. NOT `SysTabControl32` (rejected per D-08 — hard to dark-theme, looks dated).

Output:
- `src/desktop/tabs.rs` (currently a 1-line stub): full window class registration, tab_window_proc, hit-test, cursor hand on hover, recompute_rects on WM_SIZE.
- `src/desktop/state.rs` enum `ActiveTab` + `TAB_NAMES` const array + new DesktopState fields (`tab_strip: Hwnd`, `tab_panels: [Hwnd; 5]`, `tab_rects: [Rect; 5]`, `icon_font: Hfont`); repurpose existing `active_tab: usize` → `active_tab: ActiveTab` per Pitfall 8 Option B.
- `src/desktop/paint.rs` new helper: `pub(super) unsafe fn draw_tab_strip(hdc: Hdc, rc: Rect, tabs: &[(char, &str)], active: usize, accent: Dword, state: &DesktopState)` — render 5 tabs with icon + label + (active) underline; reuse the existing `draw_toolbar_background` shape (paint.rs lines 104–202). The `accent` parameter is the live DesktopState.accent_color sourced from Plan 02.1-03's `refresh_accent` and refreshed on `WM_DWMCOLORIZATIONCOLORCHANGED` (Plan 02.1-05).
- `src/desktop/ffi.rs` constants for tab IDs + cursor + `LoadCursorW` if missing.
- 3 new unit tests in `tabs.rs::tests`: `tab_cell_width`, `hit_test_tab_index`; in `state.rs::tests`: `active_tab_enum`.

Plan 02.1-05 wires this into the layout — creating the strip HWND, the 5 panel HWNDs, command routing, restoring active tab on launch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-CONTEXT.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-01-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-03-PLAN.md
@src/desktop/tabs.rs
@src/desktop/paint.rs
@src/desktop/state.rs
@src/desktop/mod.rs

<interfaces>
<!-- From existing src/desktop/paint.rs::draw_toolbar_background (lines 104-202) -
     this fn is ALREADY a custom-painted tab strip with active-tab background +
     accent underline. The 02.1 draw_tab_strip is a structural copy adapted for
     content tabs. -->

Existing src/desktop/paint.rs surface to reuse:
- `pub(super) unsafe fn fill_rect(hdc: Hdc, rc: Rect, color: Dword)` (reused for tab bg + underline)
- `pub(super) unsafe fn paint_window(hwnd: Hwnd)` lines 67-102 (the GDI double-buffering template — copy this shape for the tab WM_PAINT handler)
- `pub(super) unsafe fn draw_text(hdc: Hdc, text: &str, rect: &mut Rect, format: u32)` (or similar; reuse for tab labels)

From src/desktop/icons.rs (Plan 02.1-01):
- `pub(super) const ICON_LIST_NESTED: char = '\u{F4A6}';` and 4 others (one per tab)
- `pub(super) unsafe fn draw_icon(hdc: Hdc, x: i32, y: i32, codepoint: char, size_px: i32, color: Dword);`

From src/desktop/theme.rs (Plan 02.1-03):
- `pub(super) fn palette_panel(state: &DesktopState) -> Dword` (tab-strip bg)
- `pub(super) fn palette_text(state: &DesktopState) -> Dword` (tab label + icon fg)
- `state.accent_color: Dword` (for the underline — set by refresh_accent and refreshed on WM_DWMCOLORIZATIONCOLORCHANGED in Plan 02.1-05)

New surface (this plan adds):

In src/desktop/state.rs:
  #[derive(Copy, Clone, Debug, PartialEq, Eq)]
  pub(super) enum ActiveTab {
      Details = 0,
      Top = 1,
      Extensions = 2,
      Duplicates = 3,
      Errors = 4,
  }
  impl ActiveTab {
      pub(super) const COUNT: usize = 5;
      pub(super) fn as_str(self) -> &'static str { TAB_NAMES[self as usize] }
      pub(super) fn from_str(s: &str) -> Option<Self> { /* match against TAB_NAMES; return Some(variant) or None */ }
      pub(super) fn from_index(i: usize) -> Option<Self> { /* checked cast 0..=4 */ }
      pub(super) fn from_command_id(id: isize) -> Option<Self> { /* (id - ID_TAB_DETAILS) → from_index */ }
  }
  pub(super) const TAB_NAMES: [&str; 5] = ["details", "top", "extensions", "duplicates", "errors"];
  pub(super) const TAB_LABELS: [&str; 5] = ["Details", "Top", "Extensions", "Duplicates", "Errors"];
  pub(super) const TAB_ICONS: [char; 5] = [
      crate::desktop::icons::ICON_LIST_NESTED,
      crate::desktop::icons::ICON_BAR_CHART,
      crate::desktop::icons::ICON_FILES,
      crate::desktop::icons::ICON_COPY,
      crate::desktop::icons::ICON_EXCLAMATION_TRIANGLE,
  ];
  // DesktopState field replacement: `active_tab: usize` → `active_tab: ActiveTab` (default Details).
  // New DesktopState fields:
  //   pub(super) tab_strip: Hwnd,
  //   pub(super) tab_panels: [Hwnd; 5],
  //   pub(super) tab_rects: [Rect; 5],
  //   pub(super) icon_font: Hfont,

In src/desktop/tabs.rs:
  pub(super) fn register_class(h_instance: Hinstance) -> Bool;   // RegisterClassW for "FileTreeTabStrip"; idempotent
  pub(super) unsafe extern "system" fn tab_window_proc(hwnd: Hwnd, msg: Uint, wparam: Wparam, lparam: Lparam) -> Lresult;
  pub(super) fn tab_cell_width(label: &str, dpi: u32, body_font: Hfont) -> i32;
  pub(super) fn hit_test_tab_index(x: i32, rects: &[Rect]) -> Option<usize>;
  pub(super) fn recompute_rects(hwnd: Hwnd, total_width: i32, dpi: u32, body_font: Hfont) -> [Rect; 5];

In src/desktop/paint.rs:
  pub(super) unsafe fn draw_tab_strip(
      hdc: Hdc,
      rc: Rect,
      active: usize,
      accent: Dword,
      tab_rects: &[Rect; 5],
      state: &DesktopState,
  );

In src/desktop/ffi.rs (additions if missing):
  pub(super) const ID_TAB_STRIP: isize = 114;
  pub(super) const ID_TAB_DETAILS: isize    = 3020;
  pub(super) const ID_TAB_TOP: isize        = 3021;
  pub(super) const ID_TAB_EXTENSIONS: isize = 3022;
  pub(super) const ID_TAB_DUPLICATES: isize = 3023;
  pub(super) const ID_TAB_ERRORS: isize     = 3024;
  pub(super) const IDC_HAND: usize          = 32649;
  pub(super) const WM_SETCURSOR: Uint       = 0x0020;
  pub(super) const WM_MOUSEMOVE: Uint       = 0x0200;
  pub(super) fn LoadCursorW(hInstance: Hinstance, lpCursorName: usize) -> Handle;  // if missing
  pub(super) fn SetCursor(hCursor: Handle) -> Handle;  // if missing
  pub(super) fn GetParent(hWnd: Hwnd) -> Hwnd;  // if missing
  pub(super) fn PostMessageW(hWnd: Hwnd, Msg: Uint, wParam: Wparam, lParam: Lparam) -> Bool;  // very likely already present

Spacing scale per UI-SPEC §Spacing (DPI-scale at use site via MulDiv(value, dpi, 96)):
  SP_XS  = 4   (icon-label gap)
  SP_SM  = 8   (cell left/right pad)
  SP_TAB = 30  (strip total height)
  SP_ACCENT_UNDERLINE = 2  (underline thickness)
  Minimum tab cell width = 80 px logical.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ActiveTab enum, TAB_NAMES/TAB_LABELS/TAB_ICONS constants, repurpose DesktopState.active_tab + add tab_strip/tab_panels/tab_rects/icon_font fields; add active_tab_enum unit test</name>
  <files>src/desktop/state.rs</files>
  <read_first>
    - src/desktop/state.rs (READ THE WHOLE FILE — locate DesktopState struct definition, DesktopState::new initializer, existing `active_tab: usize` field and any consumers; the existing consumers being deleted/rewritten are the toolbar hit-test code that Plan 02.1-05 removes)
    - src/desktop/icons.rs (Plan 02.1-01 — ICON_* constants are imported here by TAB_ICONS array)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md Pitfall 8 (Option B: repurpose existing field to enum; clean break since toolbar code is being deleted in 02.1-05)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md (DesktopState field-addition pattern, lines 93-115)
  </read_first>
  <behavior>
    - Test `active_tab_enum` (per VALIDATION.md): assert `ActiveTab::Details.as_str() == "details"`; assert `ActiveTab::Top.as_str() == "top"`; assert `ActiveTab::Errors.as_str() == "errors"`; assert `ActiveTab::from_str("details") == Some(ActiveTab::Details)`; assert `ActiveTab::from_str("errors") == Some(ActiveTab::Errors)`; assert `ActiveTab::from_str("unknown") == None`; assert `ActiveTab::from_str("") == None`; assert all 5 `TAB_NAMES` entries round-trip via `from_str(name).unwrap().as_str() == name`. Also assert `ActiveTab::from_index(0) == Some(Details)`, `from_index(4) == Some(Errors)`, `from_index(5) == None`.
  </behavior>
  <action>
    Three edits in src/desktop/state.rs.

    Edit 1 — Declare the enum, constants, and impl block. Place near the top of the file after the `use` statements (or below the existing `pub(super)` constants section if one exists). Per <interfaces>:
      - `#[derive(Copy, Clone, Debug, PartialEq, Eq)] pub(super) enum ActiveTab { Details = 0, Top = 1, Extensions = 2, Duplicates = 3, Errors = 4 }`
      - `pub(super) const TAB_NAMES: [&str; 5] = ["details", "top", "extensions", "duplicates", "errors"];`
      - `pub(super) const TAB_LABELS: [&str; 5] = ["Details", "Top", "Extensions", "Duplicates", "Errors"];`
      - `pub(super) const TAB_ICONS: [char; 5] = [super::icons::ICON_LIST_NESTED, super::icons::ICON_BAR_CHART, super::icons::ICON_FILES, super::icons::ICON_COPY, super::icons::ICON_EXCLAMATION_TRIANGLE];`
      - `impl ActiveTab` block with `COUNT: usize = 5`, `as_str(self) -> &'static str { TAB_NAMES[self as usize] }`, `from_str(s: &str) -> Option<Self>` (match each TAB_NAMES entry to its variant; default None), `from_index(i: usize) -> Option<Self>` (match 0..=4), `from_command_id(id: isize) -> Option<Self> { Self::from_index((id - super::ffi::ID_TAB_DETAILS) as usize) }`.

    Edit 2 — Repurpose `DesktopState.active_tab` field. Find the existing `pub(super) active_tab: usize,` line. Change the type to `pub(super) active_tab: ActiveTab,`. Add new fields adjacent:
      - `pub(super) tab_strip: Hwnd,`
      - `pub(super) tab_panels: [Hwnd; 5],`
      - `pub(super) tab_rects: [Rect; 5],` (the Rect type is already aliased in ffi.rs)
      - `pub(super) icon_font: Hfont,` (separate from the existing `font: Hfont`)
    In `DesktopState::new`, initialize: `active_tab: ActiveTab::Details, tab_strip: 0, tab_panels: [0; 5], tab_rects: [Rect{left:0,top:0,right:0,bottom:0}; 5], icon_font: 0`.

    Edit 3 — Search for any existing consumer of the OLD `active_tab: usize` field. The PATTERNS.md note + Pitfall 8 say the consumer is the toolbar hit-test code being deleted in Plan 02.1-05. If consumers exist in this codebase RIGHT NOW (this task runs before 02.1-05), the easiest path is to provide a temporary shim: rename the old field to `legacy_active_tab: usize` and keep it alongside the new enum field until 02.1-05 deletes the consumers. Alternatively, change consumers in-place to read `state.active_tab as usize`. PREFER the in-place change — `as usize` conversion is trivial and avoids a half-baked rename. If existing consumers break compilation that this task cannot fix without scope creep into mod.rs/paint.rs, document the failures in the task summary and let 02.1-05 finalize.

    Add a new `#[cfg(test)] mod tests` block (or extend existing) at the bottom of state.rs with the `active_tab_enum` test per <behavior>.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --lib desktop::state::tests::active_tab_enum &amp;&amp; cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - CI quad passes.
    - `cargo test --lib desktop::state::tests::active_tab_enum` PASSES.
    - Source assertion: `grep -c "enum ActiveTab" src/desktop/state.rs` reports 1.
    - Source assertion: `grep -c "TAB_NAMES: \\[&str; 5\\]" src/desktop/state.rs` reports 1.
    - Source assertion: `grep -c "TAB_ICONS: \\[char; 5\\]" src/desktop/state.rs` reports 1.
    - Source assertion: `grep -c "active_tab: ActiveTab" src/desktop/state.rs` reports 1 (the struct field).
    - Source assertion: `grep -c "tab_strip: Hwnd" src/desktop/state.rs` reports 1.
    - Source assertion: `grep -c "tab_panels: \\[Hwnd; 5\\]" src/desktop/state.rs` reports 1.
    - Source assertion: `grep -c "active_tab: usize" src/desktop/state.rs` reports 0 (old field gone OR renamed to `legacy_active_tab`).
  </acceptance_criteria>
  <done>ActiveTab enum + constants live in state.rs; DesktopState has the new tab fields; round-trip test passes; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/desktop/tabs.rs window class + draw_tab_strip in paint.rs + 2 unit tests; add tab-strip FFI constants</name>
  <files>src/desktop/tabs.rs, src/desktop/paint.rs, src/desktop/ffi.rs</files>
  <read_first>
    - src/desktop/tabs.rs (currently a 1-line stub per Phase 1 — replace entirely)
    - src/desktop/paint.rs lines 67–102 (`paint_window` — double-buffering template to mirror inside the tab WM_PAINT handler)
    - src/desktop/paint.rs lines 104–202 (`draw_toolbar_background` — the closest in-repo analog; copy the structure: tab bar bg fill → for each cell, fill if active + accent underline if active + draw label)
    - src/desktop/icons.rs (Plan 02.1-01 — ICON_* constants + draw_icon)
    - src/desktop/theme.rs (Plan 02.1-03 — palette_panel + palette_text + state.accent_color)
    - src/desktop/state.rs (after Task 1 — ActiveTab + TAB_LABELS + TAB_ICONS + DesktopState.tab_rects)
    - src/desktop/mod.rs lines 107–138 (`RegisterClassW` + `WndClassW` pattern — copy for the tab-strip class; WS_CHILD + ID_TAB_STRIP as menu/id when CreateWindowExW'd from 02.1-05)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md §Components & States (tab strip behavior + states)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md (tabs.rs section + "Tab Strip Hit-Test in WM_LBUTTONDOWN" code excerpt)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md (tab_cell_width + hit_test_tab_index tests)
  </read_first>
  <behavior>
    - Test `tab_cell_width(label, dpi, body_font)`: for `label = "Details"` at `dpi = 96`, with a body font handle 0 (test passes 0; the fn handles 0 by falling back to a measured-width estimate of `label.chars().count() as i32 * 7` so the test stays platform-independent), returns `max(80, 8 + 14 + 4 + measured_label_width + 8)`. For label `"X"` (tiny), returns the 80 px minimum. For label `"VeryLongTabLabel"`, returns the measured width. The test should pick label values where `chars().count() * 7 + 34` is definitively > 80 vs < 80 to assert both branches. Concrete cases: `tab_cell_width("X", 96, 0) == 80` (1 char → 7+34 = 41, clamped to 80); `tab_cell_width("VeryLongTabLabel", 96, 0) == 16*7+34 = 146` (definitively > 80).
    - Test `hit_test_tab_index(x, rects)`: build `let rects = [Rect{left:0,top:0,right:100,bottom:30}, Rect{left:100,top:0,right:200,bottom:30}, Rect{left:200,top:0,right:300,bottom:30}, Rect{left:300,top:0,right:400,bottom:30}, Rect{left:400,top:0,right:500,bottom:30}];` (slice to 3 for the simpler VALIDATION.md case; the production array is always 5). Assertions per VALIDATION.md: `hit_test_tab_index(120, &rects) == Some(1)`, `hit_test_tab_index(0, &rects) == Some(0)`, `hit_test_tab_index(99, &rects) == Some(0)`, `hit_test_tab_index(100, &rects) == Some(1)` (boundary — inclusive left), `hit_test_tab_index(500, &rects) == None` (right edge of last cell — exclusive right), `hit_test_tab_index(-1, &rects) == None`, `hit_test_tab_index(99999, &rects) == None`.
  </behavior>
  <action>
    Three edits.

    Edit 1 (src/desktop/ffi.rs) — Add tab constants and supporting FFI per <interfaces>. Place IDs in the existing IDs section (PATTERNS.md notes ID range 3xxx is vacant). Add `LoadCursorW`, `SetCursor`, `GetParent` to the User32 link block (only if missing — they may already exist). Add WM_SETCURSOR / WM_MOUSEMOVE / IDC_HAND constants. `MAKEWPARAM` helper — if not present, declare `pub(super) fn MAKEWPARAM(lo: u16, hi: u16) -> usize { ((hi as u32) << 16 | lo as u32) as usize }` (matches Win32 macro).

    Edit 2 (src/desktop/paint.rs) — Add `pub(super) unsafe fn draw_tab_strip(hdc: Hdc, rc: Rect, active: usize, accent: Dword, tab_rects: &[Rect; 5], state: &DesktopState)`. Use the existing `draw_toolbar_background` (lines 104–202) as the structural template. Body:
      - Fill the strip bg with `palette_panel(state)` (or `palette_bg(state)` — pick per UI-SPEC §Color "Secondary 30% — control fill" which says `palette_panel`; verify against the spec).
      - Draw 1 px hairline along the bottom edge of `rc` using `palette_line(state)` (structural seam per UI-SPEC §Spacing exceptions / §Color).
      - For each tab index 0..5:
        - Get `cell = tab_rects[i]` (already laid out by recompute_rects in tabs.rs WM_SIZE)
        - Use `palette_text(state)` as fg.
        - Compute `dpi = GetDpiForWindow(parent)` — pass via state or call here; existing paint code shows how (the `current_dpi` cache on state may already exist post Phase 02, else snapshot via `state.current_dpi` if added, else hardcode 96 with a TODO comment).
        - Compute `icon_size = MulDiv(14, dpi, 96)` and `pad_sm = MulDiv(8, dpi, 96)` and `pad_xs = MulDiv(4, dpi, 96)`.
        - Icon left-x = `cell.left + pad_sm`; icon top-y = `cell.top + (cell_height - icon_size) / 2`. Call `crate::desktop::icons::draw_icon(hdc, icon_x, icon_y, super::state::TAB_ICONS[i], icon_size, fg)`.
        - Label rect = Rect from `(cell.left + pad_sm + icon_size + pad_xs, cell.top)` to `(cell.right - pad_sm, cell.bottom)`. Select the body font (use `state.font`), set bg mode TRANSPARENT, set text color = fg, `DrawTextW(hdc, wide(TAB_LABELS[i]).as_ptr() as *mut u16, -1, &mut label_rect, DT_LEFT | DT_VCENTER | DT_SINGLELINE | DT_NOPREFIX)`.
        - If `i == active`: draw the 2 px accent underline. Rect from `(cell.left, cell.bottom - 2)` to `(cell.right, cell.bottom)`. Fill with `accent` color (the `accent` parameter, sourced from `state.accent_color` by the caller — single source of truth, no hardcoded magenta/blue).
      - No hover state, no rounded corners, no boxes/borders (Apple-HIG restraint per D-07).

    Edit 3 (src/desktop/tabs.rs) — Replace the 1-line stub with the full module.
      - Module-level `#![allow(non_snake_case)]` if needed for Win32 args.
      - `use super::ffi::*; use super::state::{self, DesktopState, ActiveTab, TAB_LABELS, with_state_mut}; use super::paint::draw_tab_strip; use crate::io::wide; use std::sync::OnceLock;`
      - `static CLASS_REGISTERED: OnceLock<bool> = OnceLock::new();`
      - `pub(super) const TAB_CLASS_NAME: &str = "FileTreeTabStrip";`
      - `pub(super) fn register_class(h_instance: Hinstance) -> Bool` — idempotent via OnceLock; sets up WndClassW with `style: CS_HREDRAW | CS_VREDRAW`, `lpfnWndProc: Some(tab_window_proc)`, `hInstance`, `hbrBackground: 0` (we paint everything), `lpszClassName: wide(TAB_CLASS_NAME).as_ptr()`; calls `RegisterClassW` and returns the result.
      - `pub(super) unsafe extern "system" fn tab_window_proc(hwnd, msg, wparam, lparam)` with arms:
        - `WM_PAINT` → double-buffered paint per `paint_window` template at paint.rs:67-102: BeginPaint → CreateCompatibleDC/Bitmap → SelectObject → call `with_state_mut(|state| draw_tab_strip(mem_dc, client_rc, state.active_tab as usize, state.accent_color, &state.tab_rects, state));` → BitBlt → EndPaint. If `with_state_mut` returns None (lock contention), fill_rect with `dark_brush()` as a safe fallback.
        - `WM_ERASEBKGND` → return 1 (we own paint).
        - `WM_SIZE` → call `recompute_rects(hwnd, total_width, dpi, body_font)` and store the resulting `[Rect; 5]` on `state.tab_rects` via `with_state_mut`. Return 0.
        - `WM_LBUTTONDOWN` → extract `let x = loword_signed(lparam);` per existing pattern (PATTERNS.md mod.rs:258-262). Read `state.tab_rects` via `with_state_mut` → call `hit_test_tab_index(x, &rects)`. If `Some(index)`: `PostMessageW(GetParent(hwnd), WM_COMMAND, MAKEWPARAM((ID_TAB_DETAILS + index as isize) as u16, 0), hwnd as Lparam);`. Return 0.
        - `WM_SETCURSOR` → call `SetCursor(LoadCursorW(0, IDC_HAND as usize))` and return 1. (Apple-HIG restraint: no visual repaint on hover; cursor change only — UI-SPEC §Components & States "Hover" row + Open Question 4 recommendation.)
        - `WM_MOUSEMOVE` → no-op, return 0.
        - default → DefWindowProcW.
      - `pub(super) fn tab_cell_width(label: &str, dpi: u32, body_font: Hfont) -> i32`: compute `let padded = MulDiv(8 + 14 + 4 + 8, dpi as i32, 96);` (=34 logical px chrome at 96 dpi) plus the measured label width. If `body_font == 0`, estimate label width as `label.chars().count() as i32 * 7` (test fallback). Otherwise call `GetTextExtentPoint32W` against the font — defer to a real Win32 call when `body_font != 0`. Return `max(MulDiv(80, dpi as i32, 96), padded + label_width)`. NOTE: at dpi=96, the 80 minimum is `MulDiv(80, 96, 96) = 80` so the unit test sees 80 verbatim.
      - `pub(super) fn hit_test_tab_index(x: i32, rects: &[Rect]) -> Option<usize>`: linear scan; return `Some(i)` for the first `i` where `x >= rects[i].left && x < rects[i].right`. None if no match. Pure fn, no Win32 dependency — testable.
      - `pub(super) fn recompute_rects(_hwnd: Hwnd, total_width: i32, dpi: u32, body_font: Hfont) -> [Rect; 5]`: For each label in `super::state::TAB_LABELS`, compute cell width via `tab_cell_width(label, dpi, body_font)`. Total height = `MulDiv(30, dpi as i32, 96)`. Lay out left-to-right starting at x=0; cells are sized to their natural width (no equal-width stretching — minimum 80 enforces uniformity). Return the 5-element Rect array.
      - `MulDiv` if not declared — add to ffi.rs Kernel32 block: `pub(super) fn MulDiv(nNumber: i32, nNumerator: i32, nDenominator: i32) -> i32;`.

      Add a `#[cfg(test)] mod tests` block at the bottom with the two tests per <behavior>. Both tests are pure-fn tests (no Win32 calls). They use `Rect { left, top, right, bottom }` literals — confirm Rect is constructible without unsafe (it's `#[repr(C)]` with public fields per existing pattern; if private, expose a `Rect::new` constructor in ffi.rs).
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --lib desktop::tabs:: &amp;&amp; cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - CI quad passes.
    - `cargo test --lib desktop::tabs::tests::tab_cell_width` PASSES.
    - `cargo test --lib desktop::tabs::tests::hit_test_tab_index` PASSES.
    - Source: `grep -c "extern \"system\" fn tab_window_proc" src/desktop/tabs.rs` reports 1.
    - Source: `grep -c "WM_LBUTTONDOWN" src/desktop/tabs.rs` reports at least 1.
    - Source: `grep -c "PostMessageW.*WM_COMMAND" src/desktop/tabs.rs` reports at least 1.
    - Source: `grep -c "IDC_HAND" src/desktop/tabs.rs` reports at least 1.
    - Source: `grep -c "fn draw_tab_strip" src/desktop/paint.rs` reports 1.
    - Source: `grep -c "icons::draw_icon" src/desktop/paint.rs` reports at least 1 (called from draw_tab_strip).
    - **Accent key_link enforcement (WARNING 3):** `awk '/fn draw_tab_strip/,/^}/' src/desktop/paint.rs | grep -c -E "accent_color|accent\b"` reports at least 1. The token `accent` (parameter) OR `state.accent_color` must appear within the body of `draw_tab_strip`. This is the regression guard for the must_haves key_link binding draw_tab_strip → DesktopState.accent_color: any future refactor that drops the accent param or hardcodes a colour fails this gate.
    - `cargo build --release` produces a binary that still launches; no tab strip is visible until Plan 02.1-05 wires the strip HWND into the layout — this task only builds the class/painter.
  </acceptance_criteria>
  <done>Tab strip window class registered, paint code lives in paint.rs, hit-test + cell-width pure fns tested, ActiveTab enum + state fields in place. Ready for 02.1-05 to instantiate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → tab class | WM_LBUTTONDOWN x-coord crosses into hit_test_tab_index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04-01 | DoS | tab_window_proc WM_PAINT with nested with_state_mut deadlock | mitigate | with_state_mut uses try_lock per state.rs reentrancy discipline; if locked, paint falls back to `dark_brush()` solid fill — no deadlock surface. |
| T-02.1-04-02 | T | Malformed x-coord from WM_LBUTTONDOWN | accept | hit_test_tab_index is pure; out-of-range returns None; the only consumer is PostMessageW with a known ID range. No injection surface. |
| n/a | Spoofing | n/a | not applicable | No identity surface |
| n/a | Repudiation | n/a | not applicable | No audit surface |
| n/a | Information Disclosure | n/a | not applicable | UI state only |
| n/a | Elevation of Privilege | n/a | not applicable | No privilege boundary crossed |
</threat_model>

<verification>
- `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo build --release && cargo test`.
- Three new tests green (`active_tab_enum`, `tab_cell_width`, `hit_test_tab_index`).
- Build succeeds; binary still launches (tab strip not yet visible — that lands in 02.1-05).
</verification>

<success_criteria>
- ActiveTab enum + TAB_NAMES + TAB_LABELS + TAB_ICONS constants are the single source of truth for tab identity.
- tabs.rs registers the FileTreeTabStrip window class and exposes a tab_window_proc capable of paint + hit-test + cursor-on-hover.
- draw_tab_strip renders 5 cells with icon + label + (active) accent underline reading from DesktopState.accent_color.
- All three tests are pure-fn (no Win32 dependency) and run in milliseconds.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-04-SUMMARY.md`. Record:
- Whether DesktopState.active_tab repurpose was clean or needed a temporary `legacy_active_tab` shim.
- Exact line numbers for `tab_window_proc`, `register_class`, `draw_tab_strip` so Plan 02.1-05 can navigate.
- Confirmed Bootstrap Icons codepoints actually used (in case Plan 02.1-01 verified divergence from defaults).
</output>
</content>
</invoke>