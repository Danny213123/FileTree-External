---
phase: 02.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.rs
autonomous: true
requirements:
  - SET-01
must_haves:
  truths:
    - "Settings struct has an active_tab: String field defaulting to 'details'"
    - "settings.json writes the active_tab field after dark_mode, before unknown round-trip"
    - "settings.json parser accepts and round-trips the active_tab field"
    - "Old settings.json files without the active_tab key load with active_tab == 'details'"
  artifacts:
    - path: "src/settings.rs"
      provides: "Settings.active_tab persistence following the 4-edit column-widths pattern from Plan 02-05"
      contains: "active_tab: String"
  key_links:
    - from: "src/settings.rs Default impl"
      to: "src/settings.rs Settings struct"
      via: "active_tab: \"details\".to_string() initializer"
      pattern: "active_tab: \"details\""
    - from: "src/settings.rs push_settings_json writer"
      to: "JSON output buffer"
      via: "output.push_str(\",\\\"active_tab\\\":\"); push_json_string(output, &settings.active_tab);"
      pattern: "\"active_tab\""
    - from: "src/settings.rs parse_top_object reader"
      to: "Settings.active_tab field"
      via: "\"active_tab\" match arm reading RawJsonValue::Str into the struct field"
      pattern: "\"active_tab\" =>"

threat_model:
  applicable: [V5]
  not_applicable: [V2, V3, V4, V6, V10, V14]
  threats:
    - id: T-02.1-02-01
      category: T
      component: settings.json active_tab string value
      disposition: mitigate
      mitigation: "Parser accepts any UTF-8 string but only 5 values are honored by the UI consumer (Plan 02.1-04 via ActiveTab::from_str). Unknown strings round-trip via the unknown-keys BTreeMap from Phase 02 D-02 (no data loss) and the UI falls back to default `Details` tab — consistent with backward-compat semantics. No control-char injection surface (DrawTextW handles control chars per OS rules; no HTML/JS context to inject into)."
---

<objective>
Add the `active_tab: String` field to the `Settings` struct in `src/settings.rs`, with default `"details"`, full JSON round-trip, and three new unit tests. This closes the settings layer for D-13 (active-tab persistence) and is a strict prerequisite for Plan 02.1-04 (which adds the `ActiveTab` enum on `DesktopState` and serializes via `as_str()`/`from_str()` at the settings boundary).

Purpose: This is a pure persistence-layer change. Zero UI wiring (that lives in 02.1-04 and 02.1-05). Following the locked column-widths pattern from Plan 02-05 keeps the code review trivial — same shape, four edits, three tests.

Output: `src/settings.rs` extended with one struct field, one Default initializer, one writer block, one reader match arm, three new test functions in the existing `#[cfg(test)] mod tests` block.

Per D-13 + UI-SPEC: only 5 string values are ever consumed by the UI (`details | top | extensions | duplicates | errors`), but the storage layer treats `active_tab` as an opaque `String` to stay forward-compatible with future tab additions without a settings schema bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-CONTEXT.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md
@.planning/phases/02-settings-polish/02-05-PLAN.md
@.planning/phases/02-settings-polish/02-CONTEXT.md
@src/settings.rs

<interfaces>
<!-- Existing Settings struct shape (extend, don't replace). Per PATTERNS.md the 4-edit
     idiom is already locked. Line numbers are approximate; executor confirms via Read. -->

From src/settings.rs (existing — extending the same struct):
- `pub(crate) struct Settings { schema_version: u32, last_path: String, dark_mode: bool, show_hidden: bool, show_files: bool, follow_symlinks: bool, columns: Vec<u32>, window: WindowGeometry, unknown: BTreeMap<String, RawJsonValue>, loaded_from_future: bool }` (lines ~18–32)
- `impl Default for Settings` with explicit per-field initializers (lines ~34–48)
- `pub(crate) fn push_settings_json(output: &mut String, settings: &Settings)` — append `,"<key>":<value>` blocks; see existing `dark_mode` block (lines ~183–201) for the exact `output.push_str(",\"<key>\":");` shape
- `pub(crate) fn parse_top_object(...)` — match arms on key strings; see existing `"last_path" => { if let RawJsonValue::Str(s) = v { settings.last_path = s; } }` arm (lines ~336–341)
- `crate::export::push_json_string(output: &mut String, value: &str)` — escapes + quotes a JSON string; reuse verbatim
- `enum RawJsonValue { Str(String), Num(f64), Bool(bool), Null, Array(Vec<RawJsonValue>), Object(BTreeMap<String, RawJsonValue>) }` (or similar; reader uses `if let RawJsonValue::Str(s) = ...`)
- Existing test pattern in `#[cfg(test)] mod tests` — `round_trip_all_known_fields` (lines ~802–834) and `unknown_keys_preserved_top_level` (lines ~836–848) are the analog templates

The "unknown round-trip" loop from Phase 02 D-02 lives at the END of `push_settings_json` (somewhere around line 230) — the new `active_tab` writer block MUST land BEFORE that loop so the field is written under its own key, not stuffed into the unknown bucket.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Settings.active_tab field with default 'details' and JSON round-trip + 3 unit tests</name>
  <files>src/settings.rs</files>
  <read_first>
    - src/settings.rs (READ THE WHOLE FILE — it is the source of truth for the 4-edit idiom; locate the exact line numbers for the struct, Default impl, writer block, reader match arm, unknown round-trip loop, and existing tests)
    - .planning/phases/02-settings-polish/02-05-PLAN.md (the column-widths 4-edit pattern this plan replicates)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md (Edit 1–4 examples for active_tab specifically)
    - .planning/phases/02-settings-polish/02-CONTEXT.md (D-02 unknown-keys round-trip — confirms backward-compat is automatic via the existing `unknown: BTreeMap` field)
  </read_first>
  <behavior>
    - Test 1 `active_tab_default`: `let s = Settings::default(); assert_eq!(s.active_tab, "details");` — confirms the default initializer.
    - Test 2 `active_tab_roundtrip`: Construct `Settings::default()`, set `active_tab = "errors".to_string()`, call `push_settings_json(&mut buf, &s)`, parse the result via the existing parser, assert `parsed.active_tab == "errors"` and that all other fields round-trip unchanged (use the same shape as `round_trip_all_known_fields`).
    - Test 3 `backward_compat_no_active_tab`: Build a minimal JSON literal string `{"schema_version":1,"dark_mode":true}` (NO `active_tab` key), feed it to the parser, assert the returned struct has `parsed.active_tab == "details"` AND `parsed.dark_mode == true` (old files load cleanly with the default).
    - Optional sanity check inside Test 2: assert the serialized JSON literally contains the substring `"active_tab":"errors"` so the writer block firing is confirmed.
  </behavior>
  <action>
    Apply the four edits in src/settings.rs verbatim per the column-widths pattern, then add three unit tests.

    Edit 1 — Struct field: Add `pub(crate) active_tab: String,` to the `Settings` struct. Place it AFTER `window: WindowGeometry,` and BEFORE `unknown: BTreeMap<String, RawJsonValue>,` (matches PATTERNS.md placement note line 39).

    Edit 2 — Default impl: Add `active_tab: "details".to_string(),` to the `impl Default for Settings::default()` initializer. Place adjacent to other field initializers in the same order as the struct.

    Edit 3 — Writer block in `push_settings_json`: Locate the existing `output.push_str(",\"dark_mode\":");` block (or the `columns` writer if that landed in 02-05 and is the final pre-unknown block). Add the new block IMMEDIATELY BEFORE the unknown round-trip loop:
      - `output.push_str(",\"active_tab\":");`
      - `crate::export::push_json_string(output, &settings.active_tab);`
    Match the EXACT punctuation/spacing of the surrounding writer blocks (no spaces inside the quotes; comma before the key; colon between key and value; reuse the existing `push_json_string` helper for proper JSON escaping).

    Edit 4 — Reader match arm in `parse_top_object`: Add a new arm AFTER the existing `"last_path" =>` arm and following the same shape:
      ```
      "active_tab" => {
          let v = parse_value(bytes, pos, 1)?;
          if let RawJsonValue::Str(s) = v {
              settings.active_tab = s;
          }
      }
      ```
      The exact helper name (`parse_value` here is illustrative — match whatever the existing `"last_path"` arm calls) and `RawJsonValue::Str` variant name (or `::String`, whichever the existing code uses) MUST match the surrounding arms verbatim. Unknown variants fall through to the existing `_ => { insert into unknown }` arm — do NOT add special handling.

    Edit 5 — Three unit tests inside the existing `#[cfg(test)] mod tests` block in src/settings.rs (DO NOT create a new test file). Use the existing test helpers (the `round_trip_all_known_fields` test at line ~802 shows the parse/serialize helpers in use). Place the three new test fns adjacent to each other under a `// Phase 02.1 — active_tab persistence` section comment.

    Critical: the 4 production edits must keep the existing 12+ settings parser tests green (no behavior change for any other field). If a test fails after Edit 1–4, the fix is to add `active_tab: "details".to_string(),` to that test's struct literal — same one-line addition the column-widths landing required.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --lib settings::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0.
    - `cargo clippy --all-targets -- -D warnings` exits 0.
    - `cargo test --lib settings::` exits 0 and reports the three new tests (`active_tab_default`, `active_tab_roundtrip`, `backward_compat_no_active_tab`) plus all pre-existing settings tests as PASSED.
    - `cargo test --lib settings::tests::active_tab_default` PASSES.
    - `cargo test --lib settings::tests::active_tab_roundtrip` PASSES.
    - `cargo test --lib settings::tests::backward_compat_no_active_tab` PASSES.
    - Source assertion: `grep -c "pub(crate) active_tab: String" src/settings.rs` reports 1.
    - Source assertion: `grep -c "active_tab: \"details\".to_string()" src/settings.rs` reports 1 (the Default impl).
    - Source assertion: `grep -c "\"active_tab\"" src/settings.rs` reports at least 3 (writer string literal, reader match arm, and at least one test assertion — there can be more).
    - Source assertion: `grep -v '^//' src/settings.rs | grep -c '"active_tab":"'` reports at least 1 (the writer string literal — comment-stripped to avoid counting prose).
  </acceptance_criteria>
  <done>active_tab persists in settings.json with default "details"; old files load cleanly; full settings test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → process | settings.json read at launch (existing boundary; this plan adds one string field) |
| process → filesystem | settings.json write on tab switch / change (existing boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-02-01 | Tampering | settings.json active_tab string value (loaded from %APPDATA% which has per-user ACLs) | mitigate | Parser accepts any UTF-8 string; UI consumer (Plan 02.1-04 ActiveTab::from_str) maps unknown values to the default `Details` tab. Unknown-keys round-trip from Phase 02 D-02 preserves forward-compat. No control-char injection surface (DrawTextW handles control chars per OS rules; no HTML/JS context exists). |
| n/a | Spoofing | n/a | not applicable | No identity surface |
| n/a | Repudiation | n/a | not applicable | No audit surface |
| n/a | Information Disclosure | n/a | not applicable | active_tab string is "details"/"top"/etc — no sensitive data |
| n/a | DoS | n/a | not applicable | Field is bounded by JSON parser's existing limits |
| n/a | Elevation of Privilege | n/a | not applicable | No privilege boundary crossed |
</threat_model>

<verification>
- CI quad: `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo build --release && cargo test`.
- The three new tests pass alongside the existing 12+ settings parser tests.
- Manual sanity: write a settings.json with `{"schema_version":1,"active_tab":"errors"}` to `%APPDATA%\FileTree\settings.json`, launch the binary, exit, re-read the file — the file still contains `"active_tab":"errors"` (the value was loaded, not lost). UI behavior of restoring the tab on launch is verified by 02.1-05.
</verification>

<success_criteria>
- `Settings::default().active_tab == "details"`.
- Round-trip: any value of `active_tab` survives serialize → parse intact.
- A settings.json from before this phase (no `active_tab` key) loads with `active_tab == "details"` and all other fields intact.
- All pre-existing settings tests continue to pass (zero regression).
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record the line numbers of the four edits + the three new test fns so the 02.1-04 plan (which consumes this field via `ActiveTab::as_str`) can navigate directly.
</output>
