---
phase: 02.1
plan: 08
type: execute
wave: 6
depends_on:
  - 02.1-05
  - 02.1-06
  - 02.1-07
files_modified:
  - .planning/ROADMAP.md
  - images/Screenshot-2026-05-24-light-after.png
  - images/Screenshot-2026-05-24-dark-after.png
autonomous: false
requirements: []
must_haves:
  truths:
    - "ROADMAP.md Phase 02.1 entry has a real goal sentence (not '[Urgent work - to be planned]')"
    - "Two after-state screenshots committed showing light + dark chrome polished"
    - "Visual diff confirms: no toolbar checkboxes; menu bar with View; 5-tab strip with icons; status footer 22 px without sizing grip; dark palette is neutral #1e1e1e (not brown-tinted); accent underline under active tab"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 02.1 goal text finalized"
      contains: "Phase 02.1"
    - path: "images/Screenshot-2026-05-24-light-after.png"
      provides: "Post-phase light-mode screenshot for visual diff against pre-phase Screenshot 2026-05-23 190727.png"
    - path: "images/Screenshot-2026-05-24-dark-after.png"
      provides: "Post-phase dark-mode screenshot for visual diff against pre-phase Screenshot 2026-05-23 190754.png"
  key_links:
    - from: ".planning/ROADMAP.md Phase 02.1"
      to: "all 8 plans + their requirement IDs"
      via: "Plans list + status line update"
      pattern: "Phase 02.1"

threat_model:
  applicable: []
  not_applicable: [V2, V3, V4, V5, V6, V10, V14]
  threats:
    - id: T-02.1-08-01
      category: I
      component: Committed screenshots may contain user-private data (file names, paths, sizes)
      disposition: mitigate
      mitigation: "Capture screenshots while scanning a deliberately-public test directory (e.g., a fresh git clone of the FileTree repo itself, or C:\\Windows\\System32 which is the same on every Windows install). Manual review before commit to confirm no email addresses, document names, or personal paths are visible. The developer is the sole sign-off — they are also the user, so consent is implicit."
---

<objective>
Close out Phase 02.1 by:

1. **Finalizing ROADMAP.md** — Replace the `[Urgent work - to be planned]` placeholder Phase 02.1 goal with a one-line summary derived from CONTEXT.md domain ("Dark mode that looks dark, modern chrome with content tabs replacing toolbar checkboxes, settings persistence for drive picker / active tab / view toggles, mnemonic accessibility").

2. **Capturing post-state screenshots** — Two PNGs into `images/` mirroring the pre-state pair:
   - Pre-state light: `images/Screenshot 2026-05-23 190727.png` (Phase 02 baseline)
   - Pre-state dark:  `images/Screenshot 2026-05-23 190754.png` (Phase 02 baseline)
   - Post-state light: `images/Screenshot-2026-05-24-light-after.png` (this plan)
   - Post-state dark:  `images/Screenshot-2026-05-24-dark-after.png` (this plan)

3. **Visual diff sign-off** — The developer (the human) reviews the diff against UI-SPEC + VALIDATION.md's manual test plan and confirms every D-XX decision from CONTEXT.md is visibly closed. This is a `checkpoint:human-verify` step.

Purpose: This is the close-out plan. Phase 02.1 has no UAT artifact target (the orchestrator may run /gsd-uat-phase separately) — this plan just freezes the visual evidence + finalizes ROADMAP. If the visual diff reveals a regression, the developer documents it and triggers /gsd-plan-phase --gaps for any unresolved item.

Output:
- `.planning/ROADMAP.md` updated: Phase 02.1 goal sentence replaces the placeholder; Plans list section reflects all 8 plans; status updated to "ready-to-execute" → "completed" if all prior plans executed clean.
- Two new files in `images/` capturing the post-state.

Plan is `autonomous: false` because Task 2 requires the developer to capture the screenshots manually (Windows Snipping Tool or PrtScn → save), and Task 3 is a `checkpoint:human-verify`. Wave 5 because it depends on Plans 02.1-05/06 (Wave 2/3) AND 02.1-07 (Wave 4) — this is the final close-out gate after the first-paint flash fix lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-CONTEXT.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-01-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-02-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-03-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-04-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-05-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-06-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .planning/ROADMAP.md — finalize Phase 02.1 goal sentence + Plans list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (READ THE WHOLE FILE — locate the Phase 02.1 entry, currently with `[Urgent work - to be planned]` in the Goal line; also locate the `Plans:` list under Phase 02.1 — currently empty or stub)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-CONTEXT.md (the domain summary informing the goal sentence)
  </read_first>
  <action>
    Two edits in .planning/ROADMAP.md.

    Edit 1 — Replace the Phase 02.1 Goal line. Find the line `**Goal:** [Urgent work - to be planned]` (or similar placeholder) under the `### Phase 02.1` heading. Replace with:

      `**Goal:** Dark mode that actually looks dark (#1e1e1e neutral), modern chrome with a View menu and 5 content tabs replacing the toolbar checkbox row, settings persistence for active tab and drive picker, and Windows-native mnemonic accessibility.`

    The goal sentence is outcome-shaped (per goal-backward principle) and covers the 4 phase requirement IDs (SET-01, POL-01, POL-02, POL-03) plus the 13 decisions (D-01..D-13) from CONTEXT.md.

    Edit 2 — Update the Plans list under Phase 02.1. The orchestrator's update_roadmap step from `/gsd-plan-phase` writes a list of plan files; this plan ensures that list is complete and correct post-execution. Format:
      ```
      **Plans:** 8 plans

      Plans:
      - [x] 02.1-01-PLAN.md — Bootstrap Icons font asset + register + draw_icon helper
      - [x] 02.1-02-PLAN.md — settings.active_tab field with JSON round-trip
      - [x] 02.1-03-PLAN.md — Theme primitives (palette rewrite, accent caching, uxtheme ordinal resolver, WCAG luminance)
      - [x] 02.1-04-PLAN.md — ActiveTab enum + tab-strip window class + draw_tab_strip
      - [x] 02.1-05-PLAN.md — Rewire mod.rs: delete toolbar, add View menu, route tab commands, restore drive picker + active tab, Ctrl+1..5 accelerators
      - [x] 02.1-06-PLAN.md — Owner-draw menu (MF_OWNERDRAW + MIM_BACKGROUND + UISF_HIDEACCEL) + custom FileTreeStatusFooter window class replacing msctls_statusbar32
      - [x] 02.1-07-PLAN.md — First-paint flash fix: pre-window SetPreferredAppMode bootstrap + per-HWND DWM dark application before ShowWindow
      - [x] 02.1-08-PLAN.md — ROADMAP goal text update + after-state screenshots + visual diff sign-off
      ```

    Use `[x]` for completed plans (this plan runs LAST in Wave 5 — by definition all earlier plans completed). If this plan is being written but earlier plans have not yet completed, use `[ ]` for incomplete plans and `[x]` only for the ones that have run.

    Mark Phase 02.1 status as `completed` if a status line exists (e.g., `**Status:** ready-to-execute` → `**Status:** completed`). If no status line exists, leave it alone — ROADMAP convention is unclear.
  </action>
  <verify>
    <automated>grep -c "Dark mode that actually looks dark" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[Urgent work - to be planned\\]" .planning/ROADMAP.md` reports 0 (placeholder gone).
    - `grep -c "Dark mode that actually looks dark" .planning/ROADMAP.md` reports 1 (new goal line).
    - `grep -c "02.1-01-PLAN.md" .planning/ROADMAP.md` reports 1.
    - `grep -c "02.1-08-PLAN.md" .planning/ROADMAP.md` reports 1.
    - `grep -c "02.1-0\\|02.1-1" .planning/ROADMAP.md` reports at least 8 (all 8 plans listed).
  </acceptance_criteria>
  <done>ROADMAP Phase 02.1 entry finalized.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Developer-supplied — capture post-state light + dark screenshots</name>
  <files>images/Screenshot-2026-05-24-light-after.png, images/Screenshot-2026-05-24-dark-after.png</files>
  <read_first>
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md (reference for what every visual element should look like)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md (manual test plan that informs what the screenshot should capture)
  </read_first>
  <what-built>
    Plans 02.1-01 through 02.1-07 have shipped: dark palette rewrite, tab strip, owner-draw menu, status footer, first-paint fix. The visual evidence needs to be frozen in the repo so future regression checks can diff against today's state.
  </what-built>
  <how-to-verify>
    1. Build the release binary: `cargo build --release`.
    2. Run a scan on a deliberately-public directory (e.g., the FileTree repo itself: `target\\release\\filetree.exe` then point the path bar at `C:\\Users\\dannguan\\FileTree` or `C:\\Windows\\System32` — confirm no personal data is visible in the result list).
    3. Wait for the scan to complete so all 5 tabs have content.
    4. With View → Dark Mode OFF, hover a menu item to show the accent state, take a full-window screenshot via the built-in Snipping Tool / Win+Shift+S → save as PNG. Save to `images\\Screenshot-2026-05-24-light-after.png`.
    5. Toggle View → Dark Mode ON, repeat the capture, save to `images\\Screenshot-2026-05-24-dark-after.png`.
    6. Open each PNG and visually compare against the pre-state baselines:
        - `images\\Screenshot 2026-05-23 190727.png` (Phase 02 light baseline)
        - `images\\Screenshot 2026-05-23 190754.png` (Phase 02 dark baseline)
       Confirm the post-state shows: no toolbar checkbox row; menu bar with `View` entry; 5-tab strip (Details / Top / Extensions / Duplicates / Errors) with Bootstrap Icons glyphs and accent underline under the active tab; status footer 22 px tall without the Windows 95 sizing grip; dark palette is the neutral `#1e1e1e` (not the previous brown-tinted `#0c0d0f`).
    7. Confirm no personal data is visible in either screenshot. If anything sensitive sneaked in, re-capture against a cleaner directory.
  </how-to-verify>
  <acceptance_criteria>
    - `Test-Path images/Screenshot-2026-05-24-light-after.png` returns True.
    - `Test-Path images/Screenshot-2026-05-24-dark-after.png` returns True.
    - Both PNG files are valid PNGs: `Get-Content -Encoding Byte -TotalCount 8 images\\Screenshot-2026-05-24-light-after.png` returns `137 80 78 71 13 10 26 10` (the PNG magic header); same check for the dark capture.
    - **Image dimension check (replaces the pre-revision file-size band — INFO 1 from checker):** Both captures resolve to image dimensions of at least 800×600 (i.e., a real window screenshot, not a clipped icon or empty rect). Verify with:
      ```powershell
      Add-Type -AssemblyName System.Drawing
      foreach ($p in 'images\\Screenshot-2026-05-24-light-after.png','images\\Screenshot-2026-05-24-dark-after.png') {
          $img = [System.Drawing.Image]::FromFile((Resolve-Path $p))
          if ($img.Width -lt 800 -or $img.Height -lt 600) {
              throw "Image $p is too small: $($img.Width)x$($img.Height) (need >= 800x600)"
          }
          Write-Host "$p OK: $($img.Width)x$($img.Height)"
          $img.Dispose()
      }
      ```
      Both files must pass (width >= 800 AND height >= 600). The full-window capture of a polished FileTree binary is at least ~1000×700 in practice; the 800×600 floor catches clipped/empty captures while staying tolerant of small windows.
    - Visual confirmation per how-to-verify step 6.
  </acceptance_criteria>
  <resume-signal>Type "captured" once both PNGs are committed to `images/` and visually verified, or "blocked: &lt;reason&gt;" if the binary fails to launch or a visual regression is found.</resume-signal>
  <done>After-state visual evidence committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual diff sign-off — confirm every D-XX decision from CONTEXT.md is visibly closed</name>
  <files>(no file changes; verification only)</files>
  <what-built>
    All 8 plans of Phase 02.1 have shipped. The 4 phase requirement IDs (SET-01, POL-01, POL-02, POL-03) and 13 decisions (D-01..D-13) should each be visually + behaviorally confirmable from the built binary + the freshly-captured screenshots.
  </what-built>
  <how-to-verify>
    Open the 4 screenshots side by side (pre-light, post-light, pre-dark, post-dark). For each decision in CONTEXT.md, confirm visually:
    - **D-01 Bootstrap Icons font:** The 5 tab glyphs are present (list-nested / bar-chart / files / copy / exclamation-triangle).
    - **D-02 No custom-rendered glyphs:** Glyphs render via the font (consistent stroke width across DPI).
    - **D-03 Owner-draw menu only when system theme breaks:** In dark mode, no white menu gutter visible.
    - **D-04 First-paint flash eliminated:** Manual launch in dark mode shows zero flash (also covered by Plan 02.1-07 acceptance).
    - **D-05 Palette is neutral #1e1e1e:** Dark bg color in screenshot matches `#1e1e1e` (compare with a color picker if uncertain).
    - **D-06 Toolbar checkboxes removed:** No checkbox row visible in either screenshot.
    - **D-07 Apple-HIG borders/spacing:** No 3D bevels, no chunky borders, single 1 px hairlines only.
    - **D-08 View menu + 5 content tabs:** Both visible and labeled per UI-SPEC.
    - **D-09 System accent honored:** Accent underline color under active tab matches `DwmGetColorizationColor` output (change Windows accent → verify the underline updates without re-launch).
    - **D-10 System theme honored on first launch:** With settings.json missing or `dark_mode: false`, light theme opens cleanly.
    - **D-11 Drive picker restore:** Launch, scan D:, close, re-launch → D: still pre-selected.
    - **D-12 Ctrl+1..5 accelerators:** Press each combo → corresponding tab activates.
    - **D-13 Active tab restore:** Launch, switch to Errors tab, close, re-launch → Errors tab still active.

    Plus the 4 requirement IDs:
    - **SET-01:** settings.active_tab persists across launches (covered by D-13).
    - **POL-01:** drive picker restores (covered by D-11).
    - **POL-02:** Ctrl+1..5 accelerators (covered by D-12).
    - **POL-03:** Mnemonic underlines hidden until ALT (press ALT → underlines appear; release → disappear) + zero first-paint flash on dark launch.

    If ANY item is not closed → mark as a gap, type "blocked: D-XX visual regression / POL-XX behavior missing", and trigger `/gsd-plan-phase 02.1 --gaps` after this checkpoint to plan the closure.

    If ALL items closed → sign off and the phase is complete.
  </how-to-verify>
  <acceptance_criteria>
    - All 13 D-XX items visually + behaviorally confirmed.
    - All 4 requirement IDs (SET-01, POL-01, POL-02, POL-03) behaviorally confirmed.
    - Developer types the sign-off signal.
  </acceptance_criteria>
  <resume-signal>Type "signed-off" if all D-XX + requirement IDs closed, or "gap: &lt;list&gt;" if any item remains open.</resume-signal>
  <done>Phase 02.1 visually + behaviorally signed off as complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer screen → repository | Screenshots may inadvertently capture private data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-08-01 | Information Disclosure | Committed screenshots reveal user-private data | mitigate | Task 2 how-to-verify step 7 explicitly requires reviewing each PNG before commit and re-capturing if anything sensitive is visible. Test directory recommendations (FileTree repo, C:\\Windows\\System32) are deliberately public. |
| n/a | Spoofing | n/a | not applicable | No identity surface |
| n/a | Repudiation | n/a | not applicable | No audit surface |
| n/a | Tampering | n/a | not applicable | Documentation + screenshot updates only |
| n/a | DoS | n/a | not applicable | No runtime impact |
| n/a | Elevation of Privilege | n/a | not applicable | No privilege boundary crossed |
</threat_model>

<verification>
- ROADMAP.md placeholder gone, real goal line in place, all 8 plans listed.
- Both screenshots committed; visual diff confirms phase contracts.
- Manual sign-off recorded.
</verification>

<success_criteria>
- ROADMAP Phase 02.1 goal text finalized.
- After-state screenshots committed.
- Image-dimension check (width >= 800 AND height >= 600 via PowerShell `System.Drawing`) passes for both PNGs.
- All 13 decisions from CONTEXT.md visually/behaviorally closed.
- All 4 phase requirement IDs (SET-01, POL-01, POL-02, POL-03) confirmed in behavior.
- Developer sign-off recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-08-SUMMARY.md`. Record:
- The final ROADMAP goal sentence text.
- The two screenshot filenames + their dimensions (from the PowerShell image-dimension check).
- Sign-off result: "all closed" or "gap: D-XX / POL-XX".
- If any gap was identified, the next-step recommendation (`/gsd-plan-phase 02.1 --gaps`).
</output>
