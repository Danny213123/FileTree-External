---
phase: 02.1
plan: 05
type: execute
wave: 2
depends_on:
  - 02.1-02
  - 02.1-03
  - 02.1-04
files_modified:
  - src/desktop/mod.rs
  - src/desktop/state.rs
  - src/desktop/ffi.rs
autonomous: true
requirements:
  - SET-01
  - POL-01
  - POL-02
must_haves:
  truths:
    - "Toolbar checkbox row (Hidden / Files / Links / Dark) is deleted from create_controls and resize_controls"
    - "A menu bar with a View submenu (&Show Hidden, Show &Files, &Dark Mode) is attached to the main window via SetMenu"
    - "WM_INITMENUPOPUP arm refreshes the 3 View-menu check states from settings via CheckMenuItem, filtered by the cached state.view_menu handle (Pitfall 7)"
    - "WM_DWMCOLORIZATIONCOLORCHANGED arm calls theme::refresh_accent and InvalidateRect(NULL) to repaint the accent underline"
    - "5 ID_TAB_DETAILS..ID_TAB_ERRORS WM_COMMAND arms mutate state.active_tab, show/hide the 5 tab_panels HWNDs, snapshot+save settings"
    - "Ctrl+1..Ctrl+5 accelerators map to ID_TAB_DETAILS..ID_TAB_ERRORS in the LoadAcceleratorsW resource table or runtime CreateAcceleratorTableW"
    - "Tab-strip HWND + 5 content-panel HWNDs are created in create_controls and laid out in resize_controls (strip = 30 px tall under the path bar; panels fill the remaining client area minus status footer)"
    - "Drive-picker restore reads state.settings.last_path (not just state.initial_path) and pre-selects the matching drive letter via the existing populate_drive_picker path"
    - "drive_letter_from_path is an extracted pure fn that handles C:\\..., \\\\?\\D:\\..., \\\\server\\share\\..., empty, and non-Windows paths (returns Option<char>; fallback 'C' applied at the call site)"
    - "Active tab is restored on launch by PostMessageW(WM_COMMAND, MAKEWPARAM(ID_TAB_<settings.active_tab>, 0), 0) AFTER ShowWindow + UpdateWindow"
    - "activate_tab uses with_state_mut (try_lock) for the state mutation and save_settings_if_dirty(snap) is invoked OUTSIDE the closure (PATTERNS.md non-negotiable: save NEVER runs while the state lock is held)"
  artifacts:
    - path: "src/desktop/mod.rs"
      provides: "Rewired window proc: View menu construction, WM_INITMENUPOPUP, WM_DWMCOLORIZATIONCOLORCHANGED, ID_TAB_* command arms, accelerator table extension, tab-strip + panel layout, drive-picker restore, post-show active-tab restore"
      contains: "WM_INITMENUPOPUP, WM_DWMCOLORIZATIONCOLORCHANGED, ID_TAB_DETAILS, ID_VIEW_DARK_MODE, drive_letter_from_path"
    - path: "src/desktop/state.rs"
      provides: "DesktopState.view_menu: Hmenu field + drive_letter_from_path pure fn + 1 unit test"
      contains: "view_menu: Hmenu, fn drive_letter_from_path"
    - path: "src/desktop/ffi.rs"
      provides: "New menu IDs (ID_VIEW_SHOW_HIDDEN=3010, ID_VIEW_SHOW_FILES=3011, ID_VIEW_DARK_MODE=3012) + Hmenu alias if missing"
      contains: "ID_VIEW_DARK_MODE, ID_VIEW_SHOW_HIDDEN, ID_VIEW_SHOW_FILES"
  key_links:
    - from: "src/desktop/mod.rs window_proc WM_COMMAND ID_TAB_* arms"
      to: "state.settings.active_tab + state.active_tab"
      via: "with_state_mut → activate_tab → snapshot_for_save → save_settings_if_dirty (save OUTSIDE the closure; Phase 02 idiom)"
      pattern: "ID_TAB_DETAILS"
    - from: "src/desktop/mod.rs WM_INITMENUPOPUP arm"
      to: "View-menu check states"
      via: "if hmenu == state.view_menu { CheckMenuItem(..., MF_BYCOMMAND|MF_CHECKED|MF_UNCHECKED) }"
      pattern: "WM_INITMENUPOPUP"
    - from: "src/desktop/mod.rs WM_DWMCOLORIZATIONCOLORCHANGED arm"
      to: "DesktopState.accent_color"
      via: "theme::refresh_accent(state) + InvalidateRect(state.hwnd, null(), 1)"
      pattern: "WM_DWMCOLORIZATIONCOLORCHANGED"
    - from: "src/desktop/mod.rs post-ShowWindow block"
      to: "active-tab restore"
      via: "PostMessageW(hwnd, WM_COMMAND, MAKEWPARAM(ID_TAB_DETAILS + ActiveTab::from_str(&state.settings.active_tab).unwrap_or(Details) as u16, 0), 0)"
      pattern: "PostMessageW.*WM_COMMAND.*ID_TAB"
    - from: "src/desktop/mod.rs populate_drive_picker call"
      to: "settings.last_path"
      via: "drive_letter_from_path(&state.settings.last_path).unwrap_or('C')"
      pattern: "drive_letter_from_path"

threat_model:
  applicable: [V5, V14]
  not_applicable: [V2, V3, V4, V6, V10]
  threats:
    - id: T-02.1-05-01
      category: T
      component: settings.active_tab loaded from disk → routed via PostMessageW WM_COMMAND
      disposition: mitigate
      mitigation: "ActiveTab::from_str returns None for unknown strings (test active_tab_enum from Plan 02.1-04). Restore path uses unwrap_or(ActiveTab::Details) — unknown values silently fall back to the default tab. No way for a malformed settings.json to navigate to a non-existent tab or run arbitrary code."
    - id: T-02.1-05-02
      category: D
      component: WM_INITMENUPOPUP arm without Pitfall 7 hmenu filter
      disposition: mitigate
      mitigation: "Per Pitfall 7, WM_INITMENUPOPUP fires for EVERY popup including Shell context menus from the right-click handler in shell.rs. Without the `hmenu == state.view_menu` guard, CheckMenuItem would corrupt arbitrary menu state. The guard is enforced verbatim in the action and verified by source assertion (grep)."
    - id: T-02.1-05-03
      category: T
      component: settings.last_path drive-letter parse
      disposition: mitigate
      mitigation: "drive_letter_from_path is a pure fn; unit-tested for all 5 path shapes (C:\\..., \\\\?\\D:\\..., \\\\server\\share\\..., empty, non-Windows). Returns Option<char>; call site uses unwrap_or('C') so any unparseable path falls back to C: without crash."
    - id: T-02.1-05-04
      category: D
      component: activate_tab reentrancy / save-while-locked deadlock
      disposition: mitigate
      mitigation: "activate_tab is invoked inside with_state_mut (try_lock; non-blocking — drops on contention per PATTERNS.md Reentrancy-safe state access). The save_settings_if_dirty(snap) call is positioned at function-body indentation AFTER the closure returns, never inside it. Verified by grep + awk indentation check in acceptance criteria. This prevents the deadlock pattern where save_settings would attempt to re-enter the state lock while the closure still holds it."
---

<objective>
Rewire `src/desktop/mod.rs` to:
1. **Delete** the 4 toolbar checkboxes (Hidden / Files / Links / Dark) per D-06 (toolbar removal) — `create_child` calls in `create_controls`, `BM_SETCHECK` initializers, and `resize_controls` layout.
2. **Add a menu bar** with a View submenu carrying the 3 toggles (Show Hidden, Show Files, Dark Mode) per D-08 + UI-SPEC §Components.
3. **Wire 5 content tabs** (Details / Top / Extensions / Duplicates / Errors) via the tab-strip class from Plan 02.1-04 — create the strip HWND + 5 panel HWNDs, route ID_TAB_* WM_COMMAND, show/hide on switch, save + restore via settings.active_tab (D-13).
4. **Hook system accent** via `WM_DWMCOLORIZATIONCOLORCHANGED` (D-09 / RESEARCH Pattern 7).
5. **Refresh menu checks** via `WM_INITMENUPOPUP` filtered by cached `state.view_menu` handle (Pitfall 7).
6. **Restore drive picker** to the last scanned drive on launch (D-11 / POL-01) — extract `drive_letter_from_path` as a pure unit-tested fn.
7. **Restore active tab** on launch (D-13 / SET-01) — PostMessageW after ShowWindow.
8. **Add Ctrl+1..Ctrl+5 accelerators** (POL-02 / D-12) mapping to ID_TAB_DETAILS..ID_TAB_ERRORS.

Purpose: This is the largest plan in Phase 02.1 and the integration point that makes the new tab strip, menu, palette, and accent caching come together as the actual UI a user sees. All 3 phase requirement IDs flow through this plan: SET-01 (active_tab persistence wiring), POL-01 (drive picker restore), POL-02 (Ctrl+1..5 tab accelerators).

Output:
- `src/desktop/mod.rs` heavily edited: 4 toolbar `create_child` calls removed; View menu built in `create_controls`; `WM_INITMENUPOPUP` + `WM_DWMCOLORIZATIONCOLORCHANGED` + 5×`ID_TAB_*` + 3×`ID_VIEW_*` arms added to `window_proc`; LoadAcceleratorsW table extended with Ctrl+1..5; tab-strip + 5 panel HWNDs created and laid out; drive-picker call site updated to use `settings.last_path`; post-`ShowWindow` block extended with active-tab restore.
- `src/desktop/state.rs`: `view_menu: Hmenu` field + `drive_letter_from_path` pure fn + 1 unit test (`drive_letter_from_path_cases`).
- `src/desktop/ffi.rs`: 3 new menu-item ID constants (ID_VIEW_SHOW_HIDDEN, ID_VIEW_SHOW_FILES, ID_VIEW_DARK_MODE).

Critical preservation: the existing `apply_theme` per-control SetWindowTheme loop stays — extended to cover the new tab_strip + tab_panels HWNDs. The Phase-2 D-02 unknown-keys round-trip is unaffected (no settings schema change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-CONTEXT.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-02-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-03-PLAN.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-04-PLAN.md
@src/desktop/mod.rs
@src/desktop/state.rs
@src/desktop/ffi.rs
@src/desktop/shell.rs

<interfaces>
<!-- Inputs from upstream plans -->

From src/settings.rs (Plan 02.1-02 lands `active_tab: String` default "details").
From src/desktop/state.rs (Plan 02.1-04 lands ActiveTab enum + TAB_NAMES + TAB_LABELS + TAB_ICONS + DesktopState.{active_tab: ActiveTab, tab_strip: Hwnd, tab_panels: [Hwnd;5], tab_rects: [Rect;5], icon_font: Hfont}; this plan ADDS view_menu: Hmenu).
From src/desktop/theme.rs (Plan 02.1-03 lands refresh_accent(state) + apply_dark_mode_to_window(hwnd, enabled) + uxtheme_ordinals() + state.accent_color/selected_text_color).
From src/desktop/ffi.rs (Plan 02.1-03 lands WM_INITMENUPOPUP/WM_DWMCOLORIZATIONCOLORCHANGED/MF_*/MIM_* constants and CreateMenu/CreatePopupMenu/SetMenu/CheckMenuItem/SetMenuInfo/GetSubMenu/AppendMenuW User32 fns).
From src/desktop/ffi.rs (Plan 02.1-04 lands ID_TAB_STRIP=114, ID_TAB_DETAILS=3020..ID_TAB_ERRORS=3024, MAKEWPARAM, LoadCursorW/SetCursor/GetParent/PostMessageW).
From src/desktop/tabs.rs (Plan 02.1-04 lands tabs::register_class(h_instance), tabs::tab_window_proc, tabs::recompute_rects, tabs::TAB_CLASS_NAME = "FileTreeTabStrip").

New surface this plan adds:

In src/desktop/state.rs (extending Plan 02.1-04 DesktopState):
  pub(super) view_menu: Hmenu,                  // cached for WM_INITMENUPOPUP filter (Pitfall 7)
  // DesktopState::new initializes to 0; populated in create_controls.

  pub(super) fn drive_letter_from_path(path: &str) -> Option<char>;
  // Pure fn. Handles:
  //   "C:\\Users\\..."           → Some('C')
  //   "c:/users/..."             → Some('C')  (uppercased)
  //   "\\\\?\\D:\\..."           → Some('D')  (long-path prefix stripped)
  //   "\\\\server\\share\\..."   → None       (UNC path — no single drive letter; caller falls back to 'C')
  //   ""                         → None
  //   "/home/user/..."           → None       (POSIX path on non-Windows scan input — fallback to 'C')
  //   "1:\\foo"                  → None       (non-alpha drive letter)
  // Implementation: trim leading "\\\\?\\" if present; check bytes[0].is_ascii_alphabetic() && bytes[1] == b':'.

In src/desktop/ffi.rs:
  pub(super) const ID_VIEW_SHOW_HIDDEN: isize = 3010;
  pub(super) const ID_VIEW_SHOW_FILES:  isize = 3011;
  pub(super) const ID_VIEW_DARK_MODE:   isize = 3012;
  // Hmenu = isize alias should already exist from Plan 02.1-03; if not, add it.

Accelerator table extension (LoadAcceleratorsW table OR CreateAcceleratorTableW at runtime):
  Win32 ACCEL { fVirt: FVIRTKEY | FCONTROL, key: VK_1..VK_5, cmd: ID_TAB_DETAILS..ID_TAB_ERRORS }.
  VK_1 = 0x31, VK_2 = 0x32, ..., VK_5 = 0x35.
  FVIRTKEY = 0x01, FCONTROL = 0x08.
  If LoadAcceleratorsW reads a .rc resource file (Phase 02 added the resource — verify), edit the .rc text to add 5 new ACCEL lines. If accelerators are built at runtime via CreateAcceleratorTableW (more likely given no build.rs constraint), extend the ACCEL[] array there.

Menu construction skeleton (shell.rs:162-211 is the closest analog):
  let main_menu = CreateMenu();
  let view_menu = CreatePopupMenu();
  let label_show_hidden = crate::io::wide("&Show Hidden");
  AppendMenuW(view_menu, MF_STRING, ID_VIEW_SHOW_HIDDEN as usize, label_show_hidden.as_ptr());
  let label_show_files = crate::io::wide("Show &Files");
  AppendMenuW(view_menu, MF_STRING, ID_VIEW_SHOW_FILES as usize, label_show_files.as_ptr());
  let label_dark = crate::io::wide("&Dark Mode");
  AppendMenuW(view_menu, MF_STRING, ID_VIEW_DARK_MODE as usize, label_dark.as_ptr());
  let label_view = crate::io::wide("&View");
  AppendMenuW(main_menu, MF_POPUP, view_menu as usize, label_view.as_ptr());
  SetMenu(state.hwnd, main_menu);
  state.view_menu = view_menu;

  // MF_POPUP = 0x0010 — add to ffi.rs constants if missing.

WM_INITMENUPOPUP filter (PATTERNS.md lines 209-227):
  WM_INITMENUPOPUP => {
      let hmenu = wparam as Hmenu;
      with_state_mut(|state| {
          if hmenu == state.view_menu {
              let check = |id: isize, on: bool| unsafe {
                  CheckMenuItem(hmenu, id as Uint, MF_BYCOMMAND | if on { MF_CHECKED } else { MF_UNCHECKED });
              };
              check(ID_VIEW_SHOW_HIDDEN, state.settings.show_hidden);
              check(ID_VIEW_SHOW_FILES,  state.settings.show_files);
              check(ID_VIEW_DARK_MODE,   state.settings.dark_mode);
          }
      });
      0
  }

ID_TAB_* arm template (one of 5) — note save_settings_if_dirty is OUTSIDE the closure:
  ID_TAB_DETAILS => {
      let snap = with_state_mut(|state| {
          activate_tab(state, ActiveTab::Details);
          state::snapshot_for_save(state)
      }).flatten();
      if let Some(snap) = snap { state::save_settings_if_dirty(snap); }   // ← OUTSIDE the closure
      0
  }
  // 4 more arms identical except for the ActiveTab variant. PREFER calling the activate_tab helper —
  // single mutation site, single visual side-effect site, save runs at function-body indentation only.

ID_VIEW_DARK_MODE arm (re-routed from the existing ID_DARK_CHECK arm at mod.rs:346-360):
  ID_VIEW_DARK_MODE => {
      let snap = with_state_mut(|state| {
          state.dark_mode = !state.dark_mode;
          state.settings.dark_mode = state.dark_mode;
          DARK_MODE_ATOMIC.store(state.dark_mode, Ordering::Relaxed);
          apply_theme(state);
          // apply_dark_mode_to_window already called per-HWND by apply_theme post-02.1-03 extension
          let _ = render_list(state);
          InvalidateRect(state.hwnd, null(), 1);
          state::snapshot_for_save(state)
      }).flatten();
      if let Some(snap) = snap { state::save_settings_if_dirty(snap); }
      0
  }
  // Same shape for ID_VIEW_SHOW_HIDDEN and ID_VIEW_SHOW_FILES (toggle the bool, save).

WM_DWMCOLORIZATIONCOLORCHANGED arm:
  WM_DWMCOLORIZATIONCOLORCHANGED => {
      with_state_mut(|state| {
          theme::refresh_accent(state);
          InvalidateRect(state.tab_strip, null(), 1);   // accent underline is on the tab strip only
      });
      0
  }

Post-ShowWindow active-tab restore (mod.rs:150-152 analog):
  ShowWindow(hwnd, SW_SHOW);
  UpdateWindow(hwnd);
  start_scan_from_controls(hwnd);
  // 02.1 NEW: restore active tab — read settings, map to ID_TAB_*, post WM_COMMAND through the same code path as user click.
  let restore_tab_id = with_state_mut(|state| {
      ActiveTab::from_str(&state.settings.active_tab).unwrap_or(ActiveTab::Details) as isize
  }).unwrap_or(0) + ID_TAB_DETAILS;
  PostMessageW(hwnd, WM_COMMAND, MAKEWPARAM(restore_tab_id as u16, 0) as Wparam, 0);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ID_VIEW_* constants to ffi.rs; add view_menu field + drive_letter_from_path pure fn + unit test to state.rs</name>
  <files>src/desktop/ffi.rs, src/desktop/state.rs</files>
  <read_first>
    - src/desktop/ffi.rs (locate the existing ID block around lines 122-134 and the MF_* constants block added by Plan 02.1-03 — add MF_POPUP = 0x0010 here if missing)
    - src/desktop/state.rs (locate DesktopState struct + DesktopState::new initializer; the existing Plan 02.1-04 additions of ActiveTab/tab_strip/tab_panels are the immediate-prior field block)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md (lines 122-134 for ID block + lines 277-300 for new menu IDs)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md (drive_letter_from_path test cases — exact list)
  </read_first>
  <behavior>
    - Test `drive_letter_from_path_cases`: assert these 9 exact cases (from VALIDATION.md + Pitfall 1):
      - `drive_letter_from_path("C:\\Users\\foo") == Some('C')`
      - `drive_letter_from_path("c:/users/foo") == Some('C')` (lowercase input → uppercase output)
      - `drive_letter_from_path("D:\\") == Some('D')`
      - `drive_letter_from_path("\\\\?\\E:\\very\\long\\path") == Some('E')` (long-path prefix stripped)
      - `drive_letter_from_path("\\\\server\\share\\folder") == None` (UNC — caller falls back to 'C')
      - `drive_letter_from_path("") == None`
      - `drive_letter_from_path("/home/user/foo") == None` (POSIX path)
      - `drive_letter_from_path("1:\\foo") == None` (non-alpha)
      - `drive_letter_from_path(":") == None` (degenerate)
  </behavior>
  <action>
    Three edits.

    Edit 1 (src/desktop/ffi.rs) — Add the 3 menu-item ID constants alongside the existing ID block. Use the values 3010 / 3011 / 3012 per PATTERNS.md "NEW 02.1 IDs" note. If `MF_POPUP` is not already present (Plan 02.1-03 may have added it; if not), add `pub(super) const MF_POPUP: Uint = 0x0010;` adjacent to the other MF_* constants. If `Hmenu` alias is not present (it should be from Plan 02.1-03), add `pub(super) type Hmenu = isize;`.

    Edit 2 (src/desktop/state.rs) — Add `pub(super) view_menu: Hmenu,` field to `DesktopState`, placed adjacent to the existing menu-related fields (or alongside the new `tab_strip: Hwnd` from Plan 02.1-04). In `DesktopState::new`, initialize `view_menu: 0` (populated in create_controls when the menu is built).

    Edit 3 (src/desktop/state.rs) — Add a pure free fn `pub(super) fn drive_letter_from_path(path: &str) -> Option<char>` per <interfaces>. Implementation strips a leading `\\?\` prefix if present, then accepts when `bytes[0].is_ascii_alphabetic() && bytes[1] == b':'`, returning the uppercased letter as `char`; otherwise returns `None`. No reliance on `Path::new` — pure byte parsing keeps it cross-platform testable; the real Windows call site passes `&state.settings.last_path` which is a `String`.

    Add the `drive_letter_from_path_cases` test inside the existing `#[cfg(test)] mod tests` block in state.rs (extend the Plan 02.1-04 `active_tab_enum` test block) — assert all 9 cases per <behavior>.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --lib desktop::state::tests::drive_letter_from_path_cases &amp;&amp; cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - CI quad passes.
    - `cargo test --lib desktop::state::tests::drive_letter_from_path_cases` PASSES.
    - `grep -c "ID_VIEW_SHOW_HIDDEN: isize = 3010" src/desktop/ffi.rs` reports 1.
    - `grep -c "ID_VIEW_SHOW_FILES: isize = 3011" src/desktop/ffi.rs` reports 1.
    - `grep -c "ID_VIEW_DARK_MODE: isize = 3012" src/desktop/ffi.rs` reports 1.
    - `grep -c "view_menu: Hmenu" src/desktop/state.rs` reports 1 (the struct field).
    - `grep -c "fn drive_letter_from_path" src/desktop/state.rs` reports 1.
  </acceptance_criteria>
  <done>Constants + state field + tested pure fn ready for Task 2's mod.rs rewire.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire src/desktop/mod.rs — delete toolbar, build View menu, add WM_INITMENUPOPUP + WM_DWMCOLORIZATIONCOLORCHANGED + ID_TAB_* + ID_VIEW_* arms, extend accelerators, lay out tab strip + 5 panels, fix drive-picker restore, post-show active-tab restore</name>
  <files>src/desktop/mod.rs</files>
  <read_first>
    - src/desktop/mod.rs (READ THE WHOLE FILE — every edit landmark below is referenced by approximate line; executor confirms exact line via Read)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md (the entire "src/desktop/mod.rs" section — lines 142-256, plus the shared-pattern sections "Save-on-mutate" and "Reentrancy-safe state access")
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md (Pitfall 7 WM_INITMENUPOPUP filter; Pitfall 6 LoadAcceleratorsW table extension; Pattern 7 WM_DWMCOLORIZATIONCOLORCHANGED)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md §Components & States (menu bar height, tab strip height, content-panel layout)
    - src/desktop/shell.rs lines 162-211 (the in-repo CreatePopupMenu + AppendMenuW shape — pattern verbatim)
    - src/desktop/state.rs (post-Task-1 view_menu field + drive_letter_from_path pure fn — call sites for both land in this task)
    - src/desktop/tabs.rs (Plan 02.1-04 register_class + TAB_CLASS_NAME — CreateWindowExW the tab strip with this class)
    - src/desktop/theme.rs (Plan 02.1-03 refresh_accent + apply_dark_mode_to_window — call sites for both land in this task)
  </read_first>
  <action>
    Apply 9 edits in src/desktop/mod.rs. Each edit is independent — if a `read_first` line landmark differs from the documented approximate range, locate via the function signature or unique identifier instead.

    Edit 1 — DELETE toolbar checkbox creation. In `create_controls` (approximately mod.rs:686-721), delete the 4 `create_child(... BS_AUTOCHECKBOX ..., ID_HIDDEN_CHECK | ID_FILES_CHECK | ID_FOLLOW_CHECK | ID_DARK_CHECK)` calls. Also delete the BM_SETCHECK initializer block that follows (approximately mod.rs:753-771) which sets initial check states from settings. Also delete the layout block in `resize_controls` (approximately mod.rs:909-975) that positions the 4 checkboxes. The fields `state.hidden_check`, `state.files_check`, `state.follow_check`, `state.dark_check` may stay on DesktopState as 0-valued (handles never created) OR be deleted from state.rs if no other consumer remains. Prefer leaving the fields (deletion ripples are out of scope) and add a comment `// Phase 02.1 — toolbar checkboxes removed in favor of View menu; fields retained as 0 for binary compat.` next to the field declarations in state.rs IF you keep them, OR delete the four fields cleanly from state.rs IF no consumer remains (grep for each handle name to verify zero consumers; if zero, delete the fields).

    Edit 2 — BUILD View menu in `create_controls` AFTER the main HWND exists and BEFORE the first ShowWindow. Use the menu-construction skeleton from <interfaces>:
      - Call `CreateMenu()` for the main menu bar.
      - Call `CreatePopupMenu()` for the View submenu.
      - For each of the 3 View items, build a wide string via `crate::io::wide(&str)`, call `AppendMenuW(view_menu, MF_STRING, ID as usize, label.as_ptr())`. Labels are `"&Show Hidden"`, `"Show &Files"`, `"&Dark Mode"` — the `&` is the ALT-key accelerator hint (NOT C-string escape).
      - Attach the View submenu to the main menu via `AppendMenuW(main_menu, MF_POPUP, view_menu as usize, &label_view)` where label_view = `"&View"`.
      - Call `SetMenu(state.hwnd, main_menu)` to attach the menu bar.
      - Store: `state.view_menu = view_menu;` (Pitfall 7 cache).

    Edit 3 — CREATE tab strip + 5 content panels in `create_controls` AFTER the menu but BEFORE resize_controls. Sequence:
      - `tabs::register_class(h_instance);` (idempotent OnceLock — safe to call here)
      - `let tab_strip = CreateWindowExW(0, crate::io::wide(tabs::TAB_CLASS_NAME).as_ptr(), null(), WS_CHILD | WS_VISIBLE, 0, 0, 0, 0, state.hwnd, ID_TAB_STRIP as Hmenu, h_instance, null_mut());`
      - `state.tab_strip = tab_strip;`
      - For each tab index 0..5, create a panel HWND. The Details panel hosts the existing list-view (whatever HWND was the main content before this phase — re-parent it to `tab_panels[0]` if possible, or set `tab_panels[0] = state.list_hwnd` and skip CreateWindowExW for index 0). For indexes 1..4, create a STATIC child window with placeholder text "Coming soon — Top files / Extensions / Duplicates / Errors" matching the appropriate label from `TAB_LABELS`. These 4 panels are throwaway scaffolding; Phase 03+ replaces each with real content. Use class `"STATIC"` and styles `WS_CHILD | SS_LEFT | SS_NOPREFIX` (SS_LEFT = 0x0000, SS_NOPREFIX = 0x0080; add to ffi.rs if missing). Show only the active panel (`SW_SHOW` for the index matching the current `state.active_tab`, `SW_HIDE` for the rest); start with all HIDE then trigger Edit 9 to SHOW the correct one.
      - Call `apply_dark_mode_to_window(tab_strip, state.dark_mode)` per Plan 02.1-03 helper for the strip + each panel HWND. This satisfies D-04 first-paint flash mitigation for the new HWNDs.

    Edit 4 — LAYOUT tab strip + panels in `resize_controls`. The path-bar / drive-picker / scan-button row stays at the top. Below that, insert the 30 px tab strip (DPI-scaled via `MulDiv(30, GetDpiForWindow(hwnd), 96)`) full-width. Below the strip, the content panels fill the remaining client area down to the status-footer top edge (status footer is the existing `state.status` HWND or the new `state.status_footer` from Plan 02.1-06 — whichever exists at execution time). All 5 panels get the SAME rect; only one is visible at a time. After moving the strip, call `tabs::recompute_rects(state.tab_strip, total_width, dpi, state.font)` and store the result in `state.tab_rects`. After moving each panel, no per-index InvalidateRect needed (`MoveWindow` invalidates).

    Edit 5 — ADD `WM_INITMENUPOPUP` arm to `window_proc` match (place near the existing `WM_LBUTTONUP` at approximately mod.rs:510). Copy the skeleton verbatim from <interfaces>. The Pitfall 7 `if hmenu == state.view_menu` filter is non-negotiable — without it, the right-click Shell context menu (built by shell.rs) gets its check states corrupted on every popup.

    Edit 6 — ADD `WM_DWMCOLORIZATIONCOLORCHANGED` arm. Place adjacent to the new WM_INITMENUPOPUP arm. Body per <interfaces>: `theme::refresh_accent(state)` + `InvalidateRect(state.tab_strip, null(), 1)` (the strip is the only surface that paints the accent — main window doesn't need a full invalidate).

    Edit 7 — ADD 5×ID_TAB_* + 3×ID_VIEW_* WM_COMMAND arms. Find the existing WM_COMMAND match (around the `ID_DARK_CHECK` arm at mod.rs:346-360). Define a private helper `fn activate_tab(state: &mut DesktopState, tab: ActiveTab)` that assigns `state.active_tab = tab`, mirrors `state.settings.active_tab = tab.as_str().to_string()`, iterates `state.tab_panels` calling `ShowWindow(panel, SW_SHOW)` for the matching index and `SW_HIDE` for the rest, then `InvalidateRect(state.tab_strip, null(), 1)`. Critically: `activate_tab` is invoked INSIDE `with_state_mut(|state| { ... })` (try_lock; non-blocking per PATTERNS.md Reentrancy-safe state access) — it does NOT call save_settings_if_dirty itself. Then 5 arms (one per ID_TAB_*) follow the same shape: `let snap = with_state_mut(|state| { activate_tab(state, ActiveTab::Details); state::snapshot_for_save(state) }).flatten(); if let Some(snap) = snap { state::save_settings_if_dirty(snap); } 0` — save_settings_if_dirty MUST be at function-body indentation OUTSIDE the closure (PATTERNS.md non-negotiable). For the 3 ID_VIEW_* arms, follow the existing ID_DARK_CHECK pattern (mod.rs:346-360) but toggle the bool with `!` (no checkbox handle to query). ID_VIEW_DARK_MODE additionally calls `apply_theme(state)` and updates `DARK_MODE_ATOMIC`. After mutation, snapshot + save outside the lock. **DELETE the old ID_HIDDEN_CHECK / ID_FILES_CHECK / ID_FOLLOW_CHECK / ID_DARK_CHECK arms** since their HWND consumers were removed in Edit 1 — replaced by the 3 ID_VIEW_* arms.

    Edit 8 — EXTEND accelerator table for Ctrl+1..Ctrl+5. Two paths:
      - If `LoadAcceleratorsW` reads from a .rc resource file: locate the .rc and add 5 new ACCEL entries (`"1", ID_TAB_DETAILS, VIRTKEY, CONTROL` ... `"5", ID_TAB_ERRORS, VIRTKEY, CONTROL`).
      - More likely: accelerators are built at runtime via `CreateAcceleratorTableW` with a static `ACCEL[]` array. Add 5 entries to that array. The ACCEL struct (declare in ffi.rs if missing): `#[repr(C)] pub(super) struct ACCEL { fVirt: u8, padding: u8, key: u16, cmd: Uint }`. FVIRTKEY=0x01, FCONTROL=0x08; OR them together → 0x09. Keys VK_1=0x31..VK_5=0x35. Cmd = ID_TAB_DETAILS..ID_TAB_ERRORS.
      Verify which path Phase 02 used (`grep -E "LoadAcceleratorsW|CreateAcceleratorTableW" src/desktop/`) and extend that one. Do not introduce a parallel mechanism.

    Edit 9 — RESTORE active tab on launch. In the `run` fn after `ShowWindow(hwnd, SW_SHOW); UpdateWindow(hwnd); start_scan_from_controls(hwnd);` (approximately mod.rs:150-152), append a block that calls `with_state_mut` to read `ActiveTab::from_str(&state.settings.active_tab).unwrap_or(ActiveTab::Details) as isize`, unwraps to 0 on lock contention, adds `ID_TAB_DETAILS` to get the command id, then `PostMessageW(hwnd, WM_COMMAND, MAKEWPARAM(restore_tab_id as u16, 0) as Wparam, 0)`. This routes through the same ID_TAB_* arm a user click would — single code path; the message pump processes it on the next iteration. ALSO in the same block, FIX the drive-picker restore (the D-11 one-line): if `state.drive_picker != 0`, read `state.settings.last_path` inside `with_state_mut` (falling back to `path_to_string(&state.initial_path)` when empty), drop the lock, then call `populate_drive_picker(state.drive_picker, &path)`. The existing `populate_drive_picker` already pre-selects the drive letter from its `path` argument (line 1729) — the fix is just to PASS settings.last_path instead of always initial_path. drive_letter_from_path is invoked INSIDE populate_drive_picker as part of the existing logic; the new pure fn from Task 1 is available for any new call site but populate_drive_picker's existing internal extraction logic should be replaced by `drive_letter_from_path(path).unwrap_or('C')` if it currently uses ad-hoc parsing — verify via Read before editing. If populate_drive_picker already has clean drive-letter logic, leave it; we only need the call site fix to use settings.last_path.

    All 9 edits must compile together. Run cargo build incrementally during the work if useful; the verify step is the final gate.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo build --release &amp;&amp; cargo test --lib desktop::</automated>
  </verify>
  <acceptance_criteria>
    - CI quad passes (fmt, clippy, build, test).
    - `cargo build --release` produces a binary that launches and shows: a menu bar with `View` menu; a 30 px tab strip with 5 cells (Details / Top / Extensions / Duplicates / Errors); the Details tab content (the existing list-view) visible; the other 4 tabs hidden until clicked.
    - No toolbar checkboxes visible in the window chrome.
    - `grep -c "create_child.*BS_AUTOCHECKBOX" src/desktop/mod.rs` reports 0 (toolbar checkboxes deleted).
    - `grep -c "ID_HIDDEN_CHECK =>" src/desktop/mod.rs` reports 0 (old arm gone).
    - `grep -c "ID_DARK_CHECK =>" src/desktop/mod.rs` reports 0 (old arm gone, replaced by ID_VIEW_DARK_MODE).
    - `grep -c "ID_VIEW_DARK_MODE =>" src/desktop/mod.rs` reports 1.
    - `grep -c "ID_VIEW_SHOW_HIDDEN =>" src/desktop/mod.rs` reports 1.
    - `grep -c "ID_VIEW_SHOW_FILES =>" src/desktop/mod.rs` reports 1.
    - `grep -c "ID_TAB_DETAILS =>" src/desktop/mod.rs` reports 1.
    - `grep -c "ID_TAB_ERRORS =>" src/desktop/mod.rs` reports 1.
    - `grep -c "WM_INITMENUPOPUP =>" src/desktop/mod.rs` reports 1.
    - `grep -c "WM_DWMCOLORIZATIONCOLORCHANGED =>" src/desktop/mod.rs` reports 1.
    - `grep -v '^[[:space:]]*//' src/desktop/mod.rs | grep -c "state.view_menu"` reports at least 2 (assignment + the WM_INITMENUPOPUP filter check).
    - `grep -c "SetMenu" src/desktop/mod.rs` reports at least 1 (menu attached).
    - `grep -c "PostMessageW.*WM_COMMAND.*MAKEWPARAM" src/desktop/mod.rs` reports at least 1 (active-tab restore).
    - `grep -c "drive_letter_from_path" src/desktop/mod.rs` reports at least 1 OR `state.settings.last_path` appears in the populate_drive_picker call site (verifies D-11 wiring; drive_letter_from_path may be invoked inside populate_drive_picker rather than at the call site).
    - `grep -c "ACCEL\\b" src/desktop/mod.rs src/desktop/ffi.rs` reports at least 1 (accelerator table extended OR ACCEL struct declared for runtime use).
    - **activate_tab reentrancy discipline (WARNING 2 gate):**
      - `grep -c "fn activate_tab" src/desktop/mod.rs` reports exactly 1 (helper defined).
      - `awk '/fn activate_tab/,/^}/' src/desktop/mod.rs | grep -c "save_settings_if_dirty"` reports 0 (activate_tab itself MUST NOT call save — it runs inside with_state_mut where the lock is held).
      - `grep -c "save_settings_if_dirty" src/desktop/mod.rs` reports at least 5 (one per ID_TAB_* arm — Details, Top, Extensions, Duplicates, Errors — plus any ID_VIEW_* arms; expect ≥5, typically 8).
      - Every `save_settings_if_dirty(` call site is at function-body indentation, not inside a closure: `awk '/save_settings_if_dirty\(/' src/desktop/mod.rs | grep -cE "^[[:space:]]{4,8}if let Some\(snap\)"` reports at least 5 — i.e., the canonical `if let Some(snap) = snap { state::save_settings_if_dirty(snap); }` line at 4-or-8-space indent (function-body level for the WM_COMMAND match arm), NOT at 12+ spaces which would indicate it's inside the `with_state_mut(|state| { ... })` closure body.
      - `grep -B 2 "save_settings_if_dirty" src/desktop/mod.rs | grep -c "with_state_mut"` reports 0 — there must be NO `with_state_mut` line in the 2 lines immediately preceding any save_settings_if_dirty call (the canonical pattern interposes `.flatten();` and `if let Some(snap) = snap {`, so the closing `})` of with_state_mut is at least 3 lines back, never within 2 lines).
      - Manual code review (recorded in SUMMARY): for each of the 5 ID_TAB_* arms, confirm the structural pattern `let snap = with_state_mut(|state| { activate_tab(state, …); state::snapshot_for_save(state) }).flatten();` is followed by `if let Some(snap) = snap { state::save_settings_if_dirty(snap); }` on the next line — save is OUTSIDE the closure.
    - Manual: launch binary, press Ctrl+2 → Top tab activates; Ctrl+5 → Errors tab; close binary; relaunch → last-active tab is restored (verify via VALIDATION.md §Manual Test Plan).
    - Manual: launch binary, change Windows accent color via Settings app while binary is running → the active tab's accent underline updates without re-launch (WM_DWMCOLORIZATIONCOLORCHANGED working).
  </acceptance_criteria>
  <done>Phase 02.1's main rewiring complete: toolbar gone, menu bar present, 5 content tabs working with persistence + accelerators, system accent honored live, drive picker restores to last drive on launch, and activate_tab reentrancy discipline verified (save_settings_if_dirty outside with_state_mut closure on every ID_TAB_* arm).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → process | settings.json fields (active_tab, last_path, dark_mode, show_hidden, show_files) drive UI state at launch |
| user input → window proc | WM_COMMAND messages (menu items, accelerator keys, tab clicks via PostMessageW from tabs.rs) |
| window proc → window proc | PostMessageW for active-tab restore re-enters WM_COMMAND through the same arm a user click triggers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-05-01 | Tampering | settings.active_tab string from disk | mitigate | ActiveTab::from_str returns None for unknown strings; restore uses unwrap_or(Details). Pure fn unit-tested in Plan 02.1-04 (active_tab_enum). No code-injection path. |
| T-02.1-05-02 | DoS | WM_INITMENUPOPUP without Pitfall 7 hmenu filter | mitigate | The `if hmenu == state.view_menu` guard is enforced in Edit 5 and verified by source assertion (grep). Without it, Shell context menus (right-click) would have their states corrupted on every popup. |
| T-02.1-05-03 | Tampering | settings.last_path drive-letter parse | mitigate | drive_letter_from_path is a pure fn unit-tested for all 5 path shapes (Task 1). unwrap_or('C') at the call site means any unparseable path falls back to C: without crash. |
| T-02.1-05-04 | DoS | Active-tab restore PostMessageW races first-paint | mitigate | PostMessageW is async — message is queued and processed after the message pump pulls it. ShowWindow + UpdateWindow have already completed by the time WM_COMMAND fires. activate_tab is a pure UI mutation (ShowWindow on tab_panels) — safe to call any time after panels exist. |
| T-02.1-05-05 | DoS | activate_tab reentrancy / save-while-locked deadlock | mitigate | activate_tab runs inside with_state_mut (try_lock; non-blocking); save_settings_if_dirty is positioned OUTSIDE the closure per PATTERNS.md non-negotiable. Verified by grep + indentation awk check in Task 2 acceptance criteria. |
| n/a | Spoofing | n/a | not applicable | No identity surface |
| n/a | Repudiation | n/a | not applicable | No audit surface |
| n/a | Information Disclosure | n/a | not applicable | Active tab + last drive are user preferences, not sensitive |
| n/a | Elevation of Privilege | n/a | not applicable | No privilege boundary crossed |
</threat_model>

<verification>
- CI quad: `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo build --release && cargo test`.
- All unit tests (settings active_tab from Plan 02.1-02, ActiveTab enum + tabs from Plan 02.1-04, theme from Plan 02.1-03, drive_letter_from_path from this plan Task 1) green.
- Manual: launch binary, exercise each of:
  - View → Show Hidden toggles a check mark and shows hidden files.
  - View → Show Files toggles a check mark and shows/hides file entries.
  - View → Dark Mode toggles the entire chrome.
  - Clicking each tab cell switches the visible content panel and updates the accent underline.
  - Ctrl+1..Ctrl+5 switches tabs identically.
  - Close + re-launch: last-active tab restores; last-scanned drive pre-selected in the drive picker.
  - Change Windows accent color while binary is running: accent underline updates live.
</verification>

<success_criteria>
- D-06 closed: toolbar checkbox row deleted from the UI.
- D-08 closed: View menu with 3 toggles + 5-tab strip with content panels active.
- D-09 closed: WM_DWMCOLORIZATIONCOLORCHANGED arm refreshes accent + repaints strip.
- D-11 closed (POL-01): drive picker pre-selects settings.last_path's drive on launch.
- D-12 closed (POL-02): Ctrl+1..Ctrl+5 accelerators switch tabs.
- D-13 closed (SET-01): settings.active_tab is read at launch and routes through the same ID_TAB_* arm as a user click.
- Pitfall 7 honored: WM_INITMENUPOPUP filtered by state.view_menu so Shell context menus are unaffected.
- PATTERNS.md reentrancy discipline honored: activate_tab runs inside with_state_mut; save_settings_if_dirty runs OUTSIDE the closure on every ID_TAB_* and ID_VIEW_* arm. Verified by grep + awk indentation checks in Task 2 acceptance criteria.
- All Phase 02 desktop tests continue to pass (no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-05-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Record:
- Which accelerator path was used (.rc edit OR runtime CreateAcceleratorTableW array extension).
- Whether the 4 toolbar checkbox fields on DesktopState were deleted or retained as 0-valued.
- The Details panel strategy: was state.list_hwnd re-parented to tab_panels[0], or was tab_panels[0] aliased to the existing list HWND?
- Final line numbers for WM_INITMENUPOPUP arm, WM_DWMCOLORIZATIONCOLORCHANGED arm, and the activate_tab helper so Plan 02.1-06 (owner-draw menu) can navigate.
- Explicit confirmation that save_settings_if_dirty is positioned OUTSIDE every with_state_mut closure on the 5 ID_TAB_* arms and the 3 ID_VIEW_* arms (per WARNING 2 gate).
</output>
