---
phase: 02.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/desktop/theme.rs
  - src/desktop/state.rs
  - src/desktop/ffi.rs
autonomous: true
requirements:
  - POL-03
must_haves:
  truths:
    - "Dark palette constants match UI-SPEC exact RGB values (no brown tint)"
    - "Accent color is queryable, cached on DesktopState, and recomputed on demand"
    - "selected_text_for_accent returns black when accent luminance > 0.6, else white"
    - "uxtheme_ordinals() returns cached Option<fn> pointers for ordinals 135/133/136 with null-check fallback"
    - "The dual DwmSetWindowAttribute(20, ...) / (19, ...) cross-version pattern in set_window_dark_mode is preserved"
  artifacts:
    - path: "src/desktop/theme.rs"
      provides: "D-05 palette rewrite, accent caching, uxtheme ordinal resolver, WCAG luminance helper, apply_dark_mode_to_window consolidated helper"
      contains: "uxtheme_ordinals, refresh_accent, selected_text_for_accent, palette_bg, palette_panel, palette_text, palette_line, palette_disabled"
    - path: "src/desktop/state.rs"
      provides: "accent_color + selected_text_color fields on DesktopState"
      contains: "accent_color: Dword, selected_text_color: Dword"
    - path: "src/desktop/ffi.rs"
      provides: "FFI declarations for DwmGetColorizationColor, LoadLibraryW, GetProcAddress, WM_DWMCOLORIZATIONCOLORCHANGED, and other constants needed by theme.rs"
  key_links:
    - from: "src/desktop/theme.rs uxtheme_ordinals()"
      to: "uxtheme.dll ordinals 135/133/136"
      via: "LoadLibraryW + GetProcAddress with MAKEINTRESOURCEA cast + null-check"
      pattern: "LoadLibraryW.*uxtheme"
    - from: "src/desktop/theme.rs refresh_accent"
      to: "DwmGetColorizationColor"
      via: "extern fn returning ARGB; strip alpha; convert to COLORREF; recompute luminance"
      pattern: "DwmGetColorizationColor"
    - from: "src/desktop/theme.rs selected_text_for_accent"
      to: "DesktopState.selected_text_color"
      via: "WCAG luminance formula; threshold 0.6; called by refresh_accent"
      pattern: "selected_text_for_accent"

threat_model:
  applicable: [V14]
  not_applicable: [V2, V3, V4, V5, V6, V10]
  threats:
    - id: T-02.1-03-01
      category: D
      component: GetProcAddress(uxtheme.dll, ordinal 135) on Win10 1809 (signature mismatch)
      disposition: mitigate
      mitigation: "Per Pitfall 2 in RESEARCH.md, ordinal 135 had a different signature on Win10 1809 (AllowDarkModeForApp(BOOL)) vs 1903+ (SetPreferredAppMode(PreferredAppMode)). Mitigation: defensively null-check the GetProcAddress result; if missing, palette + DWMWA flips still work (scrollbar/combobox stay light only — visible degradation, not a crash). Optional follow-up: gate on RtlGetVersion >= build 18362 if a 1809 user reports issues. Phase 02.1 ships the null-check only (sufficient for the documented target audience)."
    - id: T-02.1-03-02
      category: D
      component: theme.rs palette rewrite breaking existing list-view colors
      disposition: mitigate
      mitigation: "Unit test palette_matches_spec (Wave 0) is a regression guard. Test asserts every palette_* fn returns the exact RGB triple from UI-SPEC. Any drift fails CI."
---

<objective>
Extend `src/desktop/theme.rs` with the D-05 palette rewrite, system-accent caching, WCAG luminance helper, and uxtheme ordinal resolver. Extend `DesktopState` with the two new accent-derived fields. Extend `ffi.rs` with the supporting FFI declarations. Three new unit tests guard the palette and luminance contracts.

Purpose: This plan is the theme primitives layer. Plans 02.1-04 (tabs), 02.1-05 (mod.rs rewire), 02.1-06 (owner-draw menu + status footer), and 02.1-07 (dark bootstrap) all depend on these primitives being correct and tested before they wire any UI on top. POL-03 (mnemonic accessibility) depends on this plan because the `UISF_HIDEACCEL` paint paths (Plans 02.1-05/06) need the palette and accent helpers to render menu/tab labels with proper ALT-mnemonic overlay colors. **Wave 1 with no dependencies — parallel with 02.1-01 and 02.1-02.**

Output:
- `theme.rs` palette constants rewritten to UI-SPEC values (closing Pitfall 9 — the current `rgb(12,13,15)` brown-tinted dark bg becomes the neutral `#1e1e1e = rgb(30,30,30)`).
- `theme.rs` new public helpers: `uxtheme_ordinals() -> &'static UxthemeOrdinals`, `refresh_accent(state: &mut DesktopState)`, `selected_text_for_accent(r,g,b) -> Dword`, `apply_dark_mode_to_window(hwnd) -> ()` (consolidates the existing dual-DWMWA call + `AllowDarkModeForWindow` + `SetWindowTheme("DarkMode_Explorer")` into one helper Plans 02.1-04/05/07 call per-HWND).
- `state.rs` new fields: `accent_color: Dword`, `selected_text_color: Dword`, plus the three `Option<fn>` uxtheme ordinal pointers (cached at startup but stored on state for convenient `with_state_mut` access).
- `ffi.rs` new declarations: `DwmGetColorizationColor` (Dwmapi block), `LoadLibraryW` + `GetProcAddress` (Kernel32 block), plus the `WM_DWMCOLORIZATIONCOLORCHANGED` / `WM_INITMENUPOPUP` / `WM_DRAWITEM` / `WM_MEASUREITEM` / `MF_*` / `MIM_*` / `MENUINFO` / `DRAWITEMSTRUCT` / `MEASUREITEMSTRUCT` constants & structs that subsequent plans need.
- 3 new unit tests in `src/desktop/theme.rs` (new `#[cfg(test)] mod tests` block): `accent_luminance`, `selected_text_threshold`, `palette_matches_spec`.

Critical preservation: the existing `set_window_dark_mode` dual-call pattern (`DwmSetWindowAttribute(hwnd, 20, ...); DwmSetWindowAttribute(hwnd, 19, ...);`) at theme.rs lines ~37–42 is LOAD-BEARING per Pitfall 3 — DO NOT consolidate into a single call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-CONTEXT.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md
@.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md
@src/desktop/theme.rs
@src/desktop/state.rs
@src/desktop/ffi.rs

<interfaces>
<!-- Existing theme.rs surface (extend, don't replace). -->

From src/desktop/theme.rs (existing):
- `pub(super) static DARK_BRUSH: OnceLock<Hbrush>`
- `pub(super) static LIGHT_BRUSH: OnceLock<Hbrush>`
- `pub(super) static DARK_MODE_ATOMIC: AtomicBool` — read by WM_CTLCOLOR* paint paths (do not bypass)
- `pub(super) unsafe fn dark_brush() -> Hbrush`
- `pub(super) unsafe fn set_window_dark_mode(hwnd: Hwnd, enabled: bool)` — preserve verbatim (Pitfall 3 dual-call)
- `pub(super) unsafe fn apply_theme(state: &mut DesktopState)` — extend by adding `apply_dark_mode_to_window` calls; do not rewrite
- `pub(super) fn palette_bg(state: &DesktopState) -> Dword` — REWRITE dark branch from `rgb(12,13,15)` to `rgb(30,30,30)` per Pitfall 9
- `pub(super) fn palette_panel(state: &DesktopState) -> Dword` — REWRITE dark branch to `rgb(45,45,45)` (#2d2d2d)
- `pub(super) fn palette_text(state: &DesktopState) -> Dword` — REWRITE dark branch to `rgb(204,204,204)` (#cccccc)
- `pub(super) fn palette_line(state: &DesktopState) -> Dword` — REWRITE dark branch to `rgb(58,58,58)` (#3a3a3a); light branch to `rgb(229,229,229)` (#e5e5e5)
- `pub(super) fn palette_disabled(state: &DesktopState) -> Dword` — REWRITE dark branch to `rgb(122,122,122)` (#7a7a7a); add if not present

New public surface added by this plan (in theme.rs):
  pub(super) struct UxthemeOrdinals {
      pub(super) set_preferred_app_mode: Option<unsafe extern "system" fn(i32) -> i32>,
      pub(super) allow_dark_mode_for_window: Option<unsafe extern "system" fn(Hwnd, i32) -> i32>,
      pub(super) flush_menu_themes: Option<unsafe extern "system" fn()>,
  }
  pub(super) fn uxtheme_ordinals() -> &'static UxthemeOrdinals;
  pub(super) fn refresh_accent(state: &mut DesktopState);
  pub(super) fn selected_text_for_accent(r: u8, g: u8, b: u8) -> Dword;
  pub(super) unsafe fn apply_dark_mode_to_window(hwnd: Hwnd, enabled: bool);
  // PreferredAppMode constants (used by Plan 02.1-07 cli.rs bootstrap):
  pub(super) const PREFERRED_APP_MODE_DEFAULT: i32 = 0;
  pub(super) const PREFERRED_APP_MODE_ALLOW_DARK: i32 = 1;
  pub(super) const PREFERRED_APP_MODE_FORCE_DARK: i32 = 2;
  pub(super) const PREFERRED_APP_MODE_FORCE_LIGHT: i32 = 3;

From src/desktop/state.rs (existing DesktopState struct, lines ~25–78):
- Add fields adjacent to similarly-typed existing fields:
  - `pub(super) accent_color: Dword,`        // raw COLORREF, refreshed on WM_DWMCOLORIZATIONCOLORCHANGED
  - `pub(super) selected_text_color: Dword,` // recomputed from accent_color via WCAG luminance
- DesktopState::new — initialize both to 0 (refresh_accent is called once at startup by Plan 02.1-07; this plan only declares the fields and initializers).
- DO NOT add the ux_* fn-ptr fields to DesktopState — they live in theme.rs OnceLock per the cached-resource convention. (PATTERNS.md showed them as a layout option, but the OnceLock-in-theme.rs is cleaner and matches the DARK_BRUSH precedent.)

From src/desktop/ffi.rs (existing — extend, never create parallel link blocks):
- Existing `#[link(name = "Dwmapi")]` block (lines ~355–363) — append `DwmGetColorizationColor`.
- Existing `#[link(name = "Kernel32")]` block (lines ~405–460) — append `LoadLibraryW` and `GetProcAddress` if missing.
- Constants block: add `WM_DWMCOLORIZATIONCOLORCHANGED: Uint = 0x0320`, `WM_INITMENUPOPUP: Uint = 0x0117`, `WM_DRAWITEM: Uint = 0x002B`, `WM_MEASUREITEM: Uint = 0x002C`, `WM_DPICHANGED: Uint = 0x02E0`, `MF_CHECKED: Uint = 0x0008`, `MF_UNCHECKED: Uint = 0x0000`, `MF_BYCOMMAND: Uint = 0x0000`, `MF_OWNERDRAW: Uint = 0x0100`, `MF_SEPARATOR: Uint = 0x0800`, `MF_STRING: Uint = 0x0000`, `MIM_BACKGROUND: Dword = 0x00000002`, `ODS_SELECTED: Uint = 0x0001`, `ODS_DISABLED: Uint = 0x0004`, `ODS_CHECKED: Uint = 0x0008`, `ODS_HOTLIGHT: Uint = 0x0040`, `UISF_HIDEACCEL: Uint = 0x0002`.
- Struct: `#[repr(C)] pub(super) struct MENUINFO { cbSize: Dword, fMask: Dword, dwStyle: Dword, cyMax: Uint, hbrBack: Hbrush, dwContextHelpID: Dword, dwMenuData: usize }`.
- Struct: `#[repr(C)] pub(super) struct DRAWITEMSTRUCT { CtlType: Uint, CtlID: Uint, itemID: Uint, itemAction: Uint, itemState: Uint, hwndItem: Hwnd, hDC: Hdc, rcItem: Rect, itemData: usize }`.
- Struct: `#[repr(C)] pub(super) struct MEASUREITEMSTRUCT { CtlType: Uint, CtlID: Uint, itemID: Uint, itemWidth: Uint, itemHeight: Uint, itemData: usize }`.

WCAG luminance formula (Pattern 7 in RESEARCH.md, threshold locked at 0.6 per UI-SPEC):
  lum = 0.2126*(r/255) + 0.7152*(g/255) + 0.0722*(b/255)
  selected_text = if lum > 0.6 { rgb(0,0,0) } else { rgb(255,255,255) }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ffi.rs with DwmGetColorizationColor, LoadLibraryW, GetProcAddress, and supporting WM_/MF_/MIM_/ODS_/UISF_ constants and MENUINFO/DRAWITEMSTRUCT/MEASUREITEMSTRUCT structs</name>
  <files>src/desktop/ffi.rs</files>
  <read_first>
    - src/desktop/ffi.rs (READ THE WHOLE FILE — locate the existing Dwmapi link block, Kernel32 link block, constants block, and struct block; new additions land inside the existing blocks, never in parallel)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md (lines 260–349 — exact FFI insertion shape per existing block)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md (Pattern 1 uxtheme resolver context + the FFI signatures table)
    - CLAUDE.md (UPPER_SNAKE_CASE for Win32 constants matching their native names; PascalCase for structs mirroring native; `pub(super)` visibility)
  </read_first>
  <behavior>
    No behavior tests added — this task is pure FFI declarations. Compilation is the test: every constant and struct must compile under `cargo build --release` and pass clippy. The "behavior" being asserted is the type signatures and constant values per the Win32 headers.
  </behavior>
  <action>
    Five edits in src/desktop/ffi.rs.

    Edit 1 — Constants: Add a labeled section comment `// Phase 02.1 — menu, owner-draw, DWM color change, DPI` and declare every constant from the <interfaces> "Constants block" list. Each as `pub(super) const NAME: <Type> = <Hex>;`. Use `Uint` for `WM_*`/`MF_*`/`ODS_*`/`UISF_*` (these are u32/UINT in Win32) and `Dword` for `MIM_BACKGROUND`. If a constant already exists in the file, do NOT duplicate — verify the value matches and skip. Hex values are non-negotiable (they come from the Win32 SDK headers):
      - WM_INITMENUPOPUP = 0x0117
      - WM_DWMCOLORIZATIONCOLORCHANGED = 0x0320
      - WM_DRAWITEM = 0x002B
      - WM_MEASUREITEM = 0x002C
      - WM_DPICHANGED = 0x02E0
      - MF_CHECKED = 0x0008
      - MF_UNCHECKED = 0x0000
      - MF_BYCOMMAND = 0x0000
      - MF_OWNERDRAW = 0x0100
      - MF_SEPARATOR = 0x0800
      - MF_STRING = 0x0000
      - MIM_BACKGROUND = 0x00000002
      - ODS_SELECTED = 0x0001
      - ODS_DISABLED = 0x0004
      - ODS_CHECKED = 0x0008
      - ODS_HOTLIGHT = 0x0040
      - UISF_HIDEACCEL = 0x0002

    Edit 2 — Structs: Declare `MENUINFO`, `DRAWITEMSTRUCT`, `MEASUREITEMSTRUCT` per the <interfaces> field layouts. Each MUST be `#[repr(C)]` and `pub(super)`. Field names match Win32 casing exactly (lowercase `f` prefix for booleans like `fMask`; `CtlType`/`CtlID` PascalCase per WinUSER.h). `Hbrush` may not be aliased — add `pub(super) type Hbrush = isize;` if absent.

    Edit 3 — Dwmapi extern block: Locate the existing `#[link(name = "Dwmapi")] unsafe extern "system" { ... }` block. Append exactly one declaration:
      `pub(super) fn DwmGetColorizationColor(pcrColorization: *mut Dword, pfOpaqueBlend: *mut i32) -> i32;`
      Preserve the existing `DwmSetWindowAttribute` declaration verbatim.

    Edit 4 — Kernel32 extern block: Locate the existing `#[link(name = "Kernel32")] unsafe extern "system" { ... }` block. Append (only if missing — if either already exists, skip the duplicate):
      `pub(super) fn LoadLibraryW(lpLibFileName: *const u16) -> Hinstance;`
      `pub(super) fn GetProcAddress(hModule: Hinstance, lpProcName: *const i8) -> *mut std::ffi::c_void;`
      `Hinstance` may need to be aliased: `pub(super) type Hinstance = isize;` if not present.

    Edit 5 — User32 extern block: Locate the existing `#[link(name = "User32")]` block. Append (only if missing):
      `pub(super) fn CreateMenu() -> Hmenu;`
      `pub(super) fn CreatePopupMenu() -> Hmenu;`
      `pub(super) fn SetMenu(hWnd: Hwnd, hMenu: Hmenu) -> Bool;`
      `pub(super) fn CheckMenuItem(hMenu: Hmenu, uIDCheckItem: Uint, uCheck: Uint) -> Dword;`
      `pub(super) fn SetMenuInfo(hMenu: Hmenu, lpcmi: *const MENUINFO) -> Bool;`
      `pub(super) fn GetSubMenu(hMenu: Hmenu, nPos: i32) -> Hmenu;`
      `pub(super) fn AppendMenuW(hMenu: Hmenu, uFlags: Uint, uIDNewItem: usize, lpNewItem: *const u16) -> Bool;` (skip if already present from Phase 02 — it almost certainly is)
      `Hmenu` aliased as `pub(super) type Hmenu = isize;` if absent.

    Do NOT create any new `#[link(name = ...)]` blocks; per Pattern Map "No new link blocks needed" — every DLL (Dwmapi, UxTheme, Gdi32, User32, Kernel32) is already linked.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - CI quad passes.
    - `grep -c "fn DwmGetColorizationColor" src/desktop/ffi.rs` reports 1.
    - `grep -c "fn LoadLibraryW" src/desktop/ffi.rs` reports 1.
    - `grep -c "fn GetProcAddress" src/desktop/ffi.rs` reports 1.
    - `grep -c "struct MENUINFO" src/desktop/ffi.rs` reports 1.
    - `grep -c "struct DRAWITEMSTRUCT" src/desktop/ffi.rs` reports 1.
    - `grep -c "struct MEASUREITEMSTRUCT" src/desktop/ffi.rs` reports 1.
    - `grep -c "WM_DWMCOLORIZATIONCOLORCHANGED" src/desktop/ffi.rs` reports at least 1.
    - `grep -c "WM_INITMENUPOPUP" src/desktop/ffi.rs` reports at least 1.
    - `grep -c "MF_CHECKED" src/desktop/ffi.rs` reports at least 1.
    - `grep -c "MIM_BACKGROUND" src/desktop/ffi.rs` reports at least 1.
    - `grep -c "#\\[link(name" src/desktop/ffi.rs` produces the same count it had before this task (no new link blocks created).
  </acceptance_criteria>
  <done>FFI surface ready for Task 2 (theme.rs) and downstream plans (02.1-05 menu construction, 02.1-06 owner-draw).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite theme.rs palette to D-05, add accent caching, WCAG luminance, uxtheme ordinal resolver, apply_dark_mode_to_window helper; add 3 unit tests; add 2 fields to DesktopState</name>
  <files>src/desktop/theme.rs, src/desktop/state.rs</files>
  <read_first>
    - src/desktop/theme.rs (READ THE WHOLE FILE — every palette_* fn must be located; the dual-DWMWA call in set_window_dark_mode is load-bearing and must NOT change)
    - src/desktop/state.rs (READ the DesktopState struct definition AND DesktopState::new initializer)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-RESEARCH.md Pattern 1 (uxtheme resolver code skeleton) AND Pattern 7 (luminance + WM_DWMCOLORIZATIONCOLORCHANGED)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-PATTERNS.md "Pitfall 9 — REWRITE palette constants" section (the exact RGB values per UI-SPEC)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-UI-SPEC.md "Color" section (60/30/10 split table)
    - .planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-VALIDATION.md (the three tests this task creates)
  </read_first>
  <behavior>
    - Test 1 `accent_luminance`: Compute `selected_text_for_accent` for these exact inputs (the boundary cases from VALIDATION.md):
      - `(255, 255, 255)` (pure white, lum = 1.0): expect `rgb(0,0,0)` (black text).
      - `(0, 0, 0)` (pure black, lum = 0.0): expect `rgb(255,255,255)` (white text).
      - `(255, 255, 0)` (pale yellow, lum ≈ 0.9278): expect `rgb(0,0,0)`.
      - `(0, 120, 215)` (typical Win11 default blue accent, lum ≈ 0.196): expect `rgb(255,255,255)`.
      - `(180, 200, 220)` (pale grey-blue, lum ≈ 0.5919): expect `rgb(255,255,255)` (lum just under 0.6 threshold).
      - `(190, 210, 220)` (slightly lighter pale grey-blue, lum ≈ 0.6437): expect `rgb(0,0,0)` (lum just over 0.6 threshold).
    - Test 2 `selected_text_threshold`: Verify the threshold edge condition — find an RGB triple whose luminance computes to exactly > 0.6 and one whose luminance is exactly < 0.6, assert the transition flips at the boundary. (Simpler alternative: pick two close shades around the threshold and confirm one returns white, the other black. This is functionally the same test as Test 1's last two cases but isolated; both tests stay since VALIDATION.md lists them separately.)
    - Test 3 `palette_matches_spec`: Construct a `DesktopState` with `dark_mode = true` (use a test helper or unsafe-zeroed state — match the style of any existing theme.rs test if present; otherwise use `DesktopState::new(...)` if it accepts a dark flag, or set the field directly via a `pub(super)` constructor). Assert:
      - `palette_bg(&state) == rgb(30, 30, 30)`
      - `palette_panel(&state) == rgb(45, 45, 45)`
      - `palette_text(&state) == rgb(204, 204, 204)`
      - `palette_line(&state) == rgb(58, 58, 58)`
      - `palette_disabled(&state) == rgb(122, 122, 122)`
      Then flip `state.dark_mode = false` and assert:
      - `palette_line(&state) == rgb(229, 229, 229)` (explicit override; do NOT delegate to GetSysColor in light mode for the hairline per UI-SPEC table)
      - Other light-mode palette values delegate to `GetSysColor` per existing behavior (no test assertion needed).
      Note: if `rgb(...)` is private to theme.rs, test inside `mod tests` can call it directly via `super::rgb(...)`. If `DesktopState` is hard to construct in a unit test, add a `pub(super) fn for_test(dark: bool) -> DesktopState` constructor to state.rs OR refactor palette_* to take `(dark_mode: bool)` instead of `(&DesktopState)` — choose whichever is least invasive. The test must compile and assert the literal RGB triples.
  </behavior>
  <action>
    Three edits — theme.rs palette rewrite (Edit 1), theme.rs new helpers + tests (Edit 2), state.rs field additions (Edit 3).

    Edit 1 (src/desktop/theme.rs) — Rewrite palette constants per Pitfall 9 + UI-SPEC. For every `palette_*(state: &DesktopState) -> Dword` fn that has a `state.dark_mode` branch, REWRITE the dark branch return value to the EXACT RGB triple from UI-SPEC:
      - palette_bg dark → `rgb(30, 30, 30)` (was `rgb(12, 13, 15)`)
      - palette_panel dark → `rgb(45, 45, 45)`
      - palette_text dark → `rgb(204, 204, 204)`
      - palette_line dark → `rgb(58, 58, 58)`
      - palette_disabled dark → `rgb(122, 122, 122)` (add fn if absent — same shape as the others)
      - palette_line light → `rgb(229, 229, 229)` (REWRITE — current value may be `rgb(196,202,210)` or similar; the UI-SPEC explicit override is non-negotiable for the hairline)
    Leave all other light-branch palette values alone unless UI-SPEC explicitly mandates an override (it does NOT for bg/panel/text — those continue to delegate to `GetSysColor(COLOR_WINDOW)`, `GetSysColor(COLOR_3DFACE)`, `GetSysColor(COLOR_WINDOWTEXT)` per UI-SPEC's "Light mode (system-color delegation)" table). Add new palette_* fns for any role missing today (UI-SPEC declares `palette_disabled` and uses `palette_panel` for menu/status — most should already exist post Phase 02).

    DO NOT modify `set_window_dark_mode` — its dual-DWMWA call (attribute values 20 then 19) is preserved verbatim per Pitfall 3.

    DO NOT modify the `DARK_BRUSH` / `LIGHT_BRUSH` color triples even if they read differently from `palette_bg` — those brushes are referenced by WM_CTLCOLOR* paint paths and Phase 02 has them tuned. Phase 02.1 wires the new palette into custom paint paths via the palette_* fns; the brushes are a separate concern.

    Edit 2 (src/desktop/theme.rs) — Add new public surface per <interfaces>:
      - `PreferredAppMode` constants (4 consts).
      - `UxthemeOrdinals` struct + `OnceLock<UxthemeOrdinals>` + `uxtheme_ordinals()` fn implementing RESEARCH Pattern 1 verbatim: `LoadLibraryW(wide("uxtheme.dll").as_ptr())` → on null return UxthemeOrdinals with all-None → `GetProcAddress(module, 135 as usize as *const i8)` / 133 / 136 → null-check each result → `std::mem::transmute` the non-null ones into the typed fn pointers. The `wide` helper is `crate::io::wide(&str) -> Vec<u16>` per existing usage. Add a one-line `eprintln!` ONLY in `#[cfg(debug_assertions)]` if any ordinal returns null, naming which ordinal was missing (helps diagnose Pitfall 2 on Win10 1809).
      - `selected_text_for_accent(r: u8, g: u8, b: u8) -> Dword` implementing the WCAG formula: `let rf = r as f32 / 255.0; let gf = g as f32 / 255.0; let bf = b as f32 / 255.0; let lum = 0.2126 * rf + 0.7152 * gf + 0.0722 * bf; if lum > 0.6 { rgb(0, 0, 0) } else { rgb(255, 255, 255) }`. Threshold is 0.6 EXACTLY (UI-SPEC + Open Question 6 in RESEARCH lock this).
      - `refresh_accent(state: &mut DesktopState)` implementing RESEARCH Pattern 7: declare `let mut color: u32 = 0; let mut opaque_blend: i32 = 0;` then unsafe-call `DwmGetColorizationColor(&mut color as *mut u32, &mut opaque_blend as *mut i32);`. ARGB to BGR conversion: `let stripped = color & 0x00FFFFFF; let r = ((stripped >> 16) & 0xFF) as u8; let g = ((stripped >> 8) & 0xFF) as u8; let b = (stripped & 0xFF) as u8;`. Assign `state.accent_color = rgb(r, g, b);` and `state.selected_text_color = selected_text_for_accent(r, g, b);`. Handle the DwmGetColorizationColor return value: if non-zero (error), do NOT mutate `state.accent_color` — leave whatever was previously cached (so accent change failures are no-ops rather than zeroing the color).
      - `unsafe fn apply_dark_mode_to_window(hwnd: Hwnd, enabled: bool)`: 3 calls in this order:
        1. `set_window_dark_mode(hwnd, enabled);` (existing dual-DWMWA helper — DO NOT inline; reuse)
        2. If `enabled` and `uxtheme_ordinals().allow_dark_mode_for_window` is `Some(f)`, call `f(hwnd, 1)` (BOOL 1 = TRUE). If `enabled == false`, call with 0.
        3. `SetWindowTheme(hwnd, wide("DarkMode_Explorer" if enabled else "Explorer").as_ptr(), std::ptr::null());`
      This helper is called per-HWND by Plan 02.1-05 (in mod.rs::create_controls after each new CreateWindowExW) and Plan 02.1-07 (in cli.rs bootstrap + mod.rs::run for the main HWND).

    Edit 3 (src/desktop/state.rs) — Add two fields to `DesktopState`:
      - `pub(super) accent_color: Dword,` — place adjacent to other `Dword` color fields or alongside `dark_mode`.
      - `pub(super) selected_text_color: Dword,` — placed adjacent to `accent_color`.
    In `DesktopState::new` (lines ~91–134), initialize both to `0` (refresh_accent will populate them at startup; 0 is a benign sentinel — text comes out invisible briefly if read before refresh, which is fine because refresh_accent is called before first paint per Plan 02.1-07).

    Add a new `#[cfg(test)] mod tests { ... }` block at the bottom of `src/desktop/theme.rs` (or extend existing if present). Implement the three tests per <behavior>. If `DesktopState` is hard to construct in a unit test (likely — it holds HWNDs), the easiest path is to refactor `palette_*` to accept `(dark_mode: bool)` instead of `(&DesktopState)`. If doing so, update all call sites in `src/desktop/paint.rs` and `src/desktop/theme.rs` (any internal caller) — this is a mechanical search-and-replace; the WM_CTLCOLOR* fast-path already reads `DARK_MODE_ATOMIC` so it's unaffected. PREFER this refactor — it makes the tests trivial AND decouples the palette from the full state struct.

    OR, alternatively, keep palette_* taking `(&DesktopState)` and add `pub(super) fn for_test_dark_mode(dark: bool) -> DesktopState { let mut s: DesktopState = unsafe { std::mem::zeroed() }; s.dark_mode = dark; s }` to state.rs gated on `#[cfg(test)]`. Either path is acceptable; refactor is preferred.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --lib desktop::theme:: &amp;&amp; cargo test --lib desktop::</automated>
  </verify>
  <acceptance_criteria>
    - CI quad passes.
    - `cargo test --lib desktop::theme::tests::accent_luminance` PASSES.
    - `cargo test --lib desktop::theme::tests::selected_text_threshold` PASSES.
    - `cargo test --lib desktop::theme::tests::palette_matches_spec` PASSES.
    - `cargo test --lib desktop::` PASSES (no regression — pre-existing desktop tests still green).
    - Source assertion: `grep -c "rgb(12, 13, 15)" src/desktop/theme.rs` reports 0 (the brown-tinted dark bg is gone — Pitfall 9 closed).
    - Source assertion: `grep -c "rgb(30, 30, 30)" src/desktop/theme.rs` reports at least 1 (the new #1e1e1e dark bg).
    - Source assertion: `grep -c "fn selected_text_for_accent" src/desktop/theme.rs` reports 1.
    - Source assertion: `grep -c "fn uxtheme_ordinals" src/desktop/theme.rs` reports 1.
    - Source assertion: `grep -c "fn refresh_accent" src/desktop/theme.rs` reports 1.
    - Source assertion: `grep -c "fn apply_dark_mode_to_window" src/desktop/theme.rs` reports 1.
    - Source assertion: `grep -c "DwmSetWindowAttribute(hwnd, 20" src/desktop/theme.rs` reports 1 (preserved per Pitfall 3).
    - Source assertion: `grep -c "DwmSetWindowAttribute(hwnd, 19" src/desktop/theme.rs` reports 1 (preserved per Pitfall 3).
    - Source assertion: `grep -c "accent_color: Dword" src/desktop/state.rs` reports 1.
    - Source assertion: `grep -c "selected_text_color: Dword" src/desktop/state.rs` reports 1.
  </acceptance_criteria>
  <done>Theme primitives complete: palette matches UI-SPEC, accent + luminance helpers exist with tests, uxtheme ordinals resolvable with fallback, apply_dark_mode_to_window is the single per-HWND entry point Plans 02.1-05/07 will use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → uxtheme.dll | Runtime DLL load + ordinal resolution by integer; ordinals are not in any public header |
| process → dwmapi.dll | DwmGetColorizationColor returns the system accent; benign read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-03-01 | DoS | GetProcAddress(uxtheme.dll, ordinal 135) on Win10 1809 returns a function with a different signature | mitigate | Per Pitfall 2 — defensive null-check + Option<fn> wrapper. If a 1809 user reports a crash, gate the call on `RtlGetVersion >= build 18362` in a follow-up. Phase 02.1 ships the null-check only. |
| T-02.1-03-02 | DoS | Palette rewrite breaks existing paint code by returning wrong types or panicking | mitigate | Unit test `palette_matches_spec` is the regression guard. CI quad enforces. |
| T-02.1-03-03 | T | DwmGetColorizationColor error sets accent_color to 0 → invisible selection text | mitigate | refresh_accent leaves state.accent_color untouched on non-zero return (error path documented in action). First refresh_accent call at startup is unconditional; if it fails, state.accent_color stays at the `0` initializer — selection-fill paints black, which is visually degraded but not a crash. |
| n/a | Spoofing | n/a | not applicable | No identity surface |
| n/a | Repudiation | n/a | not applicable | No audit surface |
| n/a | Information Disclosure | n/a | not applicable | Accent color is a public OS setting |
| n/a | Elevation of Privilege | n/a | not applicable | No privilege boundary crossed |
</threat_model>

<verification>
- `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo build --release && cargo test`.
- The three new tests pass.
- Manual sanity: `cargo run --release` — application launches with light mode; subsequent plan (02.1-05) wires the menu and tab strip that will exercise the palette.
</verification>

<success_criteria>
- D-05 palette mathematically matches UI-SPEC RGB values (verified by `palette_matches_spec`).
- Accent caching contract (refresh_accent + selected_text_for_accent) is unit-tested at the boundary.
- Uxtheme ordinals are resolvable at runtime with a null-safe fallback for older Windows builds.
- apply_dark_mode_to_window is the single canonical per-HWND dark application path; Plans 02.1-05 and 02.1-07 call it instead of reimplementing.
- DwmSetWindowAttribute dual-call (20 + 19) preserved per Pitfall 3.
- DesktopState carries accent_color + selected_text_color fields for paint code to read.
- POL-03 prerequisite: palette + accent primitives ready for menu/tab ALT-mnemonic overlay rendering downstream.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ui-polish-dark-mode-chrome-persistence/02.1-03-SUMMARY.md`. Record:
- Final shape of palette_* fns (taking &DesktopState or `(dark_mode: bool)` after refactor).
- Whether DesktopState gained a `for_test` constructor or not.
- The exact lines where uxtheme_ordinals + refresh_accent + apply_dark_mode_to_window were inserted (so Plans 02.1-05/07 can link).
</output>
</content>
</invoke>