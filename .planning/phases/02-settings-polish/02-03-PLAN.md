---
phase: 02-settings-polish
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/desktop/ffi.rs
  - src/desktop/mod.rs
  - src/desktop/theme.rs
  - src/desktop/state.rs
autonomous: true
requirements: [POL-01, POL-02]
tags: [path-bar, drive-picker, shautocomplete, accelerator-table, win32-controls]

must_haves:
  truths:
    - "The main window shows a path bar at the top containing a drive picker (ComboBoxEx32) and a path edit (EDIT with SHAutoComplete attached)"
    - "The drive picker is populated from `GetLogicalDrives` + `GetDriveTypeW` and excludes `DRIVE_UNKNOWN` / `DRIVE_NO_ROOT_DIR`; selecting a drive sets the path edit text to `X:\\`"
    - "Typing in the path edit shows the Explorer-style filesystem autocomplete dropdown (via `SHAutoComplete(SHACF_FILESYS_DIRS | SHACF_AUTOSUGGEST_FORCE_ON | SHACF_AUTOAPPEND_FORCE_ON)`)"
    - "Pressing Enter anywhere in the window starts a scan; Esc cancels an active scan; F5 restarts a scan; Ctrl+E fires the export command; Ctrl+F fires the focus-search command (no-op stub); Del fires the delete-selection command (no-op stub, focus-conditional on the tree list)"
    - "Drive picker logic is unit-testable via `pure_filter_drive_letters` taking a `u32` mask and a closure returning `Uint` drive type"
    - "ACCEL struct uses `#[repr(C, packed(1))]` per Pitfall #1 with `BYTE fVirt; WORD key; WORD cmd;` ordering"
    - "Dark mode toggle re-themes the drive picker and path edit via `SetWindowTheme(..., L\"DarkMode_Explorer\")` / `L\"Explorer\"`"
  artifacts:
    - path: "src/desktop/ffi.rs"
      provides: "ComboBoxEx32 + EDIT extensions + SHAutoComplete + accelerator FFI: ICC_USEREX_CLASSES, ICC_BAR_CLASSES, CBEM_INSERTITEMW, CBEIF_TEXT, CBS_DROPDOWNLIST, DRIVE_* constants, ComboBoxExItemW struct, GetLogicalDrives, GetDriveTypeW, GetVolumeInformationW, SHAutoComplete + SHACF_* flags, Accel struct (#[repr(C, packed(1))]), VK_RETURN/ESCAPE/DELETE/F5, FVIRTKEY/FCONTROL, CMD_SCAN/CANCEL_SCAN/DELETE_SEL/FOCUS_SEARCH/EXPORT/REFRESH, CreateAcceleratorTableW, TranslateAcceleratorW, DestroyAcceleratorTable, GetFocus"
      contains: "Accel"
    - path: "src/desktop/mod.rs"
      provides: "ICC_USEREX_CLASSES + ICC_BAR_CLASSES added to InitCommonControlsEx; drive picker + path edit created in create_controls; SHAutoComplete called after path edit creation; CreateAcceleratorTableW + TranslateAcceleratorW in message pump; WM_COMMAND arms for CMD_*; pure_filter_drive_letters helper + tests"
      contains: "CreateAcceleratorTableW"
    - path: "src/desktop/theme.rs"
      provides: "apply_theme extended to set DarkMode_Explorer / Explorer theme on drive_picker and path_edit"
      contains: "drive_picker"
    - path: "src/desktop/state.rs"
      provides: "DesktopState.drive_picker: Hwnd, DesktopState.accel_table: Handle, DesktopState.scanning: bool (if not already present); WM_COMMAND helpers for the six command IDs"
      contains: "drive_picker"
  key_links:
    - from: "src/desktop/mod.rs::create_controls"
      to: "src/desktop/ffi.rs::SHAutoComplete"
      via: "call immediately after CreateWindowExW returns the path-edit HWND"
      pattern: "SHAutoComplete\\(.*path_edit"
    - from: "src/desktop/mod.rs::run (message pump)"
      to: "src/desktop/ffi.rs::TranslateAcceleratorW"
      via: "called BEFORE TranslateMessage in the GetMessageW loop per Pitfall #8"
      pattern: "TranslateAcceleratorW.*TranslateMessage"
    - from: "src/desktop/mod.rs::window_proc (WM_COMMAND arms)"
      to: "existing start_scan_from_controls / stop_current_scan"
      via: "CMD_SCAN/CMD_CANCEL_SCAN/CMD_REFRESH dispatch"
      pattern: "CMD_SCAN.*start_scan_from_controls"
---

<objective>
Land the path bar (drive picker + autocomplete-equipped path edit) and the six-shortcut accelerator table. New surface lives at the top of the main window above the existing ribbon, sized per UI-SPEC tokens (32px bar height, 80px drive picker, 8px insets, all DPI-scaled). The accelerator table dispatches `CMD_SCAN` (Enter), `CMD_CANCEL_SCAN` (Esc), `CMD_DELETE_SEL` (Del — focus-conditional stub), `CMD_FOCUS_SEARCH` (Ctrl+F — stub), `CMD_EXPORT` (Ctrl+E), `CMD_REFRESH` (F5) as `WM_COMMAND` IDs handled by the existing command router. New Shlwapi.dll link added for `SHAutoComplete`.

Purpose: POL-01 + POL-02 + ROADMAP Success Criteria 3 and 4 — "User can pick a drive from a dropdown ... and type a path with folder-name autocomplete" and "User can drive the app from the keyboard."

Output: Working path bar with Explorer-native autocomplete; six keyboard shortcuts driving the existing commands; dark-mode coherence preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-settings-polish/02-CONTEXT.md
@.planning/phases/02-settings-polish/02-RESEARCH.md
@.planning/phases/02-settings-polish/02-PATTERNS.md
@.planning/phases/02-settings-polish/02-UI-SPEC.md
@.planning/phases/02-settings-polish/02-VALIDATION.md
@.planning/phases/02-settings-polish/02-02-PLAN.md
@CLAUDE.md
@src/desktop/ffi.rs
@src/desktop/mod.rs
@src/desktop/theme.rs
@src/desktop/state.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend desktop/ffi.rs with ComboBoxEx32 + SHAutoComplete + accelerator-table FFI; add new Shlwapi #[link] block</name>
  <files>src/desktop/ffi.rs</files>
  <read_first>
    - src/desktop/ffi.rs (post-Plan-02 baseline; existing const block ordering; existing `#[repr(C)]` struct style; existing `Msg` struct used in GetMessageW)
    - .planning/phases/02-settings-polish/02-RESEARCH.md §"Pattern 4 (ComboBoxEx32)", §"Pattern 5 (SHAutoComplete)", §"Pattern 6 (Accelerators)", §"Pitfall 1 (ACCEL packing)"
    - .planning/phases/02-settings-polish/02-PATTERNS.md §"`src/desktop/ffi.rs` (MODIFIED)" (constant declarations + struct layout pattern)
    - .planning/phases/02-settings-polish/02-UI-SPEC.md §"Keyboard shortcut focus / conflict model"
  </read_first>
  <behavior>
    - Test (`accel_struct_is_six_bytes_packed`): `size_of::<Accel>() == 6` (BYTE+WORD+WORD packed; the packed(1) ensures no padding to 8). Note: the natural size of `BYTE+WORD+WORD` with `#pragma pack(1)` is 5 bytes; Rust's `#[repr(C, packed(1))]` matches but `size_of` may report 5 — accept either 5 or 6 (Win32 docs document ACCEL as 6 due to compiler-dependent trailing padding; `CreateAcceleratorTableW` reads only the first 5 bytes per element). The assert pins whichever value the chosen Rust packing produces and documents the choice.
    - Test (`accel_field_values_match_pitfall_1_recipe`): construct `Accel { fVirt: FVIRTKEY | FCONTROL, key: 'F' as u16, cmd: CMD_FOCUS_SEARCH }`, read fields back via `unsafe { core::ptr::addr_of!(...).read_unaligned() }`, assert each matches.
    - Test (`command_ids_in_expected_range`): `CMD_SCAN..=CMD_REFRESH` all fall in 0xA001..=0xA006 and are pairwise distinct.
  </behavior>
  <action>
    Extend `src/desktop/ffi.rs` (post-Plan-02 baseline). Do not remove or reorder existing declarations.

    **Constants block additions** (UPPER_SNAKE_CASE matching Win32):
    - `ICC_USEREX_CLASSES: Dword = 0x0000_0200`
    - `ICC_BAR_CLASSES: Dword = 0x0000_0004`
    - `CBEM_INSERTITEMW: Uint = 0x040B`
    - `CBEIF_TEXT: Uint = 0x0000_0001`
    - `CBS_DROPDOWNLIST: Dword = 0x0003`
    - `CB_SETCURSEL: Uint = 0x014E`
    - `CB_GETCURSEL: Uint = 0x0147`
    - `DRIVE_UNKNOWN: Uint = 0`, `DRIVE_NO_ROOT_DIR: Uint = 1`, `DRIVE_REMOVABLE: Uint = 2`, `DRIVE_FIXED: Uint = 3`, `DRIVE_REMOTE: Uint = 4`, `DRIVE_CDROM: Uint = 5`, `DRIVE_RAMDISK: Uint = 6`
    - `SHACF_FILESYS_DIRS: Dword = 0x0000_0020`
    - `SHACF_AUTOSUGGEST_FORCE_ON: Dword = 0x1000_0000`
    - `SHACF_AUTOAPPEND_FORCE_ON: Dword = 0x4000_0000`
    - `FVIRTKEY: u8 = 0x01`, `FCONTROL: u8 = 0x08`, `FSHIFT: u8 = 0x04`, `FALT: u8 = 0x10`, `FNOINVERT: u8 = 0x02`
    - `VK_RETURN: u16 = 0x0D`, `VK_ESCAPE: u16 = 0x1B`, `VK_DELETE: u16 = 0x2E`, `VK_F5: u16 = 0x74`
    - Command IDs (use unique values not colliding with existing `ID_*` constants in the file — Grep first for `0xA001`..`0xA006` to confirm vacant): `CMD_SCAN: u16 = 0xA001`, `CMD_CANCEL_SCAN: u16 = 0xA002`, `CMD_DELETE_SEL: u16 = 0xA003`, `CMD_FOCUS_SEARCH: u16 = 0xA004`, `CMD_EXPORT: u16 = 0xA005`, `CMD_REFRESH: u16 = 0xA006`.
    - `WM_EXITSIZEMOVE: Uint = 0x0232`, `WM_LBUTTONUP: Uint = 0x0202` (used by Plan 04 but landed here to keep all new WM_ constants in one location).

    **Struct additions**:
    - `#[repr(C)] pub(super) struct ComboBoxExItemW { mask: Uint, iItem: isize, pszText: *mut u16, cchTextMax: i32, iImage: i32, iSelectedImage: i32, iOverlay: i32, iIndent: i32, lParam: Lparam }` — fields `pub(super)` per existing struct convention in the file.
    - **`#[repr(C, packed(1))]` for ACCEL** per Pitfall #1: `pub(super) struct Accel { pub(super) fVirt: u8, pub(super) key: u16, pub(super) cmd: u16 }`. Add an inline comment documenting that this is the ONLY packed struct in the file and that field reads must use `core::ptr::addr_of!(...).read_unaligned()` to satisfy clippy's `unaligned-references` lint. Add `#[allow(non_snake_case)]` only if the module-level allow does not already cover field names.

    **FFI declarations**:
    - In existing Kernel32 block: `GetLogicalDrives() -> Dword`, `GetDriveTypeW(lpRootPathName: *const u16) -> Uint`, `GetVolumeInformationW(lpRootPathName: *const u16, lpVolumeNameBuffer: *mut u16, nVolumeNameSize: Dword, lpVolumeSerialNumber: *mut Dword, lpMaximumComponentLength: *mut Dword, lpFileSystemFlags: *mut Dword, lpFileSystemNameBuffer: *mut u16, nFileSystemNameSize: Dword) -> Bool`.
    - In existing User32 block: `CreateAcceleratorTableW(paccel: *const Accel, cAccel: i32) -> Handle`, `TranslateAcceleratorW(hWnd: Hwnd, hAccTable: Handle, lpMsg: *mut Msg) -> i32`, `DestroyAcceleratorTable(hAccel: Handle) -> Bool`, `GetFocus() -> Hwnd`, `EnableWindow(hWnd: Hwnd, bEnable: Bool) -> Bool` (skip if already declared — `Grep` first).
    - NEW link block (CLAUDE.md Constraint #3 — the ONE new DLL this phase): `#[link(name = "Shlwapi")] unsafe extern "system" { pub(super) fn SHAutoComplete(hwndEdit: Hwnd, dwFlags: Dword) -> i32; }`.

    Extend the `#[cfg(test)] mod tests` block (created in Plan 02) with the three new tests above. Use `core::mem::size_of` and `core::ptr::addr_of!` plus `.read_unaligned()` for the `Accel` field reads (Rust forbids direct references to packed-struct fields).

    No fenced code blocks in this action prose.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo build &amp;&amp; cargo test --lib desktop::ffi::tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` green; `cargo build --release` green.
    - `Grep` of `src/desktop/ffi.rs` for `#\[link\(name = "Shlwapi"\)\]` returns exactly one match (new DLL link confirmed).
    - `Grep` for `#\[repr\(C, packed\(1\)\)\]` returns exactly one match (ACCEL).
    - `Grep` for `fn SHAutoComplete` returns one match.
    - `Grep` for `fn CreateAcceleratorTableW`, `fn TranslateAcceleratorW`, `fn DestroyAcceleratorTable`, `fn GetLogicalDrives`, `fn GetDriveTypeW`, `fn GetVolumeInformationW`, `fn GetFocus` returns ≥1 match each.
    - `Grep` for `CMD_SCAN.*0xA001`, `CMD_CANCEL_SCAN.*0xA002`, `CMD_DELETE_SEL.*0xA003`, `CMD_FOCUS_SEARCH.*0xA004`, `CMD_EXPORT.*0xA005`, `CMD_REFRESH.*0xA006` each returns one match.
    - `Grep` for `read_unaligned` returns ≥1 match inside the new test (packed struct read discipline).
    - `cargo test --lib desktop::ffi::tests` passes the new tests (≥3 added).
    - `cargo clippy --all-targets -- -D warnings` clean (no `unaligned-references` warning; addr_of! pattern silences it).
  </acceptance_criteria>
  <done>FFI surface for path bar + accelerators + Shlwapi link is declared. Packed ACCEL passes layout test. New constants are in place for downstream tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create path bar controls, drive enumeration, SHAutoComplete wire-up; extend message pump with accelerator table; add WM_COMMAND arms; extend theme.rs</name>
  <files>src/desktop/mod.rs, src/desktop/theme.rs, src/desktop/state.rs</files>
  <read_first>
    - src/desktop/mod.rs (`create_controls` around lines 418-568; `create_child` helper around 570-595; the existing `InitCommonControlsEx` call around lines 80-84; existing message pump in `run` around lines 142-146; existing `window_proc` `WM_COMMAND` match around lines 222-366; existing `start_scan_from_controls` and `stop_current_scan` function names — `Grep` for actual names)
    - src/desktop/state.rs (`DesktopState` struct definition; identify field for path_edit, scan state, font, etc.)
    - src/desktop/theme.rs (`apply_theme` function — list of HWNDs it currently calls `SetWindowTheme` on)
    - .planning/phases/02-settings-polish/02-UI-SPEC.md §"Component Inventory & Layout Tokens / Path bar", §"Keyboard shortcut focus / conflict model", §"Copywriting Contract / Drive picker entries"
    - .planning/phases/02-settings-polish/02-RESEARCH.md §"Pattern 4", §"Pattern 5", §"Pattern 6", §"Pitfall 8 (edit eats Enter)"
    - .planning/phases/02-settings-polish/02-PATTERNS.md §"`src/desktop/mod.rs`", §"`src/desktop/state.rs`"
  </read_first>
  <behavior>
    - Test (`pure_filter_drive_letters`, in `src/desktop/mod.rs` inline tests, `#[cfg(windows)]`): given `mask = 0b0000_0000_0000_1100` (bits for C: and D:) and a closure `|letter| match letter { 'C' => DRIVE_FIXED, 'D' => DRIVE_CDROM, _ => DRIVE_UNKNOWN }`, the helper returns `vec!['C', 'D']` (BOTH included — CDROM stays when its drive type is valid; we only exclude DRIVE_UNKNOWN/DRIVE_NO_ROOT_DIR per UI-SPEC).
    - Test (`pure_filter_drive_letters_excludes_unknown`): given `mask = 0b0000_0000_0000_0100` (bit for C:) and a closure returning `DRIVE_UNKNOWN`, the helper returns an empty `Vec<char>`.
    - Test (`pure_filter_drive_letters_excludes_no_root_dir`): given a closure returning `DRIVE_NO_ROOT_DIR`, the helper returns empty.
    - Test (`format_drive_entry_uses_label`): `format_drive_entry('C', DRIVE_FIXED, Some("Windows"))` returns `"C: Windows"`.
    - Test (`format_drive_entry_falls_back_to_type_name`): `format_drive_entry('C', DRIVE_FIXED, None)` returns `"C: Local Disk"`; for `DRIVE_REMOVABLE` → `"D: Removable"`; for `DRIVE_CDROM` → `"E: CD/DVD"`; for `DRIVE_REMOTE` → `"Z: Network"`; for `DRIVE_RAMDISK` → `"R: RAM Disk"`.
  </behavior>
  <action>
    **`src/desktop/state.rs`**: extend `DesktopState` (use Grep to confirm current shape; do not duplicate existing fields):
    - Add `pub(super) drive_picker: Hwnd` (initialized to 0; populated in `create_controls`).
    - Add `pub(super) accel_table: Handle` (initialized to 0; populated in `run`; destroyed on `WM_DESTROY`).
    - If `scanning: bool` does not already exist in `DesktopState`, add it (default false; toggled by existing scan-start/scan-done paths).

    **`src/desktop/mod.rs`** — five sub-changes:

    1) **InitCommonControlsEx**: change the existing `dwICC` assignment to OR in `ICC_USEREX_CLASSES | ICC_BAR_CLASSES` alongside the existing `ICC_LISTVIEW_CLASSES`. Without this, `CreateWindowExW` for the new classes silently returns 0 (RESEARCH Anti-Patterns).

    2) **create_controls — drive picker + path edit**:
    - Identify the current path-edit creation in `create_controls` (it already exists per Phase 1 baseline; Grep for `EDIT` or `path_edit` HWND assignment).
    - REPLACE the path-edit creation with a drive picker + path edit pair laid out per UI-SPEC tokens (xs=4, sm=8, xl=32 logical px, all DPI-scaled via the new `dpi_scale` helper from Plan 01).
    - Create drive picker: `state.drive_picker = CreateWindowExW(0, wide("ComboBoxEx32").as_ptr(), null(), WS_CHILD | WS_VISIBLE | CBS_DROPDOWNLIST as Dword, /*x,y,w,h placeholder*/ 0, 0, 0, 0, hwnd, ID_DRIVE_PICKER as Hmenu, h_instance, null_mut())`. Choose a fresh `ID_DRIVE_PICKER` constant in the existing `ID_*` range (Grep first to find unused).
    - Populate the picker: call a new `populate_drive_picker(state)` helper that uses `GetLogicalDrives()` + the new `pure_filter_drive_letters` helper + `format_drive_entry` for each surviving letter. For each entry, build a `ComboBoxExItemW { mask: CBEIF_TEXT, iItem: -1, pszText: <wide string ptr>, ... }` and `SendMessageW(state.drive_picker, CBEM_INSERTITEMW, 0, &item as *const _ as Lparam)`. Use `GetVolumeInformationW(root_w.as_ptr(), name_buf.as_mut_ptr(), 256, null_mut(), null_mut(), null_mut(), null_mut(), 0)` to fetch the label (treat empty label as `None` for fallback).
    - Initial selection: read `state.last_path` (loaded from settings — this plan's wiring depends on Plan 04 finalizing the load; for THIS task, default to C: if present, else first enumerated drive). Use `CB_SETCURSEL` to set initial index.
    - Create path edit: `state.path_edit = CreateWindowExW(WS_EX_CLIENTEDGE_or_0, wide("EDIT").as_ptr(), wide(&state.last_path).as_ptr(), WS_CHILD | WS_VISIBLE | WS_TABSTOP | ES_AUTOHSCROLL, ...)`. Sizes are placeholders; `resize_controls` (Plan 04) does final layout.
    - IMMEDIATELY after `CreateWindowExW` returns non-zero `path_edit`, call `SHAutoComplete(state.path_edit, SHACF_FILESYS_DIRS | SHACF_AUTOSUGGEST_FORCE_ON | SHACF_AUTOAPPEND_FORCE_ON)` per Pitfall #3 (must run after HWND is valid). Document the call site inline.
    - Add a `WM_COMMAND` arm for `HIWORD(wparam) == CBN_SELCHANGE && LOWORD(wparam) == ID_DRIVE_PICKER`: on drive selection, fetch the selected letter via the picker text, call `SetWindowTextW(state.path_edit, wide(&format!("{}:\\", letter)).as_ptr())`, then SHAutoComplete is already wired so the dropdown will suggest first-level folders. Do NOT auto-start a scan (per UI-SPEC).

    3) **Message pump** (in `run`, around lines 142-146): change the existing loop from `while GetMessageW(...) > 0 { TranslateMessage(...); DispatchMessageW(...); }` to `while GetMessageW(...) > 0 { if TranslateAcceleratorW(hwnd, state.accel_table, &mut message) == 0 { TranslateMessage(&message); DispatchMessageW(&message); } }`. This is the EXACT structure from RESEARCH Pattern 6 + Pitfall #8 — accelerator wins over edit-control default key handling.

    Before the message loop, build the accelerator table:
    - Construct the 6-element array using `core::mem::MaybeUninit::<Accel>` slots filled via `core::ptr::write_unaligned` to avoid Rust's `unaligned-references` lint:
      - Entry 0: `fVirt: FVIRTKEY, key: VK_RETURN, cmd: CMD_SCAN`
      - Entry 1: `fVirt: FVIRTKEY, key: VK_ESCAPE, cmd: CMD_CANCEL_SCAN`
      - Entry 2: `fVirt: FVIRTKEY, key: VK_DELETE, cmd: CMD_DELETE_SEL`
      - Entry 3: `fVirt: FVIRTKEY | FCONTROL, key: 'F' as u16, cmd: CMD_FOCUS_SEARCH`
      - Entry 4: `fVirt: FVIRTKEY | FCONTROL, key: 'E' as u16, cmd: CMD_EXPORT`
      - Entry 5: `fVirt: FVIRTKEY, key: VK_F5, cmd: CMD_REFRESH`
    - `state.accel_table = CreateAcceleratorTableW(accels.as_ptr(), 6)`.
    - After the message loop exits, `DestroyAcceleratorTable(state.accel_table)`.

    4) **WM_COMMAND arms** (extend the existing `match` block; preserve existing arms):
    - `CMD_SCAN as isize` (note: WM_COMMAND wparam LOWORD is the command ID; match on `(LOWORD(wparam) as isize, HIWORD(wparam))` per existing pattern in mod.rs): on CMD_SCAN, if `with_state_mut(|s| s.scanning).flatten().unwrap_or(false) == false`, call the existing `start_scan_from_controls(hwnd)` (verify name via Grep). If a scan is in flight, no-op (per UI-SPEC).
    - `CMD_CANCEL_SCAN`: if scanning, call the existing `stop_current_scan()` (verify name via Grep — it lives near mod.rs line 915 per PATTERNS.md). Otherwise no-op.
    - `CMD_REFRESH`: if scanning, set the existing cancel atomic and start a new scan after; otherwise just `start_scan_from_controls(hwnd)`.
    - `CMD_EXPORT`: wire to the existing export entry (find via Grep — likely an `ID_EXPORT_*` button command in the current `WM_COMMAND` arms; route CMD_EXPORT to the same handler). If no export entry exists yet, no-op stub with a debug-only `eprintln!("CMD_EXPORT fired");` and a TODO comment.
    - `CMD_FOCUS_SEARCH`: no-op stub per UI-SPEC; debug-only `eprintln!("CMD_FOCUS_SEARCH fired (stub — Phase 3 wires)");`.
    - `CMD_DELETE_SEL`: focus-conditional per UI-SPEC. `let list_hwnd = with_state_mut(|s| s.list).flatten().unwrap_or(0); if unsafe { GetFocus() } == list_hwnd { /* debug-only eprintln (stub) */ }`. Otherwise let the keystroke fall through (the accelerator handler returns having done nothing; the edit control gets its default Del behavior).

    5) **Pure-Rust testable helpers** (in `src/desktop/mod.rs`, gated `#[cfg(windows)]`):
    - `fn pure_filter_drive_letters(mask: u32, drive_type: impl Fn(char) -> Uint) -> Vec<char>`: iterate letters 'A'..='Z', for each letter where `(mask >> (letter - 'A')) & 1 == 1`, call `drive_type(letter)`. Include if result NOT in `{DRIVE_UNKNOWN, DRIVE_NO_ROOT_DIR}`. Return collected vec.
    - `fn format_drive_entry(letter: char, dt: Uint, label: Option<&str>) -> String`: per UI-SPEC §"Copywriting Contract / Drive picker entries". If `label` is `Some(non-empty)`, return `"{letter}: {label}"`. Otherwise return `"{letter}: {fallback_name}"` where fallback is: `DRIVE_FIXED -> "Local Disk"`, `DRIVE_REMOVABLE -> "Removable"`, `DRIVE_CDROM -> "CD/DVD"`, `DRIVE_REMOTE -> "Network"`, `DRIVE_RAMDISK -> "RAM Disk"`, anything else -> "Disk".
    - Add `#[cfg(test)] mod tests` (or extend existing) with the five behavior tests above. Tests are pure-Rust; no Win32 calls.

    **`src/desktop/theme.rs`**: extend `apply_theme(state: &DesktopState)` to call `SetWindowTheme` on `state.drive_picker` and `state.path_edit` (path_edit may already be themed pre-Phase-2; preserve existing behavior). In dark mode call with `L"DarkMode_Explorer"` (use `wide("DarkMode_Explorer")`); in light mode call with `L"Explorer"`. Match the existing theme-application pattern in the file (the list of HWNDs receiving theme calls extends, not replaces, the existing list).

    No fenced code blocks in this action prose.
  </action>
  <verify>
    <automated>cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo build --release &amp;&amp; cargo test --lib desktop::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` green.
    - `Grep` of `src/desktop/mod.rs` for `pure_filter_drive_letters` returns ≥2 matches (definition + test).
    - `Grep` of `src/desktop/mod.rs` for `format_drive_entry` returns ≥2 matches (definition + tests).
    - `Grep` for `SHAutoComplete\(` returns exactly one call site, located AFTER a `state.path_edit = CreateWindowExW(` line (verify by reading the file).
    - `Grep` for `TranslateAcceleratorW\(` returns exactly one match in the message pump, BEFORE `TranslateMessage` (verify ordering).
    - `Grep` for `CreateAcceleratorTableW\(` returns exactly one match.
    - `Grep` for `DestroyAcceleratorTable\(` returns ≥1 match (cleanup after loop).
    - `Grep` for `CMD_SCAN|CMD_CANCEL_SCAN|CMD_REFRESH|CMD_EXPORT|CMD_FOCUS_SEARCH|CMD_DELETE_SEL` in `src/desktop/mod.rs` returns ≥6 distinct command ID references in WM_COMMAND arms.
    - `Grep` of `src/desktop/theme.rs` for `drive_picker` returns ≥1 match.
    - `Grep` for `ICC_USEREX_CLASSES|ICC_BAR_CLASSES` in `src/desktop/mod.rs` returns ≥1 match each in the `InitCommonControlsEx` call.
    - `cargo test --lib desktop::` includes the five new `pure_filter_drive_letters` / `format_drive_entry` tests, all green.
    - `cargo clippy --all-targets -- -D warnings` clean.
    - `Cargo.toml [dependencies]` empty.
  </acceptance_criteria>
  <done>App launches showing a drive-picker dropdown to the left of the path edit; typing in the path edit shows the Explorer autocomplete dropdown; Enter/Esc/F5/Ctrl+E/Ctrl+F/Del are wired through the accelerator table to the existing command router (with stubs where Phase 5/3 will fill in). Dark-mode toggle re-themes the new controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → path edit → SHAutoComplete | OS-controlled; SHAutoComplete handles enumeration in a separate worker thread under the shell's TOCTOU model. |
| keyboard input → accelerator → WM_COMMAND | User-driven; same trust level as direct button clicks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | ACCEL struct layout mismatch causing wrong-shortcut dispatch | mitigate | `#[repr(C, packed(1))]` + `read_unaligned` per Pitfall #1; layout test asserts field offsets. |
| T-02-15 | Denial of Service | Edit control eats Enter/Esc → user cannot scan/cancel | mitigate | `TranslateAcceleratorW` before `TranslateMessage` per Pitfall #8; accelerator dispatches WM_COMMAND to parent, bypassing edit. |
| T-02-16 | Information Disclosure | Drive picker enumerates network drive paths | accept | Listing mapped network drives is the requested feature per UI-SPEC; the user explicitly mapped them. |
| T-02-17 | Spoofing | Malicious path typed into the edit triggers a scan of a sensitive directory | accept | Personal-use desktop app; user has full filesystem read access via their own OS account. Out of scope. |
</threat_model>

<verification>
- `cargo fmt --check` clean.
- `cargo clippy --all-targets -- -D warnings` clean.
- `cargo build --release` green.
- `cargo test --all-targets` green (including new pure_filter_drive_letters / format_drive_entry tests).
- `Grep` of `Cargo.toml` confirms `[dependencies]` empty; ONE new DLL link (`Shlwapi`) per CLAUDE.md Constraint #3.
- No new `unsafe` blocks outside FFI call sites in `desktop/mod.rs` `create_controls` and `run`.
</verification>

<success_criteria>
- A user launching the app sees the drive-picker + path-edit pair at the top of the window.
- Clicking the drive picker lists every fixed/removable/network drive in `<letter>: <label>` form.
- Selecting a drive sets the edit to `X:\`.
- Typing in the edit shows the Explorer autocomplete dropdown.
- Enter starts a scan; Esc cancels; F5 restarts; Ctrl+E exports; Ctrl+F and Del fire their stubs.
- Dark-mode toggle propagates to the new controls.
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-polish/02-03-SUMMARY.md` covering: new DLL link confirmation, accelerator table contents, drive picker fallback labels, ID_* constants assigned, and any deviations from RESEARCH Patterns 4/5/6 or UI-SPEC.
</output>
