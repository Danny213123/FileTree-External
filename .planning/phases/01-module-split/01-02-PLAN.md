---
phase: 01-module-split
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/main.rs
  - src/desktop/mod.rs
  - src/desktop/state.rs
  - src/desktop/ffi.rs
  - src/desktop/theme.rs
  - src/desktop/paint.rs
  - src/desktop/shell.rs
  - src/desktop/tabs.rs
  - src/desktop/treemap.rs
autonomous: true
requirements: [REFAC-01]
tags: [refactor, modules, rust, win32, desktop]

must_haves:
  truths:
    - "Each of the 8 desktop extraction commits passes cargo build, cargo fmt --check, cargo clippy --all-targets -- -D warnings, and cargo test independently."
    - "After Plan 02 completes, src/main.rs no longer contains an inline `mod desktop { ... }` block — only `#[cfg(windows)] mod desktop;` declaration."
    - "The directory src/desktop/ contains exactly 8 files: mod.rs, ffi.rs, state.rs, theme.rs, paint.rs, shell.rs, tabs.rs, treemap.rs."
    - "src/desktop/tabs.rs and src/desktop/treemap.rs are locked-name placeholders (each a single //! doc comment) per RESEARCH Q1 and CONTEXT D-02."
    - "desktop::run(PathBuf) -> io::Result<()> remains the only `pub(crate)` symbol exposed by the desktop module to the rest of the crate."
    - "Win32 message-ID constants WM_SCAN_DONE = WM_APP+7 and WM_SCAN_PROGRESS = WM_APP+8 live in src/desktop/ffi.rs (per CONTEXT cross-boundary rule)."
    - "ScanDone and ScanProgressInfo boxed-payload structs live in src/desktop/state.rs (per RESEARCH Q3 recommendation)."
    - "No new dependencies in Cargo.toml [dependencies] (still empty)."
  artifacts:
    - path: "src/desktop/mod.rs"
      provides: "Facade + window_proc dispatcher + module declarations + pub(crate) fn run"
      contains: "pub(crate) fn run"
    - path: "src/desktop/ffi.rs"
      provides: "Raw Win32 type aliases, structs, constants, and all 7-8 #[link(name=...)] extern \"system\" blocks"
      contains: "WM_SCAN_DONE"
    - path: "src/desktop/state.rs"
      provides: "DesktopState struct, STATE OnceLock, with_state_mut, ScanDone, ScanProgressInfo"
      contains: "pub(super) struct DesktopState"
    - path: "src/desktop/theme.rs"
      provides: "DARK_BRUSH, LIGHT_BRUSH, DARK_MODE_ATOMIC, apply_theme, all palette_* helpers"
      contains: "DARK_MODE_ATOMIC"
    - path: "src/desktop/paint.rs"
      provides: "paint_window, GDI double-buffering primitives, format_*_ui helpers"
      contains: "pub(super) unsafe fn paint_window"
    - path: "src/desktop/shell.rs"
      provides: "enable_visual_styles, icon_for_node, show_shell_context_menu, copy_to_clipboard, show_error_in_thread"
      contains: "pub(super) unsafe fn show_shell_context_menu"
    - path: "src/desktop/tabs.rs"
      provides: "Locked-name placeholder for Phase 8 per-tab content panels"
      contains: "//! Toolbar tab strip"
    - path: "src/desktop/treemap.rs"
      provides: "Locked-name placeholder for Phase 8 VIZ-01 native treemap"
      contains: "//! Native-desktop treemap"
  key_links:
    - from: "src/main.rs"
      to: "src/desktop/mod.rs"
      via: "#[cfg(windows)] mod desktop; (called from cli::run_desktop)"
      pattern: "#\\[cfg\\(windows\\)\\]\\s*mod desktop;"
    - from: "src/desktop/mod.rs (window_proc WM_PAINT arm)"
      to: "src/desktop/paint.rs"
      via: "paint::paint_window(hwnd)"
      pattern: "paint::paint_window"
    - from: "src/desktop/state.rs (ScanDone)"
      to: "src/desktop/mod.rs (start_scan_from_controls + WM_SCAN_DONE arm)"
      via: "Box::into_raw -> LPARAM -> Box::from_raw"
      pattern: "ScanDone"
    - from: "src/desktop/paint.rs"
      to: "src/desktop/theme.rs"
      via: "palette_* and dark_brush/light_brush imports"
      pattern: "use super::theme::"
    - from: "src/desktop/shell.rs"
      to: "src/desktop/paint.rs (wide helper)"
      via: "use super::paint::wide;"
      pattern: "use super::paint::wide"
---

<objective>
Mechanically split the inline `mod desktop { ... }` block (currently `src/main.rs:1767-4799`, ~3033 lines) into 8 files under `src/desktop/`, in the locked CONTEXT D-05 order: `state.rs` → `ffi.rs` → `theme.rs` → `paint.rs` → `shell.rs` → `tabs.rs` + `treemap.rs` (placeholders) → final `mod.rs` cleanup. Each commit passes the full CI quad. This plan depends on Plan 01 (all 8 top-level modules already extracted; `src/main.rs` is the lean facade plus the still-inline `mod desktop`).

Purpose: Per CONTEXT D-01/D-02 — fine split (not one monolithic `desktop/mod.rs`) for maximum bisect granularity (D-03) and so future phases (VIZ-01 treemap in Phase 8, per-tab panels) land in focused files instead of compounding the 3000-line desktop block.

Output: A `src/desktop/` directory with 8 files. `src/main.rs` shrinks further: the inline `mod desktop { ... }` block is replaced by `#[cfg(windows)] mod desktop;`. `desktop::run(PathBuf) -> io::Result<()>` remains the only crate-visible entry point (called from `cli::run_desktop`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-module-split/01-CONTEXT.md
@.planning/phases/01-module-split/01-RESEARCH.md
@.planning/phases/01-module-split/01-PATTERNS.md
@.planning/phases/01-module-split/01-VALIDATION.md
@.planning/phases/01-module-split/01-01-SUMMARY.md
@CLAUDE.md
@src/main.rs

<interfaces>
<!-- After Plan 01: src/main.rs has the inline `mod desktop { ... }` block at lines 1767-4799 (approximately — line numbers will have shifted modestly after the top-level extractions, so executors must Grep for `mod desktop` to find the current location). -->
<!-- Each desktop submodule gets the FULL 8-line #![allow(...)] baseline copied from the current inline mod desktop opening (RESEARCH Pitfall 1). Tighten later if desired; do not tighten in Phase 1. -->
<!-- The default visibility for items shared across desktop/* siblings is `pub(super)`, NOT `pub(crate)`. Only `desktop::run` is `pub(crate)`. -->

Cross-boundary shape (from CONTEXT line 95 + RESEARCH "Cross-Boundary Types: scan ↔ desktop"):
- `cancel: Arc<AtomicBool>` — plain std type, constructed in `desktop/mod.rs::start_scan_from_controls`, passed to `scan::scan_path_with_progress`. No wrapper.
- `WM_SCAN_DONE = WM_APP + 7` and `WM_SCAN_PROGRESS = WM_APP + 8` — live in `desktop/ffi.rs` (desktop owns the Win32 namespace).
- `ScanDone { result: Result<ScanResult, String>, canceled: bool }` and `ScanProgressInfo { node_count, elapsed_ms, partial_result }` — live in `desktop/state.rs`. Used only inside `desktop/`.
- `ScanResult` flows in from `crate::model::ScanResult` via the `ScanDone` payload.

window_proc dispatch shape (RESEARCH §"`window_proc` Dispatcher Pattern" + CONTEXT D-02):
```
unsafe extern "system" fn window_proc(hwnd: Hwnd, msg: Uint, wparam: Wparam, lparam: Lparam) -> Lresult {
    match msg {
        WM_PAINT => { paint::paint_window(hwnd); 0 }
        WM_LBUTTONDOWN => { handle_mouse_click(hwnd, lparam, false); 0 }   // stays in mod.rs
        WM_CTLCOLOREDIT | WM_CTLCOLORSTATIC | WM_CTLCOLORBTN => { theme::ctl_color(...) }
        ...
    }
}
```
NO trait, NO enum-of-handlers — move-only refactor. Mouse/keyboard/command/scan-message arms stay inline in `mod.rs` as the dispatch hub itself.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert inline `mod desktop` to `#[cfg(windows)] mod desktop;` + create src/desktop/mod.rs (commit 9)</name>
  <files>src/main.rs, src/desktop/mod.rs (new), src/desktop/ (new directory)</files>
  <read_first>
    - src/main.rs (full file — use Grep to locate the current line range of `mod desktop {`; it has shifted from 1767 due to Plan 01's extractions)
    - .planning/phases/01-module-split/01-CONTEXT.md §"D-01" and §"D-02" (fine split + target layout)
    - .planning/phases/01-module-split/01-RESEARCH.md §"Inline `mod desktop` `#![allow(...)]` Audit" (the 8-line baseline)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/mod.rs" (full symbol inventory + the file header pattern)
  </read_first>
  <action>
    This is a **single-commit relocation** that lifts the entire current inline `mod desktop { ... }` body out of `src/main.rs` into a new `src/desktop/mod.rs` file, with zero internal restructuring. No submodules yet — those land in commits 10-15. **Steps:** (1) Create directory `src/desktop/`. (2) Create `src/desktop/mod.rs`. (3) Copy the ENTIRE contents currently inside `mod desktop { ... }` in `src/main.rs` (i.e. everything between the `{` and matching `}`, including the 8 `#![allow(...)]` directives at the top, `use super::*;`, the targeted `std` imports, and every type/fn/impl) verbatim into `src/desktop/mod.rs`. (4) Replace `use super::*;` with explicit `use crate::model::*; use crate::scan::scan_path_with_progress; use crate::io::reveal_path;` per CONTEXT "Established Patterns" line 88 (the inline `mod` convention `use super::*;` does not scale to a directory module). Verify by Grep that no `super::SomeSymbol` reference remains in the body — every cross-module use must come from `crate::*`. (5) Mark `pub fn run` as `pub(crate) fn run` (per RESEARCH desktop public-surface row). (6) In `src/main.rs`: replace the entire `#[cfg(windows)] mod desktop { ... }` block with the single line `#[cfg(windows)] mod desktop;`. The `mod desktop;` declaration belongs in alphabetical order among the other `mod` declarations (alphabetically `desktop` falls between `cli` and `diff`; the existing form keeps it grouped under `#[cfg(windows)]` so place it after `mod server;` or wherever Plan 01 left it — confirm by reading the post-Plan-01 `src/main.rs`). Do NOT yet add `mod ffi;` / `mod state;` / etc. inside `src/desktop/mod.rs` — those happen in commits 10-15 below. **Do not split anything internally yet** — this commit is pure file-relocation. Commit message: `refactor(01): hoist mod desktop block into src/desktop/mod.rs`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/mod.rs` exists and contains `pub(crate) fn run` and the 8-line `#![allow(...)]` baseline at the top
    - `src/desktop/mod.rs` no longer contains `use super::*;` (replaced with explicit `use crate::...` imports)
    - `src/main.rs` no longer contains an inline `mod desktop { ... }` block — only `#[cfg(windows)] mod desktop;` declaration
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): hoist mod desktop`
  </acceptance_criteria>
  <done>The desktop module is now a directory-form module. `cli::run_desktop` still reaches it via `desktop::run`. Zero behavior change. Internal split begins in the next task.</done>
</task>

<task type="auto">
  <name>Task 2: Extract desktop/state.rs (commit 10)</name>
  <files>src/desktop/state.rs (new), src/desktop/mod.rs (modified)</files>
  <read_first>
    - src/desktop/mod.rs (the just-relocated file — Grep for `struct DesktopState`, `STATE: OnceLock`, `fn with_state_mut`, `struct ScanDone`, `struct ScanProgressInfo`)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/state.rs" (symbol inventory, top-of-file imports, visibility annotations)
    - .planning/phases/01-module-split/01-RESEARCH.md §"`pub(crate)` Visibility Matrix" → `desktop::state` row
    - .planning/phases/01-module-split/01-RESEARCH.md Q3 (rationale for ScanDone/ScanProgressInfo placement)
  </read_first>
  <action>
    Create `src/desktop/state.rs`. Copy these symbols verbatim from `src/desktop/mod.rs` into the new file: `DesktopState` struct + `DesktopState::new` impl block, `STATE: OnceLock<Mutex<DesktopState>>` static, `with_state_mut<T>(...)` helper. ALSO extract the struct definitions `ScanDone { result: Result<ScanResult, String>, canceled: bool }` and `ScanProgressInfo { node_count, elapsed_ms, partial_result }` per RESEARCH Q3 recommendation (they currently live near `start_scan_from_controls` and `finish_scan` inside `mod.rs` — extract just the struct definitions, NOT the construction sites which stay in `mod.rs`). File header: copy the 8-line `#![allow(...)]` baseline (RESEARCH Pitfall 1). Top-of-file imports per PATTERNS.md §"src/desktop/state.rs": `use std::path::PathBuf; use std::sync::{Arc, Mutex, OnceLock}; use std::sync::atomic::AtomicBool; use crate::model::ScanResult; use super::ffi::{Hbrush, Hfont, Hicon, Hmenu, Hwnd /* whichever Hwnd children DesktopState stores */};` — the `super::ffi::*` import will NOT yet resolve because `ffi.rs` is extracted in the NEXT commit. Concretely: at this commit, ffi types still live inline at the top of `src/desktop/mod.rs`, so write `use super::{Hbrush, Hfont, Hicon, Hmenu, Hwnd, ...};` — the items are in the parent module's scope. The commit-11 `ffi.rs` extraction will flip these imports to `use super::ffi::{...};`. Visibility: `pub(super)` on `DesktopState`, every DesktopState field (paint and shell read them), `STATE`, `with_state_mut`, `ScanDone`, `ScanProgressInfo`. In `src/desktop/mod.rs`: delete the moved symbols, add `mod state; use state::{DesktopState, STATE, ScanDone, ScanProgressInfo, with_state_mut};` near the top of `mod.rs` (after the module declarations block when it grows). Commit message: `refactor(01): extract desktop/state submodule`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/state.rs` exists and contains `pub(super) struct DesktopState`, `pub(super) static STATE`, `pub(super) fn with_state_mut`, `pub(super) struct ScanDone`, `pub(super) struct ScanProgressInfo`
    - `src/desktop/state.rs` starts with the 8-line `#![allow(...)]` baseline
    - `src/desktop/mod.rs` no longer contains `struct DesktopState` or `fn with_state_mut` (only `mod state;` + `use state::*;`)
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): extract desktop/state`
  </acceptance_criteria>
  <done>DesktopState and the cross-thread boxed payloads live in `src/desktop/state.rs`. The CI quad is green.</done>
</task>

<task type="auto">
  <name>Task 3: Extract desktop/ffi.rs (commit 11)</name>
  <files>src/desktop/ffi.rs (new), src/desktop/mod.rs (modified), src/desktop/state.rs (import update)</files>
  <read_first>
    - src/desktop/mod.rs (use Grep to enumerate the contiguous block of type aliases, #[repr(C)] structs, const declarations, and #[link(name=...)] extern "system" blocks at the top of the file)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/ffi.rs" (full inventory — this is the largest single move in Plan 02, ~600 lines)
    - .planning/phases/01-module-split/01-RESEARCH.md §"`pub(crate)` Visibility Matrix" → `desktop::ffi` row (`pub(super)` on every item is the blanket policy)
    - .planning/phases/01-module-split/01-RESEARCH.md Pitfall 5 (forgetting `pub(super)` on a sibling-used const)
    - .planning/phases/01-module-split/01-RESEARCH.md Pitfall 1 (non_snake_case allow needed for Win32 field names like `cbSize`, `dwFlags`)
  </read_first>
  <action>
    Create `src/desktop/ffi.rs`. Copy these contents verbatim from `src/desktop/mod.rs` per PATTERNS.md §"src/desktop/ffi.rs": ALL type aliases (`Bool`, `Dword`, `Hbrush`, `Hcursor`, `Hdc`, `Hfont`, `Hicon`, `Hinstance`, `Hmenu`, `Hgdobj`, `Hwnd`, `Lparam`, `Lresult`, `Uint`, `Wparam`, `Handle`, `UlongPtr`); ALL `#[repr(C)]` structs (`ACTCTXW`, `Rect`, `Point`, `PAINTSTRUCT`, `Msg`, `WndClassExW`, `BITMAPINFO`, `BITMAPINFOHEADER`, `GUID`, `ITEMIDLIST`, `CMINVOKECOMMANDINFO`, `IUnknownVtbl`, `IShellFolderVtbl`, `IContextMenuVtbl`); ALL consts (`WM_*` family including `WM_APP`, `WM_SCAN_DONE = WM_APP + 7`, `WM_SCAN_PROGRESS = WM_APP + 8`; `WS_*`, `CS_*`, `DT_*`, `FILE_ATTRIBUTE_*`, `BS_*`, `IDC_*`, `IDI_*`, `SHGFI_*`, `MB_*`, `LR_*`, `SW_*`, `TRANSPARENT`, `TPM_RETURNCMD`, `COINIT_APARTMENTTHREADED`, `BM_GETCHECK`, all `ID_SCAN_BUTTON`..`ID_DARK_CHECK` + `ID_MENU_*` ids); ALL seven `#[link(name="User32"|"Gdi32"|"Shell32"|"Comctl32"|"Dwmapi"|"Ole32"|"UxTheme")] unsafe extern "system"` blocks. The `#[link(name="Kernel32")]` block for `GetCompressedFileSizeW` already lives in `src/io.rs` after Plan 01 — DO NOT duplicate it here. Move the `IID_IShellFolder`, `IID_IContextMenu` GUID constants. **File header (mandatory — RESEARCH Pitfall 1 + 5):** Apply the 8-line `#![allow(...)]` baseline. The single required `use`: `use std::ffi::c_void;`. No `crate::` imports — ffi is the leaf of the `desktop/` subtree. **Visibility (RESEARCH Pitfall 5):** mark EVERY type alias, struct, const, and extern declaration `pub(super)` — blanket policy avoids whack-a-mole. Per CONTEXT line 95 the message-ID consts `WM_SCAN_DONE` and `WM_SCAN_PROGRESS` MUST land in this file (desktop owns the Win32 namespace; `scan` never sees them). In `src/desktop/mod.rs`: delete every moved declaration, add `mod ffi; use ffi::*;` to the module declaration block. In `src/desktop/state.rs`: flip the temporary `use super::{Hbrush, Hfont, ...};` import (added in commit 10) to `use super::ffi::{Hbrush, Hfont, Hicon, Hmenu, Hwnd, ...};` (whichever ffi types DesktopState fields actually reference). Commit message: `refactor(01): extract desktop/ffi submodule`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0 (canary for Pitfall 1 — `non_snake_case` allow on Win32 field names)
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/ffi.rs` exists and contains `pub(super) const WM_SCAN_DONE` (verifies CONTEXT cross-boundary rule)
    - `src/desktop/ffi.rs` contains 7 `#[link(name="...")]` blocks (User32, Gdi32, Shell32, Comctl32, Dwmapi, Ole32, UxTheme); NOT 8 (Kernel32 stays in src/io.rs)
    - `src/desktop/mod.rs` no longer contains `type Hwnd` or `const WM_PAINT` or `extern "system"` blocks
    - `src/desktop/state.rs` imports ffi types via `use super::ffi::{...};`
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): extract desktop/ffi`
  </acceptance_criteria>
  <done>All raw Win32 bindings (type aliases, structs, consts, extern blocks, GUIDs) live in `src/desktop/ffi.rs`. The message-ID constants are correctly placed here per CONTEXT. The CI quad is green.</done>
</task>

<task type="auto">
  <name>Task 4: Extract desktop/theme.rs (commit 12)</name>
  <files>src/desktop/theme.rs (new), src/desktop/mod.rs (modified)</files>
  <read_first>
    - src/desktop/mod.rs (Grep for `DARK_BRUSH`, `LIGHT_BRUSH`, `DARK_MODE_ATOMIC`, `apply_theme`, `update_column_widths`, `set_window_dark_mode`, `palette_`, `dark_brush`, `light_brush`, `rgb`)
    - src/desktop/ffi.rs (confirm Hbrush, Hwnd, Dword, CreateSolidBrush, DeleteObject etc. are `pub(super)`)
    - src/desktop/state.rs (confirm with_state_mut is `pub(super)`)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/theme.rs" (symbol inventory + top-of-file imports)
    - .planning/phases/01-module-split/01-RESEARCH.md §"`pub(crate)` Visibility Matrix" → `desktop::theme` row
  </read_first>
  <action>
    Create `src/desktop/theme.rs`. Copy these symbols verbatim from `src/desktop/mod.rs` per PATTERNS.md §"src/desktop/theme.rs": `DARK_BRUSH: OnceLock<Hbrush>`, `LIGHT_BRUSH: OnceLock<Hbrush>`, `DARK_MODE_ATOMIC: AtomicBool` (statics); `apply_theme`, `update_column_widths`, `set_window_dark_mode`, `dark_brush()`, `light_brush()`; ALL `palette_*` functions (`palette_bg`, `palette_panel`, `palette_table`, `palette_table_alt`, `palette_header`, `palette_line`, `palette_grid`, `palette_text`, `palette_muted`, `palette_selected`, `palette_hovered`, `palette_size_bar`, `palette_percent_track`, `palette_percent_fill`); the `rgb` helper if it exists as a standalone fn. File header: 8-line `#![allow(...)]` baseline. Top-of-file imports: `use std::sync::OnceLock; use std::sync::atomic::{AtomicBool, Ordering}; use super::ffi::*; use super::state::with_state_mut;`. Visibility per RESEARCH theme row: `pub(super)` on every `palette_*` (paint.rs consumes them in commit 13), `dark_brush`/`light_brush`/`apply_theme` (window_proc calls them), `DARK_MODE_ATOMIC` (window_proc reads it directly), `update_column_widths`, `set_window_dark_mode`. In `src/desktop/mod.rs`: delete the moved symbols, add `mod theme; use theme::{apply_theme, dark_brush, light_brush, DARK_MODE_ATOMIC, /* whichever palette_* are needed at the mod.rs callsites */};`. Commit message: `refactor(01): extract desktop/theme submodule`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/theme.rs` exists and contains `pub(super) static DARK_MODE_ATOMIC` and at least 10 `pub(super) fn palette_*` declarations
    - `src/desktop/mod.rs` no longer contains `fn palette_bg` or `static DARK_BRUSH`
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): extract desktop/theme`
  </acceptance_criteria>
  <done>Dark-mode statics + theme/palette helpers live in `src/desktop/theme.rs`, depend on `ffi` + `state`, and the CI quad is green.</done>
</task>

<task type="auto">
  <name>Task 5: Extract desktop/paint.rs (commit 13)</name>
  <files>src/desktop/paint.rs (new), src/desktop/mod.rs (modified)</files>
  <read_first>
    - src/desktop/mod.rs (Grep for `paint_window`, `draw_toolbar_background`, `draw_table`, `draw_header`, `draw_row`, `draw_name_cell`, `draw_percent_cell`, `fill_rect`, `draw_text`, `table_top`, `columns`, `loword_signed`, `hiword_signed`, `wide`, `format_bytes_ui`, `format_count_ui`, `format_duration_ui`)
    - src/desktop/ffi.rs, src/desktop/state.rs, src/desktop/theme.rs (all `pub(super)` consumers)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/paint.rs" (full symbol inventory + use block + stylistic analog)
    - .planning/phases/01-module-split/01-RESEARCH.md Code Examples §"Pattern: `desktop` submodule file header" (the canonical paint.rs skeleton at lines 488-510)
    - .planning/phases/01-module-split/01-RESEARCH.md Q2 (format_*_ui helpers stay in paint.rs, not a separate format.rs)
  </read_first>
  <action>
    Create `src/desktop/paint.rs`. Copy these symbols verbatim from `src/desktop/mod.rs` per PATTERNS.md §"src/desktop/paint.rs": `paint_window`, `draw_toolbar_background`, `draw_table`, `draw_header`, `draw_row`, `draw_name_cell`, `draw_percent_cell`, `fill_rect`, `draw_text`, `table_top`, `columns`, `loword_signed`, `hiword_signed`, `wide`, `format_bytes_ui`, `format_count_ui`, `format_duration_ui`. Per RESEARCH Q2, the three `format_*_ui` helpers stay HERE (not in a separate `desktop/format.rs`) — they're used only by paint + a handful of `mod.rs` orchestration helpers. File header: 8-line `#![allow(...)]` baseline. Top-of-file imports per PATTERNS.md: `use std::mem::{size_of, zeroed}; use std::ptr::null; use crate::io::epoch_ms_to_utc; use crate::model::{NodeRecord, ScanResult}; use super::ffi::*; use super::state::{DesktopState, with_state_mut}; use super::theme::{dark_brush, light_brush, palette_bg, palette_grid, palette_header, palette_hovered, palette_line, palette_muted, palette_panel, palette_percent_fill, palette_percent_track, palette_selected, palette_size_bar, palette_table, palette_table_alt, palette_text};`. Visibility per RESEARCH paint row: `pub(super)` on `paint_window` (window_proc calls it), `table_top` (handle_mouse_click uses it), `loword_signed`/`hiword_signed`/`wide`/`fill_rect`/`draw_text` (used by shell and mod.rs handlers), `format_bytes_ui`/`format_count_ui`/`format_duration_ui` (used by mod.rs status-text helpers). Internal helpers like `draw_row`, `draw_name_cell`, `draw_percent_cell`, `draw_toolbar_background`, `draw_table`, `draw_header`, `columns` can stay private to paint unless mod.rs Grep shows otherwise. In `src/desktop/mod.rs`: delete the moved symbols, add `mod paint; use paint::{paint_window, table_top, loword_signed, hiword_signed, wide, fill_rect, draw_text, format_bytes_ui, format_count_ui, format_duration_ui};`. Commit message: `refactor(01): extract desktop/paint submodule`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/paint.rs` exists and contains `pub(super) unsafe fn paint_window`
    - `src/desktop/paint.rs` contains `pub(super) fn format_bytes_ui`, `pub(super) fn format_count_ui`, `pub(super) fn format_duration_ui`
    - `src/desktop/mod.rs` no longer contains `fn paint_window` or `fn draw_row` or `fn format_bytes_ui`
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): extract desktop/paint`
  </acceptance_criteria>
  <done>All GDI double-buffered paint code lives in `src/desktop/paint.rs`, depends on `model` + `io` + `ffi` + `state` + `theme`, and the CI quad is green.</done>
</task>

<task type="auto">
  <name>Task 6: Extract desktop/shell.rs (commit 14)</name>
  <files>src/desktop/shell.rs (new), src/desktop/mod.rs (modified)</files>
  <read_first>
    - src/desktop/mod.rs (Grep for `enable_visual_styles`, `icon_for_node`, `show_shell_context_menu`, `copy_to_clipboard`, `show_error_in_thread`, icon-cache destruction helpers, ACTCTX manifest activation, SHGetFileInfoW, IShellFolder vtable invocation)
    - src/desktop/ffi.rs, src/desktop/state.rs, src/desktop/paint.rs (paint exports the `wide` helper that shell uses)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/shell.rs" (symbol inventory + use block)
    - .planning/phases/01-module-split/01-RESEARCH.md §"`pub(crate)` Visibility Matrix" → `desktop::shell` row
  </read_first>
  <action>
    Create `src/desktop/shell.rs`. Copy these symbols verbatim from `src/desktop/mod.rs` per PATTERNS.md §"src/desktop/shell.rs": `enable_visual_styles` (ACTCTX manifest activation), `icon_for_node` (Shell32 `SHGetFileInfoW`), `show_shell_context_menu` (IShellFolder/IContextMenu vtable dance), `copy_to_clipboard`, `show_error_in_thread`, plus any icon-cache destruction helpers in the same area. File header: 8-line `#![allow(...)]` baseline. Top-of-file imports per PATTERNS.md: `use std::ffi::{OsStr, c_void}; use std::mem::{size_of, zeroed}; use std::os::windows::ffi::OsStrExt; use std::path::Path; use std::ptr::{null, null_mut}; use super::ffi::*; use super::paint::wide; use super::state::with_state_mut;`. Visibility per RESEARCH shell row: `pub(super)` on `enable_visual_styles`, `icon_for_node`, `show_shell_context_menu`, `copy_to_clipboard`, `show_error_in_thread`. In `src/desktop/mod.rs`: delete the moved symbols, add `mod shell; use shell::{enable_visual_styles, icon_for_node, show_shell_context_menu, copy_to_clipboard, show_error_in_thread};`. Commit message: `refactor(01): extract desktop/shell submodule`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/shell.rs` exists and contains `pub(super) unsafe fn show_shell_context_menu`
    - `src/desktop/shell.rs` contains `pub(super) unsafe fn enable_visual_styles` and `pub(super) unsafe fn icon_for_node`
    - `src/desktop/mod.rs` no longer contains `fn show_shell_context_menu` or `fn icon_for_node`
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): extract desktop/shell`
  </acceptance_criteria>
  <done>COM + Shell32 glue lives in `src/desktop/shell.rs`, depends on `ffi` + `state` + `paint::wide`, and the CI quad is green.</done>
</task>

<task type="auto">
  <name>Task 7: Add desktop/tabs.rs and desktop/treemap.rs placeholders (commit 15)</name>
  <files>src/desktop/tabs.rs (new), src/desktop/treemap.rs (new), src/desktop/mod.rs (modified)</files>
  <read_first>
    - src/desktop/mod.rs (current state — confirm post-shell extraction)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/tabs.rs", §"src/desktop/treemap.rs", §"Pattern F — Empty-file placeholder for locked module names"
    - .planning/phases/01-module-split/01-RESEARCH.md Q1 (tabs.rs stays placeholder — toolbar tab logic stays inside paint/mod for Phase 1)
    - .planning/phases/01-module-split/01-RESEARCH.md Assumption A2 (empty-with-doc-comment file passes clippy)
    - .planning/phases/01-module-split/01-CONTEXT.md §"D-02" (tabs.rs and treemap.rs are locked names in the layout)
  </read_first>
  <action>
    Create `src/desktop/tabs.rs` containing exactly one line: `//! Toolbar tab strip rendering and click dispatch. Empty in Phase 1; populated in Phase 8 (alongside per-tab content panels).` Create `src/desktop/treemap.rs` containing exactly one line: `//! Native-desktop treemap rendering. Empty in Phase 1; populated in Phase 8 (VIZ-01).` Per RESEARCH Q1, do NOT extract the toolbar-tab fragments from `paint.rs` / `handle_mouse_click` / `resize_controls` — that's deferred to Phase 8. Add `mod tabs; mod treemap;` to the module declaration block in `src/desktop/mod.rs` (alphabetically between the other `mod` lines). **Fallback for clippy (RESEARCH Assumption A2):** run `cargo clippy --all-targets -- -D warnings` first. If it fails on the empty form for either file, append `pub(super) fn _placeholder() {}` to whichever placeholder file triggered the lint. Commit message: `refactor(01): add desktop/tabs and desktop/treemap placeholders for Phase 8`.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/tabs.rs` exists and is at most 2 lines (one doc comment, optional placeholder fn)
    - `src/desktop/treemap.rs` exists and is at most 2 lines (one doc comment, optional placeholder fn)
    - `src/desktop/mod.rs` contains both `mod tabs;` and `mod treemap;`
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): add desktop/tabs and desktop/treemap placeholders`
  </acceptance_criteria>
  <done>Both Phase 8 module names are locked. The CI quad is green.</done>
</task>

<task type="auto">
  <name>Task 8: Final desktop/mod.rs cleanup pass (commit 16)</name>
  <files>src/desktop/mod.rs (modified, possibly other desktop/*.rs files)</files>
  <read_first>
    - src/desktop/mod.rs (current state — should now contain `pub(crate) fn run`, `window_proc`, control creation/resize, scan orchestration, mouse/keyboard handlers, and module declarations)
    - src/desktop/paint.rs, src/desktop/shell.rs, src/desktop/theme.rs, src/desktop/state.rs (confirm all `pub(super)` exports listed in PATTERNS.md are present)
    - .planning/phases/01-module-split/01-CONTEXT.md §"D-02" (the mod.rs facade contract)
    - .planning/phases/01-module-split/01-RESEARCH.md §"`window_proc` Dispatcher Pattern" (the post-split window_proc shape)
    - .planning/phases/01-module-split/01-RESEARCH.md §"Extraction Map → desktop/mod.rs" (the remaining symbols mod.rs should own)
    - .planning/phases/01-module-split/01-PATTERNS.md §"src/desktop/mod.rs" (the canonical post-split contents)
  </read_first>
  <action>
    Audit `src/desktop/mod.rs` against PATTERNS.md §"src/desktop/mod.rs" expected contents. **Required final state:** (1) Top of file: 8-line `#![allow(...)]` baseline. (2) Module declaration block (alphabetical): `mod ffi; mod paint; mod shell; mod state; mod tabs; mod theme; mod treemap;`. (3) Imports: `use ffi::*;` plus selective imports from `paint`, `shell`, `state`, `theme` per PATTERNS.md. (4) Items that REMAIN in `mod.rs` per RESEARCH §"Extraction Map → desktop/mod.rs": `pub(crate) fn run`, `unsafe extern "system" fn window_proc`, `create_controls`, `create_child`, `resize_controls`, `start_scan_from_controls`, `finish_scan`, `apply_scan_progress`, `stop_current_scan`, `destroy_cached_icons`, `expand_all_directories`, `collapse_to_root`, `toggle_path_column`, `choose_and_set_directory`, `browse_for_directory`, `handle_mouse_click`, `handle_right_click`, `handle_mouse_wheel`, `handle_mouse_move`, `handle_key`, `move_selection`, `scroll_rows`, `render_list`, `collect_rows`, `button_checked`, `get_window_text`, `set_window_text`, `show_error`. **Verify the window_proc dispatch shape (RESEARCH §"window_proc Dispatcher Pattern"):** WM_PAINT delegates to `paint::paint_window`; WM_CTLCOLOR* delegates to a small `theme::ctl_color` helper if extracted (otherwise stays inline — verify behavior). Mouse/keyboard/command/scan-message arms stay inline in `mod.rs`. NO trait, NO enum-of-handlers. Migrate any obvious stragglers found during audit (e.g. if `paint.rs` was incomplete in commit 13 and trailing helpers belong there, move them now). Per CONTEXT line 88 + RESEARCH "Established Patterns", confirm `src/desktop/mod.rs` uses explicit `use crate::model::*;` / `use crate::io::reveal_path;` / `use crate::scan::scan_path_with_progress;` — NOT `use super::*;` or wildcard. Per the comment in §"Inline `mod desktop` `#![allow(...)]` Audit": if any submodule was given the full 8-line baseline but demonstrably needs a narrower set, this commit may trim per-file allows — but ONLY if it doesn't risk regressing clippy. Recommended: leave the baselines as-is (CONTEXT defers cleanup). Commit message: `refactor(01): finalize desktop/mod.rs facade`.
  </action>
  <verify>
    <automated>cargo build --release &amp;&amp; cargo fmt --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` exits 0
    - `cargo fmt --check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo test` exits 0
    - `src/desktop/mod.rs` contains `pub(crate) fn run` and `unsafe extern "system" fn window_proc`
    - `src/desktop/mod.rs` contains module declarations for `ffi, paint, shell, state, tabs, theme, treemap` (7 mod declarations)
    - `src/desktop/mod.rs` does NOT contain `use super::*;` or `use crate::*;` wildcards (explicit imports only per CONTEXT)
    - `ls src/desktop/` shows exactly 8 files: mod.rs, ffi.rs, state.rs, theme.rs, paint.rs, shell.rs, tabs.rs, treemap.rs
    - `Cargo.toml` `[dependencies]` remains empty
    - Single git commit with message starting `refactor(01): finalize desktop/mod.rs facade`
  </acceptance_criteria>
  <done>`src/desktop/mod.rs` is a lean facade: module declarations + selective imports + `pub(crate) fn run` + `window_proc` dispatcher + the scan-orchestration and event-handler helpers per CONTEXT D-02. The 8-file `src/desktop/` directory is complete. The release CI quad is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Move-only refactor of an inline module into a directory module. No new code, no new network listeners, no new file writes. Win32 trust boundaries (window message dispatch, COM vtable calls, Shell context menu invocation) are preserved verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T (Tampering) | `desktop/tabs.rs` + `desktop/treemap.rs` placeholders | accept | Doc-comment-only files are inert; cannot be exploited. RESEARCH A2 confirms compile + clippy safety. |
| T-01-05 | E (Elevation of Privilege) | Cross-thread `Box::into_raw` -> LPARAM -> `Box::from_raw` for `ScanDone`/`ScanProgressInfo` | accept | This unsafe pattern existed pre-refactor; this plan moves the struct definitions into `state.rs` but does not modify the marshalling code. Behavior preserved. |
| T-01-06 | I (Information Disclosure) | `pub(super)` visibility leak | mitigate | RESEARCH Pitfall 5 + Task 3 (ffi) blanket-`pub(super)` policy. Every cross-submodule item is `pub(super)` (visible only within `desktop/`), NOT `pub(crate)`. Only `desktop::run` is `pub(crate)`. Verified by inspecting Task 8 acceptance criteria. |

This plan introduces no new attack surface. Same Win32 code, new file layout.
</threat_model>

<verification>
After all 8 tasks complete:
- `git log --oneline | head -8` shows 8 commits with `refactor(01):` prefix covering desktop hoist + 7 submodule extractions + final mod.rs cleanup.
- `ls src/desktop/` shows exactly 8 files: `mod.rs`, `ffi.rs`, `state.rs`, `theme.rs`, `paint.rs`, `shell.rs`, `tabs.rs`, `treemap.rs`.
- `wc -l src/main.rs` shows the lean ~15-line facade (the inline `mod desktop` block is gone — only `#[cfg(windows)] mod desktop;` remains).
- `wc -l src/desktop/mod.rs` is roughly ~1500 lines per RESEARCH sizing note (still the largest desktop file because window_proc + scan orchestration + control plumbing form one cohesive unit).
- `wc -l src/desktop/tabs.rs src/desktop/treemap.rs` shows 1-2 lines each (locked-name placeholders).
- `grep -n 'WM_SCAN_DONE' src/desktop/ffi.rs` finds the const (verifies CONTEXT cross-boundary placement).
- `grep -nv '^//' src/desktop/tabs.rs` has zero or one matching line (placeholder is doc-only or doc + `_placeholder()` fallback).
- `cargo tree --depth 0` shows only `filetree v0.1.0` (no new dependencies).
</verification>

<success_criteria>
- 8 atomic commits land sequentially in CONTEXT D-05 order: desktop hoist → state → ffi → theme → paint → shell → tabs+treemap (placeholders) → final mod.rs cleanup.
- Every commit passes the four-command CI quad.
- `src/desktop/` is a directory module with exactly the 8 files locked in CONTEXT D-02.
- `desktop::run` is the only `pub(crate)` symbol exposed from `desktop/` to the rest of the crate; sibling-shared items are `pub(super)`.
- `WM_SCAN_DONE`/`WM_SCAN_PROGRESS` live in `desktop/ffi.rs`; `ScanDone`/`ScanProgressInfo` live in `desktop/state.rs`.
- `src/main.rs` no longer contains an inline `mod desktop { ... }` block.
- `Cargo.toml [dependencies]` remains empty.
- The release build (`cargo build --release`) passes — canary for any `#[link]` block regression.
</success_criteria>

<output>
After completion, create `.planning/phases/01-module-split/01-02-SUMMARY.md` capturing:
- Per-commit hash + desktop submodule extracted (8 entries: hoist, state, ffi, theme, paint, shell, tabs+treemap placeholders, mod.rs finalization)
- Final line count of `src/desktop/mod.rs` (expected ~1500 per RESEARCH)
- Confirmation that `src/desktop/tabs.rs` and `src/desktop/treemap.rs` are 1-2 lines each
- Confirmation that `WM_SCAN_DONE` and `WM_SCAN_PROGRESS` are defined in `src/desktop/ffi.rs`
- Confirmation that `ScanDone` and `ScanProgressInfo` are defined in `src/desktop/state.rs`
- Any per-file `#![allow(...)]` deviations from the 8-line baseline (none expected)
- Any unexpected visibility promotions needed beyond `pub(super)` (e.g. if any item had to become `pub(in crate::desktop)`)
- Handoff note: "Phase 1 mechanical extraction is complete. Plan 03 verifies behavioral parity via the manual smoke checklist."
</output>
