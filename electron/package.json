{
  "name": "filetree",
  "version": "0.2.0",
  "description": "FileTree disk-usage explorer",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && npm run build:native",
    "build:native": "node scripts/build-native.mjs",
    "start": "npm run build && electron .",
    "dev": "tsc && electron ."
  },
  "dependencies": {
    "electron": "^33.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
